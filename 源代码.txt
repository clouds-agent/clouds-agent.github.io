
<!DOCTYPE html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no"><title>Chart Editor v0.5.7</title>
<meta http-equiv="Cache-Control" content="no-cache,no-store,must-revalidate"><script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
<style>:root{--bg:#0b0c10;--bg-panel:#1f2833;--bg-hover:#2a3a4b;--border:#374151;--accent:#66fcf1;--accent-dim:#45a29e;--text:#c5c6c7;--text-dim:#80868b;--danger:#ef4444;--lane-odd:rgba(102,252,241,0.04);--lane-even:rgba(102,252,241,0.08);--playhead:#ff2a6d;--measure:#4a5568;--beat:#2d3748;--snap:#1a202c;--note-tap:#66fcf1;--note-hold:#f1c40f;--select:#ff2a6d;}*{box-sizing:border-box;margin:0;padding:0;}html,body{width:100%;height:100%;background:var(--bg);color:var(--text);font-family:'Segoe UI','PingFang SC','Microsoft YaHei',sans-serif;overflow:hidden;user-select:none;-webkit-user-select:none;}#orientation-lock{display:none;position:fixed;inset:0;z-index:9999;background:#000;align-items:center;justify-content:center;text-align:center;}@media (orientation:portrait){#orientation-lock{display:flex;}#app{display:none !important;}}.lock-content{color:#fff;}.lock-icon{font-size:48px;margin-bottom:12px;}.lock-content p{font-size:16px;opacity:0.8;margin:4px 0;}#app{display:flex;flex-direction:column;width:100%;height:100%;}#toolbar{display:flex;align-items:center;gap:10px 14px;padding:6px 12px;background:var(--bg-panel);border-bottom:1px solid var(--border);flex-shrink:0;flex-wrap:wrap;overflow-x:hidden;overflow-y:visible;max-height:none;}.tool-group{display:flex;align-items:center;gap:4px;white-space:nowrap;padding:2px 8px;border-right:1px solid var(--border);flex-shrink:0;}.tool-group:last-child{border-right:none;}.tool-group:last-child{border-right:none;}.tool-group label{font-size:12px;color:var(--text-dim);}#toolbar button{background:var(--bg-hover);border:1px solid var(--border);color:var(--text);padding:4px 10px;border-radius:4px;cursor:pointer;font-size:13px;transition:background .15s,border-color .15s;}#toolbar button:hover{background:#35495e;border-color:var(--accent-dim);}#toolbar button.active{border-color:var(--accent);color:var(--accent);}#toolbar button.danger{border-color:var(--danger);color:var(--danger);}#toolbar button.danger:hover{background:rgba(239,68,68,0.15);}#toolbar input[type="number"],#toolbar select,#input-custom-snap{background:var(--bg);border:1px solid var(--border);color:var(--text);padding:3px 6px;border-radius:3px;font-size:13px;width:60px;}#toolbar select{width:auto;}#input-zoom{width:80px;}#select-rate{width:64px;}#input-bpm{width:54px;}#input-custom-snap{width:70px;}#time-display{font-variant-numeric:tabular-nums;font-size:13px;color:var(--accent);}#beat-display{font-size:13px;color:var(--text-dim);}#main-area{display:flex;flex:1;overflow:hidden;}#canvas-container{flex:1;position:relative;overflow:hidden;background:var(--bg);}#editor-canvas{display:block;width:100%;height:100%;cursor:crosshair;}#drag-ghost{position:absolute;pointer-events:none;display:none;background:var(--accent);opacity:0.6;border-radius:2px;}#side-panel{width:240px;background:var(--bg-panel);border-left:1px solid var(--border);padding:12px;overflow-y:auto;flex-shrink:0;}#panel-placeholder{color:var(--text-dim);font-size:13px;text-align:center;margin-top:20px;}.panel-section h3{font-size:14px;color:var(--accent);margin-bottom:10px;padding-bottom:6px;border-bottom:1px solid var(--border);}.panel-section .field{margin-bottom:10px;display:flex;align-items:center;gap:6px;}.panel-section label{font-size:12px;color:var(--text-dim);width:80px;flex-shrink:0;}.panel-section input{flex:1;background:var(--bg);border:1px solid var(--border);color:var(--text);padding:4px 6px;border-radius:3px;font-size:13px;}.panel-section .actions{margin-top:12px;}.panel-section button{background:var(--bg-hover);border:1px solid var(--border);color:var(--text);padding:4px 10px;border-radius:4px;cursor:pointer;font-size:13px;}.panel-section button.danger{border-color:var(--danger);color:var(--danger);}#audio-bar{display:flex;align-items:center;gap:10px;padding:4px 12px;background:#14161c;border-top:1px solid var(--border);flex-shrink:0;}#audio-progress-bg{flex:1;position:relative;height:10px;background:#2a2f3a;border-radius:5px;cursor:pointer;overflow:hidden;}#audio-progress-fill{position:absolute;left:0;top:0;bottom:0;background:linear-gradient(90deg,var(--accent-dim),var(--accent));width:0%;border-radius:5px;pointer-events:none;}#audio-progress-thumb{position:absolute;top:-3px;width:6px;height:16px;background:var(--accent);border-radius:3px;left:0%;transform:translateX(-50%);pointer-events:none;box-shadow:0 0 6px rgba(102,252,241,0.5);}#audio-time{font-size:11px;color:var(--text-dim);font-variant-numeric:tabular-nums;white-space:nowrap;}#status-bar{display:flex;align-items:center;gap:16px;padding:4px 12px;background:var(--bg-panel);border-top:1px solid var(--border);font-size:12px;color:var(--text-dim);flex-shrink:0;}#status-help{margin-left:auto;}::-webkit-scrollbar{width:6px;height:6px;}::-webkit-scrollbar-track{background:var(--bg);}::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px;}::-webkit-scrollbar-thumb:hover{background:#4a5568;}</style></head><body>
<div id="orientation-lock"><div class="lock-content"><div class="lock-icon">📱➡️🖥️</div><p>请使用横屏模式以获得最佳体验</p></div></div>
<div id="app">
<header id="toolbar">
<div class="tool-group"><button id="btn-play" title="播放/暂停(Space)">▶</button><button id="btn-stop" title="停止">⏹</button><button id="btn-goto-start" title="跳到0拍">⏮开头</button><span id="time-display" style="color:var(--accent);font-variant-numeric:tabular-nums">00:00.000</span><span id="beat-display" style="color:var(--text-dim)">| Beat: 0.00</span></div>
<div class="tool-group"><button id="btn-metronome" title="节拍器">♩</button><button id="btn-loop" title="循环:Shift起点Alt终点">🔁</button><button id="btn-shift-up" title="NOW上移1格">▲NOW</button><button id="btn-shift-down" title="NOW下移1格">▼NOW</button></div>
<div class="tool-group"><label>BPM:</label><input id="input-bpm" type="number" value="120"><button id="btn-add-bpm" title="加BPM变化点">+BPM点</button><button id="btn-bpm-manage" title="BPM列表">BPM列表</button></div>
<div class="tool-group"><label>横线:</label><select id="select-snap"><option value="1">1/1</option><option value="2">1/2</option><option value="3">1/3</option><option value="4" selected>1/4</option><option value="6">1/6</option><option value="8">1/8</option><option value="12">1/12</option><option value="16">1/16</option><option value="32">1/32</option></select><input id="input-custom-snap" type="number" placeholder="自定义分母" min="1" max="64"></div>
<div class="tool-group"><label>缩放:</label><input id="input-zoom" type="range" min="0" max="100" value="50" title="左=整曲可见,右=1拍占满"></div>
<div class="tool-group"><label>倍速:</label><select id="select-rate"><option value="0.25">0.25x</option><option value="0.5">0.5x</option><option value="0.75">0.75x</option><option value="1" selected>1.0x</option><option value="1.25">1.25x</option><option value="1.5">1.5x</option><option value="2">2.0x</option></select></div>
<div class="tool-group"><button id="btn-info" title="编辑谱面信息">信息 <span id="info-title-preview" style="color:var(--accent);font-size:12px;max-width:80px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">(Untitled)</span></button></div>
<div class="tool-group"><label>Offsetms:</label><input id="input-offset" type="number" value="0" step="1" title="音频偏移(毫秒)"></div>
<div class="tool-group"><button id="btn-audio" title="导入音频文件">🎵 导入音频</button><input type="file" id="file-audio" accept="audio/*" hidden><button id="btn-import" title="导入谱面ZIP">📂 导入谱面</button><input type="file" id="file-import" accept=".zip" hidden><button id="btn-export" title="导出谱面ZIP">💾 导出ZIP</button></div>
<div class="tool-group"><button id="btn-edge-add" title="添加边线参数">+ 边线参数</button><button id="btn-edge-list" title="边线事件列表">边线列表</button><button id="btn-global-add" title="添加全局参数">+ 全局参数</button><button id="btn-global-list" title="全局事件列表">全局列表</button><button id="btn-preview" title="预览谱面">▶ 预览</button><button id="btn-mirror" title="左右镜像选中音符">镜像</button><button id="btn-validate" title="谱面校验">校验</button><button id="btn-undo" title="撤销(Ctrl+Z)">↩ 撤销</button><button id="btn-redo" title="重做(Ctrl+Y)">↪ 重做</button><span id="app-version" style="color:var(--accent);font-size:12px;font-weight:bold;margin-left:6px">v0.5.7</span></div>
</header>
<div id="main-area">
<div id="canvas-container"><canvas id="editor-canvas"></canvas><div id="drag-ghost"></div></div>
<aside id="side-panel">
<div id="panel-placeholder"><p>选中音符或事件以编辑参数</p></div>
<div id="note-panel" class="panel-section" style="display:none"><h3>音符参数</h3>
<div class="field"><label>时间:</label><span id="pn-beat" style="color:var(--accent)"></span></div>
<div class="field" id="pn-endbeat-wrap"><label>结束:</label><span id="pn-endbeat" style="color:var(--accent)"></span></div>
<div class="field"><label>流速倍率:</label><input id="pn-speed" type="number" value="1.0" step="0.1" style="width:70px"></div>
<div class="field"><label>缓动:</label><select id="pn-easing" style="width:130px"><option value="easeLinear">easeLinear</option><option value="easeInSine">easeInSine</option><option value="easeOutSine">easeOutSine</option><option value="easeInOutSine">easeInOutSine</option><option value="easeInQuad">easeInQuad</option><option value="easeOutQuad">easeOutQuad</option><option value="easeInOutQuad">easeInOutQuad</option><option value="easeInCubic">easeInCubic</option><option value="easeOutCubic">easeOutCubic</option><option value="easeInOutCubic">easeInOutCubic</option><option value="easeInQuart">easeInQuart</option><option value="easeOutQuart">easeOutQuart</option><option value="easeInOutQuart">easeInOutQuart</option><option value="easeInQuint">easeInQuint</option><option value="easeOutQuint">easeOutQuint</option><option value="easeInExpo">easeInExpo</option><option value="easeOutExpo">easeOutExpo</option><option value="easeInCirc">easeInCirc</option><option value="easeOutCirc">easeOutCirc</option><option value="easeInOutCirc">easeInOutCirc</option><option value="easeInBack">easeInBack</option><option value="easeOutBack">easeOutBack</option><option value="easeInOutBack">easeInOutBack</option><option value="easeInElastic">easeInElastic</option><option value="easeOutElastic">easeOutElastic</option><option value="easeInOutElastic">easeInOutElastic</option><option value="easeInBounce">easeInBounce</option><option value="easeOutBounce">easeOutBounce</option><option value="easeInOutBounce">easeInOutBounce</option></select></div>
<div class="field"><label style="width:auto"><input type="checkbox" <div class="field"><input id="pn-expr" type="text" placeholder="公式: sin(t*6.28)*0.5+1" style="width:100%;background:#0b0c10;color:#fff;border:1px solid #374151;padding:2px;font-size:10px;display:none"></div>
<div class="field"><button id="pn-expr-apply" style="font-size:10px;padding:1px 6px;display:none">采样</button></div>
<canvas id="pn-curve" width="200" height="80" style="width:100%;height:80px;background:#0b0c10;border:1px solid var(--border);border-radius:4px;cursor:crosshair;display:none"></canvas>
<input type="checkbox" id="pn-edge-lit"> 边线常亮控制</label></div>
<div class="field actions"><button id="pn-delete" class="danger">删除</button></div>
</div>
<div id="edge-panel" class="panel-section" style="display:none"><h3>边线参数 <span id="ep-edge-id" style="color:var(--accent-dim);font-size:12px"></span></h3>
<div class="field"><label>LO蔓延:</label><input id="ep-lo-spread" type="number" step="0.01" style="width:55px"><select id="ep-lo-spread-unit" style="width:46px"><option value="beat">拍</option><option value="sec">秒</option></select></div>
<div class="field"><label>LO渐变:</label><input id="ep-lo-grad" type="number" step="0.1" value="0" style="width:70px"></div>
<div class="field"><label>LF蔓延:</label><input id="ep-lf-spread" type="number" step="0.01" style="width:55px"><select id="ep-lf-spread-unit" style="width:46px"><option value="beat">拍</option><option value="sec">秒</option></select></div>
<div class="field"><label>LF渐变:</label><input id="ep-lf-grad" type="number" step="0.1" value="0" style="width:70px"></div>
<div class="field"><label>间隔:</label><input id="ep-interval" type="number" step="0.01" style="width:55px"><select id="ep-interval-unit" style="width:46px"><option value="beat">拍</option><option value="sec">秒</option></select></div>
<div class="field"><label>总时长:</label><span id="ep-duration" style="color:var(--text-dim)"></span></div>
<div class="field"><label>LO发光强度:</label><input id="ep-lo-glow-i" type="number" step="0.1" value="1" style="width:55px"></div>
<div class="field"><label>LO发光半径:</label><input id="ep-lo-glow-r" type="number" step="0.5" value="0" style="width:55px"></div>
<div class="field"><label>LF发光强度:</label><input id="ep-lf-glow-i" type="number" step="0.1" value="1" style="width:55px"></div>
<div class="field"><label>LF发光半径:</label><input id="ep-lf-glow-r" type="number" step="0.5" value="0" style="width:55px"></div>
<div class="field"><label style="width:auto"><input type="checkbox" id="ep-show-influence"> 显示影响范围</label></div>
<div class="field actions"><button id="ep-select-mode" style="font-size:12px">✋ 选择端点</button><button id="ep-clone" style="font-size:12px">克隆参数</button><button id="ep-delete" class="danger" style="font-size:12px">删除</button></div>
</div>
<div id="global-panel" class="panel-section" style="display:none"><h3>全局参数 <span id="gp-type" style="color:var(--accent-dim);font-size:12px"></span></h3>
<div class="field"><label>类型:</label><select id="gp-kind" style="width:100px"><option value="sv">SV</option><option value="camera">Camera</option><option value="censor">Censor</option></select></div>
<div class="field"><label>时长(拍):</label><input id="gp-duration" type="number" step="0.01" style="width:80px"></div>
<div id="gp-svcam">
<div class="field"><label>起始值:</label><input id="gp-start" type="number" step="0.01" style="width:80px"></div>
<div class="field"><label>结束值:</label><input id="gp-end" type="number" step="0.01" style="width:80px"></div>
<div class="field"><label>缓动:</label><select id="gp-easing" style="width:150px"><option value="easeLinear">easeLinear</option><option value="easeInSine">easeInSine</option><option value="easeOutSine">easeOutSine</option><option value="easeInOutSine">easeInOutSine</option><option value="easeInQuad">easeInQuad</option><option value="easeOutQuad">easeOutQuad</option><option value="easeInOutQuad">easeInOutQuad</option><option value="easeInCubic">easeInCubic</option><option value="easeOutCubic">easeOutCubic</option><option value="easeInOutCubic">easeInOutCubic</option><option value="easeInQuart">easeInQuart</option><option value="easeOutQuart">easeOutQuart</option><option value="easeInOutQuart">easeInOutQuart</option><option value="easeInQuint">easeInQuint</option><option value="easeOutQuint">easeOutQuint</option><option value="easeInExpo">easeInExpo</option><option value="easeOutExpo">easeOutExpo</option><option value="easeInCirc">easeInCirc</option><option value="easeOutCirc">easeOutCirc</option><option value="easeInOutCirc">easeInOutCirc</option><option value="easeInBack">easeInBack</option><option value="easeOutBack">easeOutBack</option><option value="easeInOutBack">easeInOutBack</option><option value="easeInElastic">easeInElastic</option><option value="easeOutElastic">easeOutElastic</option><option value="easeInOutElastic">easeInOutElastic</option><option value="easeInBounce">easeInBounce</option><option value="easeOutBounce">easeOutBounce</option><option value="easeInOutBounce">easeInOutBounce</option><option value="custom">custom</option></select></div>
<div class="field" style="margin-top:4px"><label style="width:auto;font-size:11px"><input type="checkbox" id="gp-offset"> Camera偏移模式</label></div>
<div class="field" id="gp-amp-wrap" style="display:none"><label>振幅:</label><input id="gp-amplitude" type="number" value="20" step="1" style="width:70px"></div>
<div class="field"><input id="gp-expr" type="text" placeholder="公式:sin(t*6.28)*0.8+1.2" style="display:none;width:100%;background:#0b0c10;color:#fff;border:1px solid #374151;padding:3px;font-size:11px"></div>
<div class="field"><button id="gp-expr-apply" style="display:none;font-size:11px;padding:2px 8px">采样公式</button></div>
<canvas id="gp-curve" width="200" height="100" style="width:100%;height:100px;background:#0b0c10;border:1px solid var(--border);border-radius:4px;cursor:crosshair"></canvas>
</div>
<div id="gp-censor" style="display:none">
<div class="field"><label>方向:</label><select id="gp-dir" style="width:100px"><option value="top">上方</option><option value="bottom">下方</option><option value="left">左右</option><option value="all">四周</option></select></div>
<div class="field"><label>淡入(拍):</label><input id="gp-fadein" type="number" step="0.01" style="width:70px"></div>
<div class="field"><label>淡出(拍):</label><input id="gp-fadeout" type="number" step="0.01" style="width:70px"></div>
</div>
<div class="field actions"><button id="gp-select-mode" style="font-size:12px">✋ 选择端点</button><button id="gp-delete" class="danger">删除</button></div>
</div>
</aside>
</div>
<div id="audio-bar">
<div id="audio-progress-bg"><div id="audio-progress-fill"></div><div id="audio-progress-thumb"></div></div>
<span id="audio-time">0:00 / 0:00</span>
</div>
<footer id="status-bar">
<span id="status-audio">未加载音频</span>
<span id="status-bpm-list">BPM: 120.0 (恒定)</span>
<span id="status-note-count">Notes: 0</span>
<span id="status-help">框选|多选拖|Del删|Shift/Alt循环|🔁</span>
</footer>
</div>
<script>class Chart {constructor() {this.title ='Untitled';this.artist ='';this.charter ='';this.difficulty ='';this.offset =0;this.baseBPM =120;this.bpmList =[{beat:0,bpm:120 }];this.baseSpeed =1.0;this.audioData =null;this.notes =[];this.edgeEvents =[];this.globalEvents =[];this.nextId =1;}
generateId() {const used =new Set();for (const n of this.notes) if (n &&n.id !=null) used.add(String(n.id));for (const e of this.edgeEvents) if (e &&e.id !=null) used.add(String(e.id));for (const g of this.globalEvents ||[]) if (g &&g.id !=null) used.add(String(g.id));let id;do {id =(this.nextId++).toString(36);} while (used.has(id));return id;}
getNoteById(id) {return this.notes.find(n =>n.id ===id) ||null;}
getEdgeById(id) {return this.edgeEvents.find(e =>e.id ===id) ||null;}
getGlobalById(id) {return (this.globalEvents ||[]).find(g =>g.id ===id) ||null;}
ensureUniqueIds() {const used =new Set();const alloc =(obj) =>{if (!obj) return false;let id =obj.id !=null ?String(obj.id) :'';let changed =false;if (!id ||used.has(id)) {let nid;do {nid =(this.nextId++).toString(36);} while (used.has(nid));obj.id =nid;changed =true;id =nid;}
used.add(id);return changed;};let n =0;for (const x of this.notes) if (alloc(x)) n++;for (const x of this.edgeEvents) if (alloc(x)) n++;for (const x of this.globalEvents ||[]) if (alloc(x)) n++;return n;}
getNotesInRange(minBeat,maxBeat) {return this.notes.filter(n =>{const e =n.type ==='hold' ?n.endBeat :n.beat;return n.beat <=maxBeat &&e >=minBeat;});}
getTotalBeats() {let maxBeat =0;for (const n of this.notes) {const e =n.type ==='hold' &&n.endBeat !=null ?n.endBeat :n.beat;if (e >maxBeat) maxBeat =e;}
for (const ev of this.edgeEvents) {let e;if (typeof ev.endBeat ==='number') e =ev.endBeat;else e =(ev.beat ||0) + (ev.interval ||0) + (ev.lfSpread ||0);if (e >maxBeat) maxBeat =e;}
for (const g of this.globalEvents) {const e =(g.beat ||0) + (g.duration ||0);if (e >maxBeat) maxBeat =e;}
if (this.audioData &&this.audioData.duration) {const audioBeats =this.timeToBeat(this.audioData.duration);if (audioBeats >maxBeat) maxBeat =audioBeats;}
return Math.max(16,maxBeat + 4);}
beatToTime(beat) {let time =0;let prevBeat =0;let prevBPM =this.bpmList[0].bpm;for (const pt of this.bpmList) {if (pt.beat >=beat) break;const segBeats =pt.beat - prevBeat;time +=(segBeats / prevBPM) *60;prevBeat =pt.beat;prevBPM =pt.bpm;}
const segBeats =beat - prevBeat;time +=(segBeats / prevBPM) *60;return time - this.offset;}
timeToBeat(seconds) {seconds =(seconds ||0) + this.offset;let timeAccum =0;let prevBeat =0;let prevBPM =this.bpmList[0].bpm;for (let i =1;i < this.bpmList.length;i++) {const pt =this.bpmList[i];const segBeats =pt.beat - prevBeat;const segTime =(segBeats / prevBPM) *60;if (timeAccum + segTime >=seconds) {return prevBeat + ((seconds - timeAccum) / segTime) *segBeats;}
timeAccum +=segTime;prevBeat =pt.beat;prevBPM =pt.bpm;}
const remaining =seconds - timeAccum;return prevBeat + (remaining / 60) *prevBPM;}
bpmAt(beat) {let bpm =this.bpmList[0].bpm;for (const pt of this.bpmList) {if (pt.beat >beat) break;bpm =pt.bpm;}
return bpm;}
beatToMeasure(beat,subdivDenominator =4) {const measure =Math.floor(beat / 4);const rem =beat - measure *4;const beatInMeasure =Math.floor(rem);const frac =rem - beatInMeasure;const subdiv =Math.round(frac *subdivDenominator);return {measure,beat:beatInMeasure,subdiv };}
addBPMPoint(beat,bpm) {this.bpmList =this.bpmList.filter(p =>p.beat !==beat ||p.beat ===0);this.bpmList.push({beat,bpm });this.bpmList.sort((a,b) =>a.beat - b.beat);if (beat ===0) this.baseBPM =bpm;}
removeBPMPoint(beat) {if (beat ===0) return;this.bpmList =this.bpmList.filter(p =>p.beat !==beat);}
toJSON() {this.ensureUniqueIds();return {version:1,meta:{title:this.title,artist:this.artist,charter:this.charter,difficulty:this.difficulty,offset:this.offset,baseBPM:this.baseBPM,},timing:this.bpmList,baseSpeed:this.baseSpeed,notes:this.notes.map(n =>({id:n.id,lane:n.lane,type:n.type,beat:round4(n.beat),endBeat:n.type ==='hold' ?round4(n.endBeat) :undefined,speedMultiplier:n.speedMultiplier,easing:n.easing ||'easeLinear',timeDivisor:n.timeDivisor ||4,endTimeDivisor:n.endTimeDivisor ||n.timeDivisor ||4,edgeLit:n.edgeLit ||false,})),edgeEvents:this.edgeEvents.map(ev =>({id:ev.id,edgeId:ev.edgeId,beat:round4(ev.beat),loSpread:round4(ev.loSpread),lfSpread:round4(ev.lfSpread),interval:round4(ev.interval),loGradient:ev.loGradient,lfGradient:ev.lfGradient,loGlowIntensity:ev.loGlowIntensity,loGlowRadius:ev.loGlowRadius,lfGlowIntensity:ev.lfGlowIntensity,lfGlowRadius:ev.lfGlowRadius,})),globalEvents:(this.globalEvents ||[]).map(g =>({id:g.id,type:g.type,beat:round4(g.beat),duration:round4(g.duration),startValue:g.startValue,endValue:g.endValue,easing:g.easing,direction:g.direction,fadeIn:g.fadeIn,fadeOut:g.fadeOut,curvePoints:g.curvePoints ||null,offsetMode:g.offsetMode ||false,amplitude:g.amplitude !=null ?g.amplitude :20,expression:g.expression ||null,})),};}
static fromJSON(obj) {const c =new Chart();if (obj.meta) {c.title =obj.meta.title ||'Untitled';c.artist =obj.meta.artist ||'';c.charter =obj.meta.charter ||'';c.difficulty =obj.meta.difficulty ||'';c.offset =obj.meta.offset ||0;c.baseBPM =obj.meta.baseBPM ||120;}
c.bpmList =(obj.timing &&obj.timing.length) ?obj.timing :[{beat:0,bpm:c.baseBPM }];c.baseSpeed =obj.baseSpeed ||1.0;let maxIdNum =0;if (obj.notes) {c.notes =obj.notes.map(n =>{const note =new Note(n.id ||c.generateId(),n.lane,n.type,n.beat,n.endBeat,n.speedMultiplier ||1.0,n.timeDivisor ||4);note.endTimeDivisor =n.endTimeDivisor ||n.timeDivisor ||4;note.easing =n.easing ||'easeLinear';note.curvePoints =n.curvePoints ||null;note.expression =n.expression ||null;note.edgeLit =n.edgeLit ||false;if (n.id) {const num =parseInt(n.id,36);if (!isNaN(num) &&num >maxIdNum) maxIdNum =num;}
return note;});}
if (obj.edgeEvents) {c.edgeEvents =obj.edgeEvents.map(ev =>{const e =new EdgeEvent(ev.id ||c.generateId(),ev.edgeId,ev.beat,ev.loSpread ??1,ev.lfSpread ??1,ev.interval ??2,ev.loGradient ??0,ev.lfGradient ??0,ev.loGlowIntensity ??1,ev.loGlowRadius ??0,ev.lfGlowIntensity ??1,ev.lfGlowRadius ??0
);if (ev.id) {const num =parseInt(ev.id,36);if (!isNaN(num) &&num >maxIdNum) maxIdNum =num;}
return e;});}
c.globalEvents =(obj.globalEvents ||[]).map(g =>new GlobalEvent(g.id ||c.generateId(),g.type,g.beat,g.duration,g
));for (const g of c.globalEvents) {if (g.id) {const num =parseInt(g.id,36);if (!isNaN(num) &&num >maxIdNum) maxIdNum =num;}
}
c.nextId =Math.max(1,maxIdNum + 1);const fixed =c.ensureUniqueIds();if (fixed >0) {c._idFixCount =fixed;}
return c;}
}
class Note {constructor(id,lane,type,beat,endBeat =null,speedMultiplier =1.0,timeDivisor =4) {this.id =id;this.lane =lane;this.type =type;this.beat =beat;this.endBeat =endBeat;this.speedMultiplier =speedMultiplier;this.easing ='easeLinear';this.timeDivisor =timeDivisor ||4;this.endTimeDivisor =timeDivisor ||4;this.edgeLit =false;this.curvePoints =null;this.expression =null;}
get duration() {return this.type ==='hold' ?(this.endBeat - this.beat) :0;}
}
class EdgeEvent {constructor(id,edgeId,beat,loSpread =1,lfSpread =1,interval =2,loGradient =0,lfGradient =0,loGlowIntensity =1,loGlowRadius =0,lfGlowIntensity =1,lfGlowRadius =0
) {this.id =id;this.edgeId =edgeId;this.beat =beat;this.loSpread =loSpread;this.lfSpread =lfSpread;this.interval =interval;this.loGradient =loGradient;this.lfGradient =lfGradient;this.loGlowIntensity =loGlowIntensity;this.loGlowRadius =loGlowRadius;this.lfGlowIntensity =lfGlowIntensity;this.lfGlowRadius =lfGlowRadius;}
get lfStartBeat() {return this.beat + this.interval;}
get endBeat() {return this.lfStartBeat + this.lfSpread;}
get duration() {return this.interval + this.lfSpread;}
}
function round4(n) {return Math.round(n *10000) / 10000;}
class GlobalEvent {constructor(id,type,beat,duration =1,data ={}) {this.id =id;this.type =type;this.beat =beat;this.duration =duration;this.startValue =data.startValue !=null ?data.startValue :(type ==='camera' ?100 :1);this.endValue =data.endValue !=null ?data.endValue :(type ==='camera' ?100 :1);this.easing =data.easing ||'linear';this.direction =data.direction ||'all';this.fadeIn =data.fadeIn !=null ?data.fadeIn :0.5;this.fadeOut =data.fadeOut !=null ?data.fadeOut :0.5;this.curvePoints =data.curvePoints ||null;this.offsetMode =data.offsetMode ||false;this.amplitude =data.amplitude !=null ?data.amplitude :20;this.expression =data.expression ||null;}
get endBeat() {return this.beat + this.duration;}
}
const ChartRuntime =(() =>{function bounceOut(t) {const n1 =7.5625,d1 =2.75;if (t < 1 / d1) return n1 *t *t;if (t < 2 / d1) return n1 *(t -=1.5 / d1) *t + 0.75;if (t < 2.5 / d1) return n1 *(t -=2.25 / d1) *t + 0.9375;return n1 *(t -=2.625 / d1) *t + 0.984375;}
function clamp01(t) {return Math.max(0,Math.min(1,t));}
function sampleEasing(name,t,curvePoints) {t =clamp01(t);if (name ==='custom' &&curvePoints &&curvePoints.length >=2) {const pts =curvePoints;let a =pts[0],b =pts[pts.length - 1];for (let i =0;i < pts.length - 1;i++) {if (t >=pts[i].t &&t <=pts[i + 1].t) {a =pts[i];b =pts[i + 1];break;}
}
const u =(b.t - a.t) < 1e-9 ?0 :(t - a.t) / (b.t - a.t);return a.v + (b.v - a.v) *u;}
 switch (name) {case 'easeLinear':case 'linear':return t;case 'easeInSine':return 1 - Math.cos((t *Math.PI) / 2);case 'easeOutSine':return Math.sin((t *Math.PI) / 2);case 'easeInOutSine':return -(Math.cos(Math.PI *t) - 1) / 2;case 'easeInQuad':case 'easeIn':return t *t;case 'easeOutQuad':case 'easeOut':return 1 - (1 - t) *(1 - t);case 'easeInOutQuad':case 'easeInOut':return t < 0.5 ?2 *t *t :1 - Math.pow(-2 *t + 2,2) / 2;case 'easeInCubic':return t *t *t;case 'easeOutCubic':return 1 - Math.pow(1 - t,3);case 'easeInOutCubic':return t < 0.5 ?4 *t *t *t :1 - Math.pow(-2 *t + 2,3) / 2;case 'easeInQuart':return t *t *t *t;case 'easeOutQuart':return 1 - Math.pow(1 - t,4);case 'easeInOutQuart':return t < 0.5 ?8 *t *t *t *t :1 - Math.pow(-2 *t + 2,4) / 2;case 'easeInQuint':return t *t *t *t *t;case 'easeOutQuint':return 1 - Math.pow(1 - t,5);case 'easeInExpo':return t ===0 ?0 :Math.pow(2,10 *t - 10);case 'easeOutExpo':return t ===1 ?1 :1 - Math.pow(2,-10 *t);case 'easeInCirc':return 1 - Math.sqrt(1 - Math.pow(t,2));case 'easeOutCirc':return Math.sqrt(1 - Math.pow(t - 1,2));case 'easeInOutCirc':return t < 0.5 ?(1 - Math.sqrt(1 - Math.pow(2 *t,2))) / 2 :(Math.sqrt(1 - Math.pow(-2 *t + 2,2)) + 1) / 2;case 'easeInBack':{const c1 =1.70158,c3 =c1 + 1;return c3 *t *t *t - c1 *t *t;}
case 'easeOutBack':{const c1 =1.70158,c3 =c1 + 1;return 1 + c3 *Math.pow(t - 1,3) + c1 *Math.pow(t - 1,2);}
case 'easeInOutBack':{const c1 =1.70158,c2 =c1 *1.525;return t < 0.5 ?(Math.pow(2 *t,2) *((c2 + 1) *2 *t - c2)) / 2 :(Math.pow(2 *t - 2,2) *((c2 + 1) *(t *2 - 2) + c2) + 2) / 2;}
case 'easeInElastic':{if (t ===0 ||t ===1) return t;return -(Math.pow(2,10 *t - 10) *Math.sin((t *10 - 10.75) *((2 *Math.PI) / 3)));}
case 'easeOutElastic':{if (t ===0 ||t ===1) return t;return Math.pow(2,-10 *t) *Math.sin((t *10 - 0.75) *((2 *Math.PI) / 3)) + 1;}
case 'easeInOutElastic':{if (t ===0 ||t ===1) return t;if (t < 0.5) return -(Math.pow(2,20 *t - 10) *Math.sin((20 *t - 11.125) *((2 *Math.PI) / 4.5))) / 2;return (Math.pow(2,-20 *t + 10) *Math.sin((20 *t - 11.125) *((2 *Math.PI) / 4.5))) / 2 + 1;}
case 'easeInBounce':return 1 - bounceOut(1 - t);case 'easeOutBounce':return bounceOut(t);case 'easeInOutBounce':return t < 0.5 ?(1 - bounceOut(1 - 2 *t)) / 2 :(1 + bounceOut(2 *t - 1)) / 2;default:return t;}
}
function lerp(a,b,t) {return a + (b - a) *t;}
function sampleValueEvents(events,type,nowBeat,defaultValue) {let value =defaultValue;const list =(events ||[]).filter(g =>g.type ===type).sort((a,b) =>a.beat - b.beat);for (const g of list) {const dur =Math.max(1e-6,g.duration ||0);if (nowBeat < g.beat) continue;if (nowBeat >g.beat + dur) {value =g.endValue !=null ?g.endValue :value;continue;}
const t =(nowBeat - g.beat) / dur;const e =sampleEasing(g.easing ||'linear',t,g.curvePoints);const s =g.startValue !=null ?g.startValue :defaultValue;const en =g.endValue !=null ?g.endValue :defaultValue;value =lerp(s,en,e);}
return value;}
function sampleCensors(events,nowBeat) {const out =[];for (const g of (events ||[])) {if (g.type !=='censor') continue;const dur =g.duration ||0;if (nowBeat < g.beat ||nowBeat >g.beat + dur) continue;const local =nowBeat - g.beat;const fi =g.fadeIn ||0;const fo =g.fadeOut ||0;let alpha =1;if (local < fi) alpha =fi <=0 ?1 :local / fi;else if (local >dur - fo) alpha =fo <=0 ?0 :Math.max(0,(dur - local) / fo);out.push({direction:g.direction ||'all',alpha:Math.max(0,Math.min(1,alpha)),id:g.id,});}
return out;}
function isLaneLit(chart,lane,nowBeat) {const edges =chart.edgeEvents ||[];const relevant =[lane,lane + 1];for (const edgeId of relevant) {if (edgeId < 0 ||edgeId >6) continue;for (const ev of edges) {if (ev.edgeId !==edgeId) continue;const loEnd =ev.beat + (ev.loSpread ||0);const lfStart =ev.beat + (ev.interval ||0);if (nowBeat >=loEnd &&nowBeat <=lfStart) return true;}
}
return false;}
function sampleEdgeFronts(edgeEvents,edgeId,nowBeat) {const segments =[];for (const ev of (edgeEvents ||[])) {if (ev.edgeId !==edgeId) continue;let loP =0;let lfP =0;if (nowBeat >=ev.beat) {loP =Math.min(1,Math.max(0,(nowBeat - ev.beat) / Math.max(1e-6,ev.loSpread ||0.25)));}
const lfStart =ev.beat + (ev.interval ||0);if (nowBeat >=lfStart) {lfP =Math.min(1,Math.max(0,(nowBeat - lfStart) / Math.max(1e-6,ev.lfSpread ||0.5)));}
if (loP >lfP + 1e-6) {segments.push({lf:lfP,lo:loP,loGlow:ev.loGlowIntensity !=null ?ev.loGlowIntensity :1,lfGlow:ev.lfGlowIntensity !=null ?ev.lfGlowIntensity :1,loGrad:ev.loGradient ||0,lfGrad:ev.lfGradient ||0,eventId:ev.id,});}
}
 segments.sort((a,b) =>a.lf - b.lf ||a.lo - b.lo);const merged =[];for (const seg of segments) {const last =merged[merged.length - 1];if (!last ||seg.lf >last.lo + 1e-6) {merged.push({...seg });} else {if (seg.lo >last.lo) {last.lo =seg.lo;last.loGlow =seg.loGlow;last.lfGlow =seg.lfGlow;last.loGrad =seg.loGrad;last.lfGrad =seg.lfGrad;last.eventId =seg.eventId;}
last.lf =Math.min(last.lf,seg.lf);}
}
const primary =merged.length ?merged[merged.length - 1] :null;return {loFront:primary ?primary.lo :0,lfFront:primary ?primary.lf :0,loGlow:primary ?primary.loGlow :1,lfGlow:primary ?primary.lfGlow :1,loGrad:primary ?primary.loGrad :0,lfGrad:primary ?primary.lfGrad :0,lit:merged.length >0,segments:merged,};}
function sampleAt(chart,nowBeat) {const globals =chart.globalEvents ||[];return {beat:nowBeat,sv:sampleValueEvents(globals,'sv',nowBeat,1),camera:sampleValueEvents(globals,'camera',nowBeat,100),censors:sampleCensors(globals,nowBeat),edges:Array.from({length:7 },(_,id) =>sampleEdgeFronts(chart.edgeEvents ||[],id,nowBeat)),};}
 function sampleDerivative(name,t,curvePoints,start,end) {const dt =0.002;const t0 =Math.max(0,t - dt);const t1 =Math.min(1,t + dt);const v0 =sampleEasing(name,t0,curvePoints);const v1 =sampleEasing(name,t1,curvePoints);return ((v1 - v0) / Math.max(1e-9,(t1 - t0))) *((end ||1) - (start ||0));}
function sampleIntegral(name,steps,curvePoints,start,end) {let area =0;const dt =1 / steps;for (let i =0;i < steps;i++) {const t =i *dt;const v =sampleEasing(name,t,curvePoints);area +=v *dt;}
return area *((end ||1) - (start ||0)) + (start ||0);}
function evaluateExpression(expr,t) {try {return Function('t','return ' + expr)(t);} catch (e) {return 0;}
}
return {sampleEasing,sampleAt,sampleValueEvents,sampleCensors,sampleEdgeFronts,isLaneLit,lerp,clamp01,sampleDerivative,sampleIntegral,evaluateExpression,};})();if (typeof window !=='undefined') window.ChartRuntime =ChartRuntime;class AudioEngine {constructor() {this.ctx =null;this.buffer =null;this.source =null;this.gainNode =null;this.isPlaying =false;this.startTime =0;this.offset =0;this.duration =0;this.playbackRate =1.0;}
async init() {if (!this.ctx) {this.ctx =new (window.AudioContext ||window.webkitAudioContext)();this.gainNode =this.ctx.createGain();this.gainNode.connect(this.ctx.destination);}
if (this.ctx.state ==='suspended') {await this.ctx.resume();}
}
async loadArrayBuffer(arrayBuffer) {await this.init();this.buffer =await this.ctx.decodeAudioData(arrayBuffer.slice(0));this.duration =this.buffer.duration;this.offset =0;this.isPlaying =false;return this.duration;}
play() {if (!this.buffer ||this.isPlaying) return;this.init().then(() =>{this.source =this.ctx.createBufferSource();this.source.buffer =this.buffer;this.source.playbackRate.value =this.playbackRate;this.source.connect(this.gainNode);this.startTime =this.ctx.currentTime;this.source.start(0,this.offset);this.isPlaying =true;this.source.onended =() =>{if (this.isPlaying &&this.getCurrentTime() >=this.duration - 0.01) {this.isPlaying =false;this.offset =0;}
};});}
pause() {if (!this.isPlaying) return;this.offset =this.getCurrentTime();this._stopSource();this.isPlaying =false;}
stop() {this._stopSource();this.isPlaying =false;this.offset =0;}
seek(seconds) {const wasPlaying =this.isPlaying;this.offset =Math.max(0,Math.min(seconds,this.duration));if (wasPlaying) {this._stopSource();this.isPlaying =false;this.play();}
}
setRate(rate) {this.playbackRate =rate;if (this.source) {this.source.playbackRate.value =rate;}
}
getCurrentTime() {if (!this.isPlaying) return this.offset;const elapsed =(this.ctx.currentTime - this.startTime) *this.playbackRate;return Math.min(this.offset + elapsed,this.duration);}
_stopSource() {try {if (this.source) {this.source.stop();} } catch (e) {}
this.source =null;}
}
class CommandStack {constructor(limit =200) {this.stack =[];this.index =-1;this.limit =limit;}
execute(cmd) {if (this.index < this.stack.length - 1) {this.stack =this.stack.slice(0,this.index + 1);}
cmd.execute();this.stack.push(cmd);if (this.stack.length >this.limit) {this.stack.shift();} else {this.index++;}
}
undo() {if (this.index < 0) return false;this.stack[this.index].undo();this.index--;return true;}
redo() {if (this.index >=this.stack.length - 1) return false;this.index++;this.stack[this.index].execute();return true;}
canUndo() {return this.index >=0;}
canRedo() {return this.index < this.stack.length - 1;}
clear() {this.stack =[];this.index =-1;}
}
class AddNoteCommand {constructor(chart,noteData) {this.chart =chart;this.note =null;this.noteData =noteData;}
execute() {this.note =new Note(this.chart.generateId(),this.noteData.lane,this.noteData.type,this.noteData.beat,this.noteData.endBeat,this.noteData.speedMultiplier ||1.0,this.noteData.timeDivisor ||4
);this.note.endTimeDivisor =this.noteData.endTimeDivisor ||this.noteData.timeDivisor ||4;this.chart.notes.push(this.note);}
undo() {this.chart.notes =this.chart.notes.filter(n =>n.id !==this.note.id);}
}
class DeleteNoteCommand {constructor(chart,noteId) {this.chart =chart;this.noteId =noteId;this.removed =null;this.index =-1;}
execute() {this.index =this.chart.notes.findIndex(n =>n.id ===this.noteId);if (this.index >=0) {this.removed =this.chart.notes[this.index];this.chart.notes.splice(this.index,1);}
}
undo() {if (this.removed &&this.index >=0) {this.chart.notes.splice(this.index,0,this.removed);}
}
}
class MoveNoteCommand {constructor(chart,noteId,newBeat,newEndBeat =null,extraPatch =null,oldState =null) {this.chart =chart;this.noteId =noteId;this.newBeat =newBeat;this.newEndBeat =newEndBeat;this.extraPatch =extraPatch;this.oldState =oldState;this._captured =false;this.oldBeat =null;this.oldEndBeat =null;this.oldExtra =null;}
execute() {const n =this.chart.getNoteById(this.noteId);if (!n) return;if (!this._captured) {if (this.oldState) {this.oldBeat =this.oldState.beat;this.oldEndBeat =this.oldState.endBeat;this.oldExtra =this.oldState.extras ||null;} else {this.oldBeat =n.beat;this.oldEndBeat =n.endBeat;if (this.extraPatch) {this.oldExtra ={};Object.keys(this.extraPatch).forEach(k =>{this.oldExtra[k] =n[k];});}
}
this._captured =true;}
n.beat =this.newBeat;if (n.type ==='hold' &&this.newEndBeat !==null) n.endBeat =this.newEndBeat;if (this.extraPatch) Object.assign(n,this.extraPatch);}
undo() {const n =this.chart.getNoteById(this.noteId);if (!n) return;n.beat =this.oldBeat;n.endBeat =this.oldEndBeat;if (this.oldExtra) Object.assign(n,this.oldExtra);}
}
class ChangeLaneCommand {constructor(chart,noteId,newLane,oldLane =null) {this.chart =chart;this.noteId =noteId;this.newLane =newLane;this.oldLane =oldLane;this._captured =oldLane !=null;}
execute() {const n =this.chart.getNoteById(this.noteId);if (!n) return;if (!this._captured) {this.oldLane =n.lane;this._captured =true;}
n.lane =this.newLane;}
undo() {const n =this.chart.getNoteById(this.noteId);if (!n) return;n.lane =this.oldLane;}
}
class AddBPMPointCommand {constructor(chart,beat,bpm) {this.chart =chart;this.beat =beat;this.bpm =bpm;this.previous =null;}
execute() {const existing =this.chart.bpmList.find(p =>p.beat ===this.beat);if (existing) this.previous ={beat:existing.beat,bpm:existing.bpm };this.chart.addBPMPoint(this.beat,this.bpm);}
undo() {if (this.beat ===0) {if (this.previous) this.chart.bpmList[0].bpm =this.previous.bpm;} else {if (this.previous) {this.chart.addBPMPoint(this.beat,this.previous.bpm);} else {this.chart.removeBPMPoint(this.beat);}
}
if (this.beat ===0 &&this.previous) this.chart.baseBPM =this.previous.bpm;}
}
class BatchCommand {constructor(commands =[]) {this.commands =commands;}
add(cmd) {this.commands.push(cmd);}
execute() {this.commands.forEach(c =>c.execute());}
undo() {for (let i =this.commands.length - 1;i >=0;i--) this.commands[i].undo();}
}
function _clonePlain(obj) {return obj ==null ?obj :JSON.parse(JSON.stringify(obj));}
class AddEdgeEventCommand {constructor(chart,edgeData) {this.chart =chart;this.edgeData =edgeData;this.edge =null;}
execute() {const d =this.edgeData;this.edge =new EdgeEvent(d.id ||this.chart.generateId(),d.edgeId,d.beat,d.loSpread ??0.25,d.lfSpread ??0.5,d.interval ??0.5,d.loGradient ??0,d.lfGradient ??0,d.loGlowIntensity ??1,d.loGlowRadius ??0,d.lfGlowIntensity ??1,d.lfGlowRadius ??0
);this.edgeData.id =this.edge.id;this.chart.edgeEvents.push(this.edge);}
undo() {this.chart.edgeEvents =this.chart.edgeEvents.filter(e =>e.id !==this.edge.id);}
}
class DeleteEdgeEventCommand {constructor(chart,edgeId,edgeRef =null) {this.chart =chart;this.edgeId =edgeId;this.edgeRef =edgeRef;this.removed =null;this.index =-1;}
execute() {if (this.edgeRef &&this.chart.edgeEvents.includes(this.edgeRef)) {this.index =this.chart.edgeEvents.indexOf(this.edgeRef);} else {this.index =this.chart.edgeEvents.findIndex(e =>e.id ===this.edgeId);}
if (this.index >=0) {this.removed =this.chart.edgeEvents[this.index];this.edgeId =this.removed.id;this.chart.edgeEvents.splice(this.index,1);}
}
undo() {if (this.removed &&this.index >=0) this.chart.edgeEvents.splice(this.index,0,this.removed);}
}
class UpdateEdgeEventCommand {constructor(chart,edgeId,patch,edgeRef =null) {this.chart =chart;this.edgeId =edgeId;this.edgeRef =edgeRef;this.patch =patch;this.before =null;this._ev =null;}
_resolve() {if (this.edgeRef &&this.chart.edgeEvents.includes(this.edgeRef)) return this.edgeRef;return this.chart.edgeEvents.find(e =>e.id ===this.edgeId) ||null;}
execute() {const ev =this._resolve();if (!ev) return;this._ev =ev;this.edgeId =ev.id;this.edgeRef =ev;if (!this.before) {this.before ={};Object.keys(this.patch).forEach(k =>{this.before[k] =ev[k];});}
Object.assign(ev,this.patch);}
undo() {const ev =this._ev ||this._resolve();if (!ev ||!this.before) return;Object.assign(ev,this.before);}
}
class AddGlobalEventCommand {constructor(chart,data) {this.chart =chart;this.data =data;this.event =null;}
execute() {const d =this.data;this.event =new GlobalEvent(d.id ||this.chart.generateId(),d.type ||'sv',d.beat ||0,d.duration !=null ?d.duration :1,d
);this.data.id =this.event.id;if (!this.chart.globalEvents) this.chart.globalEvents =[];this.chart.globalEvents.push(this.event);}
undo() {this.chart.globalEvents =(this.chart.globalEvents ||[]).filter(g =>g.id !==this.event.id);}
}
class DeleteGlobalEventCommand {constructor(chart,eventId) {this.chart =chart;this.eventId =eventId;this.removed =null;this.index =-1;}
execute() {this.index =(this.chart.globalEvents ||[]).findIndex(g =>g.id ===this.eventId);if (this.index >=0) {this.removed =this.chart.globalEvents[this.index];this.chart.globalEvents.splice(this.index,1);}
}
undo() {if (this.removed &&this.index >=0) this.chart.globalEvents.splice(this.index,0,this.removed);}
}
class UpdateGlobalEventCommand {constructor(chart,eventId,patch) {this.chart =chart;this.eventId =eventId;this.patch =patch;this.before =null;}
execute() {const g =(this.chart.globalEvents ||[]).find(e =>e.id ===this.eventId);if (!g) return;if (!this.before) {this.before ={};Object.keys(this.patch).forEach(k =>{this.before[k] =_clonePlain(g[k]);});}
Object.keys(this.patch).forEach(k =>{g[k] =_clonePlain(this.patch[k]);});}
undo() {const g =(this.chart.globalEvents ||[]).find(e =>e.id ===this.eventId);if (!g ||!this.before) return;Object.keys(this.before).forEach(k =>{g[k] =_clonePlain(this.before[k]);});}
}
class UpdateNoteParamsCommand {constructor(chart,noteId,patch) {this.chart =chart;this.noteId =noteId;this.patch =patch;this.before =null;}
execute() {const n =this.chart.getNoteById(this.noteId);if (!n) return;if (!this.before) {this.before ={};Object.keys(this.patch).forEach(k =>{this.before[k] =n[k];});}
Object.assign(n,this.patch);}
undo() {const n =this.chart.getNoteById(this.noteId);if (!n ||!this.before) return;Object.assign(n,this.before);}
}
class SetBPMListCommand {constructor(chart,nextList) {this.chart =chart;this.nextList =nextList.map(p =>({beat:p.beat,bpm:p.bpm }));this.prevList =null;this.prevBase =null;}
execute() {if (!this.prevList) {this.prevList =this.chart.bpmList.map(p =>({beat:p.beat,bpm:p.bpm }));this.prevBase =this.chart.baseBPM;}
this.chart.bpmList =this.nextList.map(p =>({beat:p.beat,bpm:p.bpm }));const z =this.chart.bpmList.find(p =>p.beat ===0);if (z) this.chart.baseBPM =z.bpm;}
undo() {this.chart.bpmList =this.prevList.map(p =>({beat:p.beat,bpm:p.bpm }));this.chart.baseBPM =this.prevBase;}
}
const PLAY_COLS =6;const PARAM_COLS =6;const EDGE_COLS =7;const GLOBAL_COLS =3;const INTERLEAVE_COLS =EDGE_COLS + PARAM_COLS;const TOTAL_COLS =PLAY_COLS + INTERLEAVE_COLS + GLOBAL_COLS;const PLAYHEAD_RATIO =0.72;const GLOBAL_TYPES =['sv','camera','censor'];const GLOBAL_LABELS =['SV','CAM','CEN'];const COL_PLAY_START =0;const COL_INTERLEAVE_START =PLAY_COLS;const COL_GLOBAL_START =PLAY_COLS + INTERLEAVE_COLS;function colKind(col) {if (col >=COL_PLAY_START &&col < COL_PLAY_START + PLAY_COLS) return {kind:'play',index:col - COL_PLAY_START };if (col >=COL_INTERLEAVE_START &&col < COL_GLOBAL_START) {const i =col - COL_INTERLEAVE_START;if (i ===12) return {kind:'edge',index:6 };if (i % 2 ===0) return {kind:'edge',index:i / 2 };return {kind:'param',index:(i - 1) / 2 };}
if (col >=COL_GLOBAL_START &&col < COL_GLOBAL_START + GLOBAL_COLS) {return {kind:'global',index:col - COL_GLOBAL_START };}
return {kind:'none',index:-1 };}
function edgeCol(edgeId) {if (edgeId < 0 ||edgeId >6) return -1;if (edgeId ===6) return COL_INTERLEAVE_START + 12;return COL_INTERLEAVE_START + edgeId *2;}
function paramCol(lane) {if (lane < 0 ||lane >5) return -1;return COL_INTERLEAVE_START + lane *2 + 1;}
function globalCol(typeOrIndex) {if (typeof typeOrIndex ==='string') {const idx =GLOBAL_TYPES.indexOf(typeOrIndex);return idx >=0 ?COL_GLOBAL_START + idx :-1;}
return COL_GLOBAL_START + typeOrIndex;}
const COL_WEIGHTS =(() =>{const w =[];for (let i =0;i < PLAY_COLS;i++) w.push(1.05);for (let i =0;i < INTERLEAVE_COLS;i++) {const k =colKind(COL_INTERLEAVE_START + i);w.push(k.kind ==='edge' ?0.48 :0.42);}
for (let i =0;i < GLOBAL_COLS;i++) w.push(0.7);return w;})();const COL_PARAM_START =COL_INTERLEAVE_START;const COL_EDGE_START =COL_INTERLEAVE_START;const COLOR_LANE_PLAY =['rgba(102,252,241,0.06)','rgba(102,252,241,0.10)'];const COLOR_LANE_PARAM =['rgba(79,172,254,0.05)','rgba(79,172,254,0.08)'];const COLOR_LANE_EDGE =['rgba(255,215,0,0.04)','rgba(255,215,0,0.07)'];const COLOR_LANE_GLOBAL =['rgba(255,42,109,0.05)','rgba(255,42,109,0.08)'];if (typeof window !=='undefined') {window.colKind =colKind;window.edgeCol =edgeCol;window.paramCol =paramCol;window.globalCol =globalCol;window.PLAY_COLS =PLAY_COLS;window.PLAYHEAD_RATIO =PLAYHEAD_RATIO;window.GLOBAL_TYPES =GLOBAL_TYPES;}
class Renderer {constructor(canvas) {this.canvas =canvas;this.ctx =canvas.getContext('2d');this.dpr =window.devicePixelRatio ||1;this.pixelsPerBeat =120;this.scrollBeat =0;this.currentBeat =0;this.isPlaying =false;this.colX =[];this.colW =[];this.selectedNoteId =null;this.selectedIds =null;this.selectedEdgeId =null;this.hoverBeat =0;this.hoverLane =-1;this.snapDivisor =4;this.overlappingIds =new Set();this.overlappingEdgeIds =new Set();this.overlappingGlobalIds =new Set();this.endpointHover =null;this.boxSelect =null;this.showMinimap =false;this.placingHold =null;this.loopStart =null;this.loopEnd =null;this.edgeInfluenceId =null;}
resize() {const rect =this.canvas.parentElement.getBoundingClientRect();this.canvas.width =rect.width *this.dpr;this.canvas.height =rect.height *this.dpr;this.canvas.style.width =rect.width + 'px';this.canvas.style.height =rect.height + 'px';this.ctx.setTransform(this.dpr,0,0,this.dpr,0,0);this._calcCols(rect.width);}
_calcCols(totalWidth) {const totalWeight =COL_WEIGHTS.reduce((a,b) =>a + b,0);const unit =totalWidth / totalWeight;this.colW =COL_WEIGHTS.map(w =>w *unit);this.colX =[0];for (let i =1;i < TOTAL_COLS;i++) {this.colX.push(this.colX[i - 1] + this.colW[i - 1]);}
}
getLaneCenterX(col) {return this.colX[col] + this.colW[col] / 2;}
beatToY(beat) {const anchor =this.isPlaying ?this.currentBeat :this.scrollBeat;const h =this.canvas.height / this.dpr;const anchorY =h *PLAYHEAD_RATIO;return (anchor - beat) *this.pixelsPerBeat + anchorY;}
yToBeat(y) {const anchor =this.isPlaying ?this.currentBeat :this.scrollBeat;const h =this.canvas.height / this.dpr;const anchorY =h *PLAYHEAD_RATIO;return anchor - (y - anchorY) / this.pixelsPerBeat;}
getVisibleBeatRange() {const h =this.canvas.height / this.dpr;return {min:this.yToBeat(h),max:this.yToBeat(0) };}
colAtX(x) {for (let i =0;i < TOTAL_COLS;i++) {if (x >=this.colX[i] &&x < this.colX[i] + this.colW[i]) return i;}
return -1;}
laneToParamCol(lane) {return paramCol(lane);}
edgeToCol(edgeId) {return edgeCol(edgeId);}
globalToCol(type) {return globalCol(type);}
render(chart) {const w =this.canvas.width / this.dpr;const h =this.canvas.height / this.dpr;const ctx =this.ctx;ctx.clearRect(0,0,w,h);this._drawLanes(ctx,w,h,chart);this._drawGrid(ctx,w,h,chart);this._drawBPMMarkers(ctx,w,h,chart);this._drawEdgeEvents(ctx,chart);try {this._drawGlobalEvents(ctx,chart);} catch (e) {console.warn('global draw',e);}
this._drawParamNodes(ctx,chart);this._drawNotes(ctx,chart);this._drawPlayhead(ctx,w,h);this._drawSelection(ctx,chart);this._drawLiveFxHud(ctx,w,h,chart);this._drawBoxSelect(ctx);this._drawLoopRegion(ctx,w);this._drawPlacingHold(ctx);if (this.endpointHover ||(this.hoverLane !=null &&this.hoverLane >=0)) {this._drawHoverIndicator(ctx);}
}
_drawLanes(ctx,w,h,chart) {const totalBeats =(typeof window.__getTotalBeats ==='function')
?window.__getTotalBeats(chart)
:(typeof chart.getTotalBeats ==='function' ?window.__getTotalBeats(chart) :64);const yEnd =this.beatToY(totalBeats);for (let col =0;col < TOTAL_COLS;col++) {const k =colKind(col);let fill ='rgba(255,255,255,0.02)';if (k.kind ==='play') fill =COLOR_LANE_PLAY[k.index % 2];else if (k.kind ==='param') fill =COLOR_LANE_PARAM[k.index % 2];else if (k.kind ==='edge') {fill =COLOR_LANE_EDGE[k.index % 2];if (this.edgeAddMode &&this.edgeAddSelected &&this.edgeAddSelected.has(k.index)) {fill ='rgba(255,215,0,0.25)';}
} else if (k.kind ==='global') fill =COLOR_LANE_GLOBAL[k.index % 2];ctx.fillStyle =fill;ctx.fillRect(this.colX[col],0,this.colW[col],h);}
 
 const y0 =this.beatToY(0);const yEndLine =this.beatToY(totalBeats);ctx.fillStyle ='rgba(0,0,0,0.55)';if (y0 < h) {const top =Math.max(0,y0);if (top < h) ctx.fillRect(0,top,w,h - top);}
 if (yEndLine >0) {const bottom =Math.min(h,yEndLine);if (bottom >0) ctx.fillRect(0,0,w,bottom);}
ctx.strokeStyle ='rgba(255,255,255,0.06)';ctx.lineWidth =1;ctx.beginPath();for (let i =1;i < TOTAL_COLS;i++) {const x =Math.floor(this.colX[i]) + 0.5;ctx.moveTo(x,0);ctx.lineTo(x,h);}
ctx.stroke();ctx.strokeStyle ='rgba(255,255,255,0.22)';ctx.lineWidth =1.5;ctx.beginPath();[COL_INTERLEAVE_START,COL_GLOBAL_START].forEach(idx =>{const x =Math.floor(this.colX[idx]) + 0.5;ctx.moveTo(x,0);ctx.lineTo(x,h);});ctx.stroke();ctx.fillStyle ='rgba(255,255,255,0.25)';ctx.font ='10px sans-serif';ctx.textAlign ='center';for (let col =0;col < TOTAL_COLS;col++) {const k =colKind(col);const cx =this.colX[col] + this.colW[col] / 2;if (k.kind ==='play') ctx.fillText('L' + k.index,cx,14);else if (k.kind ==='param') ctx.fillText('P' + k.index,cx,14);else if (k.kind ==='edge') ctx.fillText('E' + k.index,cx,14);else if (k.kind ==='global') ctx.fillText(GLOBAL_LABELS[k.index],cx,14);}
}
_drawGrid(ctx,w,h,chart) {const range =this.getVisibleBeatRange();const maxBeat =(typeof window.__getTotalBeats ==='function')
?window.__getTotalBeats(chart)
:(typeof chart.getTotalBeats ==='function' ?window.__getTotalBeats(chart) :64);const renderMin =Math.max(0,range.min);const renderMax =Math.min(maxBeat,range.max);const minBeat =Math.floor(renderMin);const maxB =Math.ceil(renderMax);ctx.strokeStyle ='rgba(255,255,255,0.32)';ctx.lineWidth =1.25;ctx.beginPath();for (let b =minBeat;b <=maxB;b++) {if (b < 0) continue;const y =this.beatToY(b);if (y < -20 ||y >h + 20) continue;const yy =Math.floor(y) + 0.5;ctx.moveTo(0,yy);ctx.lineTo(w,yy);}
ctx.stroke();for (let b =minBeat;b <=maxB;b++) {if (b < 0) continue;const y =this.beatToY(b);if (y < 8 ||y >h + 2) continue;const yy =Math.floor(y) + 0.5;ctx.fillStyle ='rgba(255,255,255,0.42)';ctx.font ='10px sans-serif';ctx.textAlign ='left';ctx.fillText(String(b),4,yy - 3);}
 
 if (this.snapDivisor >1) {const step =1 / this.snapDivisor;ctx.strokeStyle ='rgba(255,255,255,0.16)';ctx.lineWidth =1;ctx.beginPath();const start =Math.floor(renderMin *this.snapDivisor) / this.snapDivisor;for (let b =start;b <=renderMax + step;b +=step) {if (b < 0) continue;if (Math.abs(b - Math.round(b)) < 1e-9) continue;const y =this.beatToY(b);if (y < -1 ||y >h + 1) continue;const yy =Math.floor(y) + 0.5;ctx.moveTo(0,yy);ctx.lineTo(w,yy);}
ctx.stroke();}
 if (renderMin <=0 &&renderMax >=0) {const y0 =this.beatToY(0);ctx.strokeStyle ='rgba(255,255,255,0.60)';ctx.lineWidth =2;ctx.beginPath();ctx.moveTo(0,y0);ctx.lineTo(w,y0);ctx.stroke();ctx.fillStyle ='rgba(255,255,255,0.60)';ctx.font ='bold 11px sans-serif';ctx.textAlign ='left';ctx.fillText('START',4,y0 - 4);}
}
_drawBPMMarkers(ctx,w,h,chart) {ctx.fillStyle ='#f1c40f';ctx.font ='bold 11px sans-serif';ctx.textAlign ='right';for (const pt of chart.bpmList) {if (pt.beat < 0) continue;const y =this.beatToY(pt.beat);if (y < -20 ||y >h + 20) continue;const yy =Math.floor(y) + 0.5;ctx.fillText('♩=' + pt.bpm,w - 6,yy - 4);ctx.strokeStyle ='rgba(241,196,15,0.5)';ctx.setLineDash([4,4]);ctx.lineWidth =1;ctx.beginPath();ctx.moveTo(0,yy);ctx.lineTo(w,yy);ctx.stroke();ctx.setLineDash([]);}
}
_drawEdgeEvents(ctx,chart) {const range =this.getVisibleBeatRange();for (const ev of chart.edgeEvents) {const lfStartBeat =ev.beat + (ev.interval ||0);const lfEndBeat =lfStartBeat + (ev.lfSpread ||0);if (lfEndBeat < range.min - 2 ||ev.beat >range.max + 2) continue;const col =edgeCol(ev.edgeId);if (col < 0) continue;const cx =this.colX[col] + this.colW[col] / 2;const yLo =this.beatToY(ev.beat);const yLfStart =this.beatToY(lfStartBeat);const yLfEnd =this.beatToY(lfEndBeat);const isSelected =(this.selectedEdgeRef &&this.selectedEdgeRef ===ev) ||(ev.id ===this.selectedEdgeId);const isOverlap =this.overlappingEdgeIds &&this.overlappingEdgeIds.has(ev.id);const showInfluence =this.edgeInfluenceId ===ev.id;const top =Math.min(yLo,yLfStart);const hh =Math.abs(yLfStart - yLo);ctx.strokeStyle =isOverlap ?'rgba(239,68,68,0.95)' :(isSelected ?'rgba(255,215,0,1.0)' :'rgba(255,215,0,0.85)');ctx.lineWidth =isSelected ?7 :5;ctx.shadowColor =isOverlap ?'rgba(239,68,68,0.55)' :'rgba(255,215,0,0.5)';ctx.shadowBlur =isSelected ?10 :6;ctx.beginPath();ctx.moveTo(cx,top);ctx.lineTo(cx,top + Math.max(hh,1));ctx.stroke();ctx.shadowBlur =0;if (showInfluence) {ctx.setLineDash([5,4]);ctx.strokeStyle =isSelected ?'rgba(255,180,0,0.95)' :'rgba(255,215,0,0.55)';ctx.lineWidth =3;ctx.beginPath();ctx.moveTo(cx,yLfStart);ctx.lineTo(cx,yLfEnd);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle ='rgba(255,215,0,0.7)';ctx.lineWidth =1.5;ctx.beginPath();ctx.arc(cx,yLfEnd,3,0,Math.PI *2);ctx.stroke();}
 const r =4;[yLo,yLfStart].forEach(y =>{ctx.strokeStyle =isSelected ?'#ff2a6d' :'rgba(255,215,0,0.9)';ctx.lineWidth =2;ctx.beginPath();ctx.arc(cx,y,r,0,Math.PI *2);ctx.stroke();ctx.fillStyle ='rgba(0,0,0,0.6)';ctx.fill();});}
}
_drawGlobalEvents(ctx,chart) {const range =this.getVisibleBeatRange();for (const g of (chart.globalEvents ||[])) {const col =globalCol(g.type);if (col < 0 ||col >=this.colX.length) continue;const gEnd =(typeof g.endBeat ==='number') ?g.endBeat :((g.beat ||0) + (g.duration ||0));if (gEnd < range.min - 2 ||g.beat >range.max + 2) continue;const cx =this.colX[col] + this.colW[col] / 2;const yStart =this.beatToY(g.beat);const yEnd =this.beatToY(gEnd);const top =Math.min(yStart,yEnd);const hh =Math.abs(yEnd - yStart);const isSelected =g.id ===this.selectedGlobalId;let color ='rgba(102,252,241,0.9)';if (g.type ==='camera') color ='rgba(167,139,250,0.95)';if (g.type ==='censor') color ='rgba(148,163,184,0.95)';const isOverlapG =this.overlappingGlobalIds &&this.overlappingGlobalIds.has(g.id);if (isOverlapG) color ='rgba(239,68,68,0.95)';if (isSelected) {ctx.strokeStyle ='#ff2a6d';ctx.lineWidth =6;ctx.shadowColor ='#ff2a6d';ctx.shadowBlur =10;} else {ctx.strokeStyle =color;ctx.lineWidth =5;ctx.shadowColor =color;ctx.shadowBlur =6;}
ctx.beginPath();ctx.moveTo(cx,top);ctx.lineTo(cx,top + hh);ctx.stroke();ctx.shadowBlur =0;const r =4;[yStart,yEnd].forEach(y =>{ctx.strokeStyle =isSelected ?'#ff2a6d' :color;ctx.lineWidth =2;ctx.beginPath();ctx.arc(cx,y,r,0,Math.PI *2);ctx.stroke();ctx.fillStyle ='rgba(0,0,0,0.6)';ctx.fill();});}
}
_drawParamNodes(ctx,chart) {const range =this.getVisibleBeatRange();const notes =chart.getNotesInRange(range.min - 2,range.max + 2);for (const n of notes) {if (n.lane < 0 ||n.lane >=PLAY_COLS) continue;if (n.speedMultiplier ===1.0) continue;const pcol =this.laneToParamCol(n.lane);if (pcol < 0) continue;const cx =this.colX[pcol] + this.colW[pcol] / 2;const cy =this.beatToY(n.beat);ctx.fillStyle ='#4facfe';ctx.shadowColor ='rgba(79,172,254,0.6)';ctx.shadowBlur =6;ctx.beginPath();ctx.arc(cx,cy,4,0,Math.PI *2);ctx.fill();ctx.shadowBlur =0;if (n.id ===this.selectedNoteId) {ctx.strokeStyle ='#fff';ctx.lineWidth =1.5;ctx.beginPath();ctx.arc(cx,cy,6,0,Math.PI *2);ctx.stroke();}
}
}
_drawNotes(ctx,chart) {const range =this.getVisibleBeatRange();const notes =chart.getNotesInRange(range.min - 2,range.max + 2);const JUDGE_LINE_W =2;const NOTE_THICK =JUDGE_LINE_W *3;for (const n of notes) {if (n.lane < 0 ||n.lane >=PLAY_COLS) continue;const x =this.colX[n.lane];const lw =this.colW[n.lane];const isOverlap =this.overlappingIds.has(n.id);const noteLen =Math.max(4,lw - 4);const nx =x + (lw - noteLen) / 2;const thick =NOTE_THICK;if (n.type ==='tap') {const y =this.beatToY(n.beat);const yTop =y - thick / 2;ctx.fillStyle =isOverlap ?'rgba(239,68,68,0.90)' :'rgba(102,252,241,0.9)';ctx.shadowColor =isOverlap ?'rgba(239,68,68,0.7)' :'rgba(102,252,241,0.45)';ctx.shadowBlur =4;roundRect(ctx,nx,yTop,noteLen,thick,2);ctx.fill();ctx.shadowBlur =0;} else if (n.type ==='hold') {const yStart =this.beatToY(n.beat);const yEnd =this.beatToY(n.endBeat);const top =Math.min(yStart,yEnd);const hh =Math.abs(yEnd - yStart);if (hh < 1) continue;const bodyLen =Math.max(4,noteLen *0.72);const bx =x + (lw - bodyLen) / 2;ctx.fillStyle =isOverlap ?'rgba(239,68,68,0.45)' :'rgba(241,196,15,0.4)';ctx.fillRect(bx,top,bodyLen,hh);ctx.fillStyle =isOverlap ?'rgba(239,68,68,0.95)' :'rgba(241,196,15,0.95)';roundRect(ctx,nx,yStart - thick / 2,noteLen,thick,2);ctx.fill();ctx.fillStyle =isOverlap ?'rgba(239,68,68,0.7)' :'rgba(241,196,15,0.7)';ctx.fillRect(bx,yEnd - 1,bodyLen,2);}
}
}
_drawPlayhead(ctx,w,h) {const y =Math.floor(h *PLAYHEAD_RATIO) + 0.5;ctx.save();ctx.strokeStyle ='rgba(0,0,0,0.55)';ctx.lineWidth =4;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();ctx.strokeStyle ='rgba(255,42,109,1)';ctx.lineWidth =2;ctx.shadowColor ='rgba(255,42,109,0.85)';ctx.shadowBlur =12;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();ctx.shadowBlur =0;ctx.fillStyle ='rgba(255,42,109,1)';ctx.font ='bold 12px sans-serif';ctx.textAlign ='left';ctx.fillText('▶ NOW',4,y - 6);ctx.restore();}
_drawSelection(ctx,chart) {const thick =6;const outline =(n,color,lwStroke,glow) =>{if (!n ||n.lane < 0 ||n.lane >=PLAY_COLS) return;const x =this.colX[n.lane];const laneW =this.colW[n.lane];const noteLen =Math.max(4,laneW - 4);const nx =x + (laneW - noteLen) / 2;ctx.strokeStyle =color;ctx.lineWidth =lwStroke;if (glow) {ctx.shadowColor =glow;ctx.shadowBlur =10;}
if (n.type ==='tap') {const y =this.beatToY(n.beat);ctx.strokeRect(nx - 1,y - thick / 2 - 1,noteLen + 2,thick + 2);} else {const yStart =this.beatToY(n.beat);const yEnd =this.beatToY(n.endBeat);const top =Math.min(yStart,yEnd);const hh =Math.abs(yEnd - yStart);ctx.strokeRect(nx - 1,top - 1,noteLen + 2,hh + 2);}
ctx.shadowBlur =0;};if (this.selectedIds &&this.selectedIds.size) {this.selectedIds.forEach(id =>outline(chart.getNoteById(id),'rgba(255,255,255,0.75)',1.5,null));}
if (this.selectedNoteId) {outline(chart.getNoteById(this.selectedNoteId),'#ff2a6d',2,'#ff2a6d');}
 }
_drawLoopRegion(ctx,w) {if (this.loopStart ==null &&this.loopEnd ==null) return;const s =this.loopStart !=null ?this.beatToY(this.loopStart) :0;const e =this.loopEnd !=null ?this.beatToY(this.loopEnd) :(this.canvas.height / this.dpr);const t =Math.min(s,e),b =Math.max(s,e);ctx.save();ctx.fillStyle ='rgba(255,255,0,0.06)';ctx.fillRect(0,t,w,Math.max(2,b - t));ctx.strokeStyle ='rgba(255,255,0,0.6)';ctx.lineWidth =1.5;ctx.setLineDash([6,3]);ctx.beginPath();ctx.moveTo(0,t);ctx.lineTo(w,t);ctx.stroke();ctx.beginPath();ctx.moveTo(0,b);ctx.lineTo(w,b);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle ='rgba(255,255,0,0.9)';ctx.font ='10px sans-serif';ctx.textAlign ='left';if (this.loopStart !=null) ctx.fillText('LP▶ ' + Number(this.loopStart).toFixed(2),4,t - 3);if (this.loopEnd !=null) ctx.fillText('LP■ ' + Number(this.loopEnd).toFixed(2),4,b + 12);ctx.restore();}
_drawBoxSelect(ctx) {const b =this.boxSelect;if (!b) return;const x =Math.min(b.x0,b.x1);const y =Math.min(b.y0,b.y1);const bw =Math.abs(b.x1 - b.x0);const bh =Math.abs(b.y1 - b.y0);if (bw < 2 &&bh < 2) return;ctx.save();ctx.fillStyle ='rgba(102,252,241,0.12)';ctx.strokeStyle ='rgba(102,252,241,0.85)';ctx.lineWidth =1;ctx.setLineDash([4,3]);ctx.fillRect(x,y,bw,bh);ctx.strokeRect(x + 0.5,y + 0.5,bw,bh);ctx.setLineDash([]);ctx.restore();}
_drawPlacingHold(ctx) {const p =this.placingHold;if (!p ||p.lane ==null) return;const x =this.colX[p.lane];const lw =this.colW[p.lane];const noteLen =Math.max(4,lw - 4);const thick =6;const nx =x + (lw - noteLen) / 2;const yS =this.beatToY(p.beat);const yE =this.beatToY(p.endBeat);const top =Math.min(yS,yE);const hh =Math.abs(yE - yS);ctx.save();ctx.globalAlpha =0.55;ctx.fillStyle ='rgba(241,196,15,0.35)';ctx.fillRect(nx + noteLen *0.14,top,noteLen *0.72,Math.max(hh,1));ctx.fillStyle ='rgba(241,196,15,0.9)';roundRect(ctx,nx,yS - thick / 2,noteLen,thick,2);ctx.fill();ctx.restore();}
_drawHoverIndicator(ctx) {if (this.endpointHover &&this.endpointHover.col >=0) {const col =this.endpointHover.col;if (col < this.colX.length) {const y =this.beatToY(this.endpointHover.beat);const cx =this.colX[col] + this.colW[col] / 2;ctx.save();ctx.setLineDash([4,3]);ctx.strokeStyle ='rgba(255,42,109,0.5)';ctx.beginPath();ctx.moveTo(this.colX[col] + 1,y);ctx.lineTo(this.colX[col] + this.colW[col] - 1,y);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle ='rgba(255,42,109,1)';ctx.lineWidth =2.5;ctx.beginPath();ctx.arc(cx,y,6,0,Math.PI *2);ctx.stroke();ctx.fillStyle ='rgba(255,42,109,0.2)';ctx.fill();ctx.restore();}
return;}
 
 if (this.hoverLane >=0 &&this.hoverLane < PLAY_COLS &&this.hoverBeat >=0 &&this.colX &&this.colX.length) {const x =this.colX[this.hoverLane];const y =this.beatToY(this.hoverBeat);const lw =this.colW[this.hoverLane];const noteLen =Math.max(4,lw - 4);const thick =6;const nx =x + (lw - noteLen) / 2;ctx.save();ctx.globalAlpha =0.9;ctx.fillStyle ='rgba(160,170,180,0.40)';ctx.strokeStyle ='rgba(230,235,240,0.70)';ctx.lineWidth =1.5;roundRect(ctx,nx,y - thick / 2,noteLen,thick,2);ctx.fill();ctx.stroke();ctx.strokeStyle ='rgba(255,255,255,0.35)';ctx.lineWidth =1;ctx.beginPath();ctx.moveTo(x + lw / 2,y - thick);ctx.lineTo(x + lw / 2,y + thick);ctx.stroke();ctx.restore();}
}
_drawLiveFxHud(ctx,w,h,chart) {if (!window.ChartRuntime ||!ChartRuntime.sampleAt) return;const nowBeat =this.isPlaying ?this.currentBeat :this.scrollBeat;let rt;try {rt =ChartRuntime.sampleAt(chart,nowBeat);} catch (e) {return;}
const y =Math.floor(h *PLAYHEAD_RATIO) + 0.5;const boxW =168,boxH =54;const bx =w - boxW - 8;let by =y + 12;if (by + boxH >h - 8) by =Math.max(8,y - boxH - 12);ctx.save();ctx.fillStyle ='rgba(15,18,24,0.82)';ctx.strokeStyle ='rgba(102,252,241,0.35)';ctx.lineWidth =1;roundRect(ctx,bx,by,boxW,boxH,6);ctx.fill();ctx.stroke();ctx.fillStyle ='#66fcf1';ctx.font ='11px sans-serif';ctx.textAlign ='left';ctx.fillText('SV ' + Number(rt.sv).toFixed(3) + 'x',bx + 8,by + 16);ctx.fillStyle ='#c4b5fd';ctx.fillText('CAM ' + Number(rt.camera).toFixed(1),bx + 8,by + 32);const lit =(rt.edges ||[]).filter(e =>e &&e.lit).length;const cen =(rt.censors ||[]).length;ctx.fillStyle ='#fbbf24';ctx.fillText('Edge lit ' + lit + '/7  Censor ' + cen,bx + 8,by + 48);if (rt.edges) {for (let i =0;i < EDGE_COLS;i++) {const fr =rt.edges[i];if (!fr ||!fr.lit) continue;const col =edgeCol(i);const cx =this.colX[col] + this.colW[col] / 2;const glow =Math.max(0.3,fr.loGlow ||1);ctx.fillStyle ='rgba(255,215,0,' + (0.35 + 0.4 *Math.min(1,glow)) + ')';ctx.beginPath();ctx.arc(cx,y,3.5,0,Math.PI *2);ctx.fill();}
}
ctx.restore();}
}
function roundRect(ctx,x,y,w,h,r) {if (w < 2 *r) r =w / 2;if (h < 2 *r) r =h / 2;ctx.beginPath();ctx.moveTo(x + r,y);ctx.lineTo(x + w - r,y);ctx.quadraticCurveTo(x + w,y,x + w,y + r);ctx.lineTo(x + w,y + h - r);ctx.quadraticCurveTo(x + w,y + h,x + w - r,y + h);ctx.lineTo(x + r,y + h);ctx.quadraticCurveTo(x,y + h,x,y + h - r);ctx.lineTo(x,y + r);ctx.quadraticCurveTo(x,y,x + r,y);ctx.closePath();}
const Tool ={SELECT:0,TAP:1,HOLD:2 };function _playCols(){return (typeof window!=='undefined' &&window.PLAY_COLS) ||(typeof PLAY_COLS!=='undefined' ?PLAY_COLS :6);}
const OVERLAP_THRESHOLD =1 / 16;class InputHandler {resolveSelectedEdge() {if (this.engine.selectedEdgeRef &&this.chart.edgeEvents.includes(this.engine.selectedEdgeRef)) {return this.engine.selectedEdgeRef;}
const id =this.engine.selectedEdgeId;if (id ==null) return null;return this.chart.edgeEvents.find(e =>e.id ===id) ||null;}
constructor(canvas,engine,chart,audio,commands) {this.canvas =canvas;this.engine =engine;this.chart =chart;this.audio =audio;this.cmd =commands;this.tool =Tool.SELECT;this.state ='IDLE';this.dragStart ={x:0,y:0,beat:0,lane:0 };this.dragCurrent ={x:0,y:0 };this.activeNoteId =null;this.snapDivisor =4;this.selectedIds =new Set();this.clipboard =[];this.boxSelect =null;this.multiDrag =null;this.playheadDrag =false;this.endpointEdit =null;this.edgeAddMode =false;this.edgeAddSelected =new Set();this._bindEvents();}
setTool(t) {this.tool =t;}
setSnap(d) {this.snapDivisor =d;this.engine.snapDivisor =d;}
_bindEvents() {const c =this.canvas;c.addEventListener('mousedown',e =>this.onMouseDown(e));c.addEventListener('dblclick',e =>{const pos =this._getPos(e);const ht =this._hitTest(pos.x,pos.y);if (ht.edgeEvent) {if (this.chart.ensureUniqueIds) this.chart.ensureUniqueIds();this.engine.selectedEdgeId =ht.edgeEvent.id;this.engine.selectedEdgeRef =ht.edgeEvent;if (this.engine.edgeInfluenceId ===ht.edgeEvent.id) this.engine.edgeInfluenceId =null;else this.engine.edgeInfluenceId =ht.edgeEvent.id;this.refreshUI();}
});c.addEventListener('mousemove',e =>this.onMouseMove(e));window.addEventListener('mouseup',e =>this.onMouseUp(e));c.addEventListener('contextmenu',e =>{e.preventDefault();});c.addEventListener('wheel',e =>this.onWheel(e),{passive:false });c.addEventListener('touchstart',e =>{if (e.touches.length ===1) {const t =e.touches[0];this.onMouseDown({button:0,clientX:t.clientX,clientY:t.clientY,preventDefault:() =>e.preventDefault() });}
},{passive:false });c.addEventListener('touchmove',e =>{if (e.touches.length ===1) {const t =e.touches[0];this.onMouseMove({clientX:t.clientX,clientY:t.clientY,preventDefault:() =>e.preventDefault() });}
},{passive:false });window.addEventListener('touchend',e =>this.onMouseUp({}));}
_getPos(e) {const rect =this.canvas.getBoundingClientRect();const cx =e.clientX !=null ?e.clientX :0;const cy =e.clientY !=null ?e.clientY :0;return {x:cx - rect.left,y:cy - rect.top };}
_hitTest(x,y) {const col =this.engine.colAtX(x);if (col < 0) return {col:-1,lane:-1,note:null,edgeEvent:null,globalEvent:null };const meta =(typeof window !=='undefined' &&window.colKind) ?window.colKind(col) :((typeof colKind ==='function') ?colKind(col) :{kind:'none',index:-1 });let lane =meta.kind ==='play' ?meta.index :(meta.kind ==='param' ?meta.index :-1);const beat =this.engine.yToBeat(y);const range =this.engine.getVisibleBeatRange();if (meta.kind ==='global') {const type =GLOBAL_TYPES[meta.index];const events =(this.chart.globalEvents ||[]).slice().reverse();for (const g of events) {if (g.type !==type) continue;const yS =this.engine.beatToY(g.beat);const yE =this.engine.beatToY(g.endBeat);const top =Math.min(yS,yE) - 6;const bot =Math.max(yS,yE) + 6;const cx =this.engine.colX[col] + this.engine.colW[col] / 2;if (y >=top &&y <=bot &&Math.abs(x - cx) <=this.engine.colW[col] / 2 + 4) {return {col,lane:-1,note:null,edgeEvent:null,globalEvent:g };}
}
}
 if (meta.kind ==='edge') {const edgeId =meta.index;for (const ev of this.chart.edgeEvents) {if (ev.edgeId !==edgeId) continue;const yS =this.engine.beatToY(ev.beat);const yLfStart =this.engine.beatToY(ev.beat + (ev.interval ||0));const showInf =this.engine.edgeInfluenceId ===ev.id;const yLfEnd =this.engine.beatToY(ev.endBeat);const yA =yS,yB =showInf ?yLfEnd :yLfStart;const top =Math.min(yA,yB);const bot =Math.max(yA,yB);if (y >=top - 6 &&y <=bot + 6 &&Math.abs(x - (this.engine.colX[col] + this.engine.colW[col] / 2)) < this.engine.colW[col] / 2 + 4) {return {col,lane:-1,note:null,edgeEvent:ev,globalEvent:null };}
}
}
 if (lane >=0) {const notes =this.chart.getNotesInRange(range.min - 2,range.max + 2);let best =null;let bestDist =Infinity;for (const n of notes) {if (n.lane !==lane) continue;if (!this._hitNote(n,x,y)) continue;const yRef =this.engine.beatToY(n.beat);const dist =Math.abs(y - yRef);let d =dist;if (n.type ==='hold') {const yS =this.engine.beatToY(n.beat);const yE =this.engine.beatToY(n.endBeat);const top =Math.min(yS,yE),bot =Math.max(yS,yE);if (y >=top &&y <=bot) d =0;else d =Math.min(Math.abs(y - top),Math.abs(y - bot));}
if (d < bestDist) {bestDist =d;best =n;}
}
if (best) return {col,lane,note:best,edgeEvent:null,globalEvent:null };}
return {col,lane,note:null,edgeEvent:null,globalEvent:null };}
_hitNote(n,x,y) {const playCol =n.lane;const paramCol =COL_PARAM_START + n.lane;const inPlay =x >=this.engine.colX[playCol] &&x < this.engine.colX[playCol] + this.engine.colW[playCol];const inParam =x >=this.engine.colX[paramCol] &&x < this.engine.colX[paramCol] + this.engine.colW[paramCol];if (!inPlay &&!inParam) return false;if (n.type ==='tap') {const ny =this.engine.beatToY(n.beat);const h =6;return Math.abs(y - ny) <=h / 2 + 4;} else {const yS =this.engine.beatToY(n.beat);const yE =this.engine.beatToY(n.endBeat);const top =Math.min(yS,yE);const bot =Math.max(yS,yE);return y >=top - 6 &&y <=bot + 6;}
}
_snap(beat) {const d =this.snapDivisor;return Math.round(beat *d) / d;}
_clampScroll() {const total =(typeof window.__getTotalBeats ==='function')
?window.__getTotalBeats(this.chart)
:(typeof this.chart.getTotalBeats ==='function' ?this.window.__getTotalBeats(chart) :64);if (this.engine.scrollBeat < 0) this.engine.scrollBeat =0;if (this.engine.scrollBeat >total + 4) this.engine.scrollBeat =total + 4;}
_noteCenterY(n) {return this.engine.beatToY(n.beat);}
_notesInBox(x0,y0,x1,y1) {const L =Math.min(x0,x1),R =Math.max(x0,x1);const T =Math.min(y0,y1),B =Math.max(y0,y1);const out =[];for (const n of this.chart.notes) {if (n.lane < 0 ||n.lane >=PLAY_COLS) continue;const x =this.engine.colX[n.lane];const w =this.engine.colW[n.lane];const nx0 =x,nx1 =x + w;let ny0,ny1;if (n.type ==='hold') {const yS =this.engine.beatToY(n.beat);const yE =this.engine.beatToY(n.endBeat);ny0 =Math.min(yS,yE);ny1 =Math.max(yS,yE);} else {const y =this.engine.beatToY(n.beat);ny0 =y - 4;ny1 =y + 4;}
const hit =!(nx1 < L ||nx0 >R ||ny1 < T ||ny0 >B);if (hit) out.push(n);}
return out;}
_isNearPlayhead(y) {const h =this.engine.canvas.height / (window.devicePixelRatio ||1);const py =h *(typeof PLAYHEAD_RATIO !=='undefined' ?PLAYHEAD_RATIO :0.72);return Math.abs(y - py) <=8;}
_hitMinimap(pos) {const r =this.engine._minimapRect;if (!r ||!this.engine.showMinimap) return null;if (pos.x < r.x ||pos.x >r.x + r.w ||pos.y < r.y ||pos.y >r.y + r.h) return null;const t =(pos.y - r.y - 4) / Math.max(1,r.h - 8);return Math.max(0,Math.min(r.total,t *r.total));}
startEndpointEdit(kind,ref) {this.endpointEdit ={kind,which:null,ref,pendingBeat:null,live:false,before:null };this.engine.endpointHover =null;this.state ='ENDPOINT_EDIT';this.tool =Tool.SELECT;this.setTool(Tool.SELECT);}
cancelEndpointEdit() {this.endpointEdit =null;this.engine.endpointHover =null;if (this.state ==='ENDPOINT_EDIT') this.state ='IDLE';}
_edgeSolidRange(ev) {const a =ev.beat;const b =ev.beat + (ev.interval ||0);return {start:Math.min(a,b),end:Math.max(a,b) };}
_edgesOverlapStrict(a,b) {if (a.edgeId !==b.edgeId) return false;if (a.id ===b.id) return false;const A =this._edgeSolidRange(a);const B =this._edgeSolidRange(b);return A.start < B.end &&B.start < A.end;}
_globalsOverlapStrict(a,b) {if (a.type !==b.type) return false;if (a.id ===b.id) return false;const A0 =a.beat,A1 =a.beat + (a.duration ||0);const B0 =b.beat,B1 =b.beat + (b.duration ||0);const as =Math.min(A0,A1),ae =Math.max(A0,A1);const bs =Math.min(B0,B1),be =Math.max(B0,B1);return as < be &&bs < ae;}
checkEdgeOverlaps(focusEv) {const overlaps =[];for (const other of this.chart.edgeEvents) {if (!focusEv) break;if (this._edgesOverlapStrict(focusEv,other)) overlaps.push(other);}
if (!this.engine.overlappingEdgeIds) this.engine.overlappingEdgeIds =new Set();if (focusEv) {if (overlaps.length) this.engine.overlappingEdgeIds.add(focusEv.id);else this.engine.overlappingEdgeIds.delete(focusEv.id);}
if (window.reportParamOverlap) window.reportParamOverlap('edge',focusEv,overlaps);return overlaps;}
checkGlobalOverlaps(focusG) {const overlaps =[];for (const other of (this.chart.globalEvents ||[])) {if (!focusG) break;if (this._globalsOverlapStrict(focusG,other)) overlaps.push(other);}
if (!this.engine.overlappingGlobalIds) this.engine.overlappingGlobalIds =new Set();if (focusG) {if (overlaps.length) this.engine.overlappingGlobalIds.add(focusG.id);else this.engine.overlappingGlobalIds.delete(focusG.id);}
if (window.reportParamOverlap) window.reportParamOverlap('global',focusG,overlaps);return overlaps;}
copyParamSelection() {if (this.engine.selectedEdgeRef ||this.engine.selectedEdgeId) {const ev =this.resolveSelectedEdge();if (!ev) return 0;this.paramClipboard ={kind:'edge',data:{edgeId:ev.edgeId,beat:ev.beat,loSpread:ev.loSpread,lfSpread:ev.lfSpread,interval:ev.interval,loGradient:ev.loGradient,lfGradient:ev.lfGradient,loGlowIntensity:ev.loGlowIntensity,loGlowRadius:ev.loGlowRadius,lfGlowIntensity:ev.lfGlowIntensity,lfGlowRadius:ev.lfGlowRadius
}
};return 1;}
if (this.engine.selectedGlobalId) {const g =(this.chart.globalEvents ||[]).find(x =>x.id ===this.engine.selectedGlobalId);if (!g) return 0;this.paramClipboard ={kind:'global',data:{type:g.type,beat:g.beat,duration:g.duration,startValue:g.startValue,endValue:g.endValue,easing:g.easing,direction:g.direction,fadeIn:g.fadeIn,fadeOut:g.fadeOut,curvePoints:g.curvePoints ?JSON.parse(JSON.stringify(g.curvePoints)) :null
}
};return 1;}
return 0;}
pasteParamClipboard() {if (!this.paramClipboard) return 0;const now =Math.max(0,this.engine.isPlaying ?this.engine.currentBeat :this.engine.scrollBeat);if (this.paramClipboard.kind ==='edge') {const d ={...this.paramClipboard.data,beat:now };const cmd =new AddEdgeEventCommand(this.chart,d);this.cmd.execute(cmd);this.engine.selectedEdgeId =cmd.edge.id;this.engine.selectedEdgeRef =cmd.edge;this.checkEdgeOverlaps(cmd.edge);this.refreshUI();return 1;}
if (this.paramClipboard.kind ==='global') {const d ={...this.paramClipboard.data,beat:now };const cmd =new AddGlobalEventCommand(this.chart,d);this.cmd.execute(cmd);this.engine.selectedGlobalId =cmd.event.id;this.checkGlobalOverlaps(cmd.event);this.refreshUI();return 1;}
return 0;}
onMouseDown(e) {if (e.button !==0 &&e.button !==2) return;const pos =this._getPos(e);const ht =this._hitTest(pos.x,pos.y);if (this.state ==='ENDPOINT_EDIT' &&this.endpointEdit &&e.button ===0) {const ed =this.endpointEdit;const beat =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));const meta =(typeof colKind ==='function') ?colKind(ht.col) :null;if (ed.kind ==='edge') {const ev =ed.ref;if (!ev ||!meta ||meta.kind !=='edge' ||meta.index !==ev.edgeId) {this.cancelEndpointEdit();return;}
if (ed.pendingBeat ==null) {ed.pendingBeat =beat;this.engine.endpointHover ={col:edgeCol(ev.edgeId),beat };this.refreshUI();return;}
const a =ed.pendingBeat,b =beat;const startB =Math.min(a,b),endB =Math.max(a,b);const before ={beat:ev.beat,interval:ev.interval,lfSpread:ev.lfSpread,loSpread:ev.loSpread };const patch ={beat:startB,interval:Math.max(0.125,endB - startB) };Object.assign(ev,before);this.cmd.execute(new UpdateEdgeEventCommand(this.chart,ev.id,patch,ev));this.checkEdgeOverlaps(ev);this.cancelEndpointEdit();document.getElementById('endpoint-edit-tip')?.remove();document.getElementById('ep-select-mode')?.classList.remove('active');this.refreshUI();return;}
if (ed.kind ==='global') {const g =ed.ref;if (!g ||!meta ||meta.kind !=='global' ||GLOBAL_TYPES[meta.index] !==g.type) {this.cancelEndpointEdit();return;}
if (ed.pendingBeat ==null) {ed.pendingBeat =beat;this.engine.endpointHover ={col:globalCol(g.type),beat };this.refreshUI();return;}
const a =ed.pendingBeat,b =beat;const startB =Math.min(a,b),endB =Math.max(a,b);const before ={beat:g.beat,duration:g.duration };const patch ={beat:startB,duration:Math.max(0.125,endB - startB) };Object.assign(g,before);this.cmd.execute(new UpdateGlobalEventCommand(this.chart,g.id,patch));this.checkGlobalOverlaps(g);this.cancelEndpointEdit();document.getElementById('endpoint-edit-tip')?.remove();document.getElementById('gp-select-mode')?.classList.remove('active');this.refreshUI();return;}
}
if (e.button ===2) {this.state ='SCRUBBING';this.dragStart ={x:pos.x,y:pos.y,scrollBeat:this.engine.scrollBeat };e.preventDefault();return;}
 if (this.edgeAddMode) {const em =(typeof window !=='undefined' &&window.colKind) ?window.colKind(ht.col) :((typeof colKind ==='function') ?colKind(ht.col) :null);if (em &&em.kind ==='edge') {const edgeId =em.index;if (this.edgeAddSelected.has(edgeId)) {this.edgeAddSelected.delete(edgeId);} else {this.edgeAddSelected.add(edgeId);}
if (window.updateEdgeAddUI) window.updateEdgeAddUI();}
return;}
if (this.tool ===Tool.SELECT) {if (ht.globalEvent) {this.engine.selectedGlobalId =ht.globalEvent.id;this.engine.selectedEdgeId =null;this.engine.selectedNoteId =null;this.activeNoteId =null;const g =ht.globalEvent;const yS =this.engine.beatToY(g.beat);const yE =this.engine.beatToY(g.endBeat);if (this.endpointEdit &&this.endpointEdit.kind ==='global' &&(this.endpointEdit.ref ===g ||(this.endpointEdit.ref &&this.endpointEdit.ref.id ===g.id))) {this.endpointEdit.ref =g;if (Math.abs(pos.y - yS) < 14) this.endpointEdit.which ='start';else if (Math.abs(pos.y - yE) < 14) this.endpointEdit.which ='end';else this.endpointEdit.which =(Math.abs(pos.y - yS) <=Math.abs(pos.y - yE)) ?'start' :'end';this.endpointEdit.live =true;this.endpointEdit.before ={beat:g.beat,duration:g.duration };this.state ='ENDPOINT_LIVE';this.engine.endpointHover ={col:(typeof globalCol ==='function' ?globalCol(g.type) :ht.col),beat:this.endpointEdit.which ==='start' ?g.beat :(g.beat + g.duration)
};return;}
if (Math.abs(pos.y - yS) < 10) this.state ='DRAGGING_GLOBAL_START';else if (Math.abs(pos.y - yE) < 10) this.state ='DRAGGING_GLOBAL_END';else this.state ='DRAGGING_GLOBAL_BODY';this.dragStart ={x:pos.x,y:pos.y,beat:g.beat,duration:g.duration,oldBeat:g.beat,oldDuration:g.duration
};this.refreshUI();return;}
if (ht.edgeEvent) {if (this.chart.ensureUniqueIds) this.chart.ensureUniqueIds();this.engine.selectedEdgeId =ht.edgeEvent.id;this.engine.selectedEdgeRef =ht.edgeEvent;this.engine.selectedGlobalId =null;this.engine.selectedNoteId =null;this.activeNoteId =null;const col =(typeof edgeCol ==='function') ?edgeCol(ht.edgeEvent.edgeId) :ht.col;const yS =this.engine.beatToY(ht.edgeEvent.beat);const yLfStart =this.engine.beatToY(ht.edgeEvent.beat + (ht.edgeEvent.interval ||0));if (this.endpointEdit &&this.endpointEdit.kind ==='edge' &&(this.endpointEdit.ref ===ht.edgeEvent ||(this.endpointEdit.ref &&this.endpointEdit.ref.id ===ht.edgeEvent.id))) {this.endpointEdit.ref =ht.edgeEvent;if (Math.abs(pos.y - yS) < 14) this.endpointEdit.which ='start';else if (Math.abs(pos.y - yLfStart) < 14) this.endpointEdit.which ='end';else this.endpointEdit.which =(Math.abs(pos.y - yS) <=Math.abs(pos.y - yLfStart)) ?'start' :'end';this.endpointEdit.live =true;this.endpointEdit.before ={beat:ht.edgeEvent.beat,interval:ht.edgeEvent.interval,lfSpread:ht.edgeEvent.lfSpread,loSpread:ht.edgeEvent.loSpread
};this.state ='ENDPOINT_LIVE';this.engine.endpointHover ={col:(typeof edgeCol ==='function' ?edgeCol(ht.edgeEvent.edgeId) :col),beat:this.endpointEdit.which ==='start' ?ht.edgeEvent.beat :(ht.edgeEvent.beat + ht.edgeEvent.interval)
};return;}
if (Math.abs(pos.y - yS) < 10) {this.state ='DRAGGING_EDGE_START';} else if (Math.abs(pos.y - yLfStart) < 10) {this.state ='DRAGGING_EDGE_END';} else {this.state ='DRAGGING_EDGE_BODY';}
const ev0 =ht.edgeEvent;this.dragStart ={x:pos.x,y:pos.y,beat:ev0.beat,endBeat:ev0.endBeat,edgeId:ev0.edgeId,oldBeat:ev0.beat,oldInterval:ev0.interval,oldLfSpread:ev0.lfSpread,oldLoSpread:ev0.loSpread
};this.refreshUI();return;}
if (ht.note) {if (e.shiftKey) {if (this.selectedIds.has(ht.note.id)) this.selectedIds.delete(ht.note.id);else this.selectedIds.add(ht.note.id);this.engine.selectedNoteId =ht.note.id;this.engine.selectedIds =this.selectedIds;this.refreshUI();return;} else {this.selectedIds =new Set([ht.note.id]);this.engine.selectedIds =this.selectedIds;}
this.engine.selectedNoteId =ht.note.id;this.engine.selectedEdgeId =null;this.engine.selectedEdgeRef =null;this.engine.selectedGlobalId =null;this.activeNoteId =ht.note.id;if (ht.note.type ==='hold') {const yS =this.engine.beatToY(ht.note.beat);const yE =this.engine.beatToY(ht.note.endBeat);if (Math.abs(pos.y - yS) < 10) {this.state ='DRAGGING_HEAD';} else if (Math.abs(pos.y - yE) < 10) {this.state ='DRAGGING_TAIL';} else {this.state ='DRAGGING_NOTE';}
} else {this.state ='DRAGGING_NOTE';}
this.dragStart ={x:pos.x,y:pos.y,beat:ht.note.beat,lane:ht.note.lane,oldTimeDivisor:ht.note.timeDivisor ||this.snapDivisor,oldEndTimeDivisor:ht.note.endTimeDivisor ||ht.note.timeDivisor ||this.snapDivisor
};if (ht.note.type ==='hold') {this.dragStart.endBeat =ht.note.endBeat;}
 if (this.selectedIds &&this.selectedIds.size >1 &&this.selectedIds.has(ht.note.id) &&this.state ==='DRAGGING_NOTE') {this.multiDrag ={originBeat:ht.note.beat,originLane:ht.note.lane,items:[...this.selectedIds].map(id =>{const n =this.chart.getNoteById(id);return n ?{id,beat:n.beat,endBeat:n.endBeat,lane:n.lane,timeDivisor:n.timeDivisor,endTimeDivisor:n.endTimeDivisor } :null;}).filter(Boolean)
};} else {this.multiDrag =null;}
this.refreshUI();} else {if (this._isNearPlayhead(pos.y)) {this.playheadDrag =true;this.state ='SCRUB_PLAYHEAD';const b =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));this.engine.scrollBeat =b;this.engine.currentBeat =b;if (this.audio &&this.audio.buffer) {try {this.audio.seek(Math.max(0,this.chart.beatToTime(b)));} catch (err) {}
}
return;}
this.engine.selectedNoteId =null;this.engine.selectedEdgeId =null;this.engine.selectedEdgeRef =null;this.engine.selectedGlobalId =null;this.selectedIds =new Set();this.engine.selectedIds =this.selectedIds;this.boxSelect ={x0:pos.x,y0:pos.y,x1:pos.x,y1:pos.y,additive:!!e.shiftKey };this.engine.boxSelect =this.boxSelect;this.state ='BOX_SELECT';this.refreshUI();this.dragStart ={x:pos.x,y:pos.y,scrollBeat:this.engine.scrollBeat };}
return;}
if (this.tool ===Tool.TAP) {if (ht.lane >=0 &&ht.lane < PLAY_COLS) {const beat =this._snap(this.engine.yToBeat(pos.y));if (beat < 0) return;const cmd =new AddNoteCommand(this.chart,{lane:ht.lane,type:'tap',beat,speedMultiplier:1.0,timeDivisor:this.snapDivisor,endTimeDivisor:this.snapDivisor
});this.cmd.execute(cmd);this.engine.selectedNoteId =cmd.note.id;this.selectedIds =new Set([cmd.note.id]);this.engine.selectedIds =this.selectedIds;this._checkOverlapForNote(cmd.note);this.refreshUI();}
return;}
if (this.tool ===Tool.HOLD) {if (ht.lane >=0 &&ht.lane < PLAY_COLS) {this.state ='PLACING_HOLD';const beat =this._snap(this.engine.yToBeat(pos.y));this.dragStart ={x:pos.x,y:pos.y,lane:ht.lane,beat };this.activeNoteId =null;}
return;}
}
_updateLaneHover(pos,ht) {const ck =(typeof window !=='undefined' &&window.colKind) ?window.colKind
:(typeof colKind ==='function' ?colKind :null);let lane =-1;if (ht &&ht.lane >=0 &&ht.lane < PLAY_COLS) lane =ht.lane;if (lane < 0 &&ht &&ht.col >=0) {if (ck) {const m =ck(ht.col);if (m.kind ==='play' ||m.kind ==='param') lane =m.index;} else if (ht.col < PLAY_COLS) {lane =ht.col;}
}
if (lane >=0) {this.engine.hoverLane =lane;this.engine.hoverBeat =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));this._lastHoverRawY =pos.y;if (!this._hoverLoggedOnce) {console.log('[hover] lane=' + lane + ' beat=' + this.engine.hoverBeat.toFixed(3) + ' col=' + (ht&&ht.col) + ' ht.lane=' + (ht&&ht.lane));this._hoverLoggedOnce =true;}
return true;}
return false;}
onMouseMove(e) {const pos =this._getPos(e);const ht =this._hitTest(pos.x,pos.y);if (this.state !=='ENDPOINT_EDIT' &&this.state !=='ENDPOINT_LIVE') {const ok =this._updateLaneHover(pos,ht);if (!ok &&this.state !=='PLACING_HOLD') {this.engine.hoverLane =-1;this.engine.hoverBeat =-1;}
}
if (this.edgeAddMode) {const em =(typeof window !=='undefined' &&window.colKind) ?window.colKind(ht.col) :((typeof colKind ==='function') ?colKind(ht.col) :null);this.canvas.style.cursor =(em &&em.kind ==='edge') ?'pointer' :'default';return;}
if ((this.state ==='ENDPOINT_EDIT' ||this.state ==='ENDPOINT_LIVE') &&this.endpointEdit) {const ed =this.endpointEdit;const beat =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));let col =-1;if (ed.kind ==='edge' &&ed.ref) col =edgeCol(ed.ref.edgeId);if (ed.kind ==='global' &&ed.ref) col =globalCol(ed.ref.type);this.engine.endpointHover ={col,beat };this.engine.hoverLane =-1;this.canvas.style.cursor ='crosshair';if (this.state ==='ENDPOINT_LIVE' &&ed.live &&ed.which &&ed.ref) {if (ed.kind ==='edge') {const ev =ed.ref;const before =ed.before ||{beat:ev.beat,interval:ev.interval };if (ed.which ==='start') {const lfStart =before.beat + before.interval;const ns =Math.min(beat,lfStart - 0.125);ev.beat =Math.max(0,ns);ev.interval =Math.max(0.125,lfStart - ev.beat);} else {const ns =Math.max(beat,ev.beat + 0.125);ev.interval =Math.max(0.125,ns - ev.beat);}
} else if (ed.kind ==='global') {const g =ed.ref;const before =ed.before ||{beat:g.beat,duration:g.duration };if (ed.which ==='start') {const end =before.beat + before.duration;const ns =Math.min(beat,end - 0.125);g.beat =Math.max(0,ns);g.duration =Math.max(0.125,end - g.beat);} else {const ns =before.beat;const ne =Math.max(beat,ns + 0.125);g.beat =ns;g.duration =Math.max(0.125,ne - ns);}
}
}
return;}
if (this.state ==='IDLE') {this.engine.endpointHover =null;this._updateLaneHover(pos,ht);if (this.engine.hoverLane < 0) this.engine.hoverBeat =-1;if (ht.edgeEvent ||ht.note ||ht.globalEvent) this.canvas.style.cursor ='pointer';else if (this.tool !==Tool.SELECT) this.canvas.style.cursor ='crosshair';else this.canvas.style.cursor ='default';return;}
if (this.state ==='SCRUBBING' ||this.state ==='SCRUB_PLAYHEAD') {const dy =pos.y - this.dragStart.y;const dBeat =dy / this.engine.pixelsPerBeat;this.engine.scrollBeat =this.dragStart.scrollBeat + dBeat;this._clampScroll();this.canvas.style.cursor ='grabbing';return;}
if (this.state ==='SCRUB_PLAYHEAD') {const b =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));this.engine.scrollBeat =b;this.engine.currentBeat =b;if (this.audio &&this.audio.buffer) {try {this.audio.seek(Math.max(0,this.chart.beatToTime(b)));} catch (err) {}
}
this.canvas.style.cursor ='ew-resize';return;}
if (this.state ==='BOX_SELECT' &&this.boxSelect) {this.boxSelect.x1 =pos.x;this.boxSelect.y1 =pos.y;this.engine.boxSelect =this.boxSelect;this.canvas.style.cursor ='crosshair';return;}
if (this.state ==='PLACING_HOLD') {this.dragCurrent =pos;const endBeat =this._snap(this.engine.yToBeat(pos.y));this.engine.placingHold ={lane:this.dragStart.lane,beat:this.dragStart.beat,endBeat
};return;}
if (this.state ==='DRAGGING_NOTE') {const n =this.chart.getNoteById(this.activeNoteId);if (!n) {this.state ='IDLE';return;}
const dy =pos.y - this.dragStart.y;const dBeat =-dy / this.engine.pixelsPerBeat;const rawBeat =this.dragStart.beat + dBeat;const newBeat =Math.max(0,this._snap(rawBeat));if (this.multiDrag &&this.multiDrag.items &&this.multiDrag.items.length >1) {const dB =newBeat - this.multiDrag.originBeat;let dL =0;if (ht.lane >=0 &&ht.lane < PLAY_COLS) dL =ht.lane - this.multiDrag.originLane;for (const it0 of this.multiDrag.items) {const nn =this.chart.getNoteById(it0.id);if (!nn) continue;nn.beat =Math.max(0,this._snap(it0.beat + dB));if (nn.type ==='hold' &&it0.endBeat !=null) {nn.endBeat =Math.max(nn.beat + 0.125,this._snap(it0.endBeat + dB));}
nn.lane =Math.max(0,Math.min(PLAY_COLS - 1,it0.lane + dL));}
return;}
const delta =newBeat - n.beat;if (n.type ==='hold') {n.endBeat =this.dragStart.endBeat + delta;}
n.beat =newBeat;if (ht.lane >=0 &&ht.lane < PLAY_COLS) {n.lane =ht.lane;}
return;}
if (this.state ==='DRAGGING_HEAD') {const n =this.chart.getNoteById(this.activeNoteId);if (!n ||n.type !=='hold') {this.state ='IDLE';return;}
const dy =pos.y - this.dragStart.y;const dBeat =-dy / this.engine.pixelsPerBeat;const newBeat =Math.max(0,this._snap(this.dragStart.beat + dBeat));if (newBeat < n.endBeat - 0.125) {n.beat =newBeat;}
return;}
if (this.state ==='DRAGGING_TAIL') {const n =this.chart.getNoteById(this.activeNoteId);if (!n ||n.type !=='hold') {this.state ='IDLE';return;}
const dy =pos.y - this.dragStart.y;const dBeat =-dy / this.engine.pixelsPerBeat;const newEnd =Math.max(0,this._snap(this.dragStart.endBeat + dBeat));if (newEnd >n.beat + 0.125) {n.endBeat =newEnd;}
return;}
if (this.state ==='DRAGGING_EDGE_START') {const ev =(this.resolveSelectedEdge ?this.resolveSelectedEdge() :this.chart.edgeEvents.find(e =>e.id ===this.engine.selectedEdgeId));if (!ev) {this.state ='IDLE';return;}
 const lfStart =this.dragStart.oldBeat + this.dragStart.oldInterval;const newBeat =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));const maxBeat =lfStart - 0.125;ev.beat =Math.min(newBeat,maxBeat);ev.interval =Math.max(0.125,lfStart - ev.beat);return;}
if (this.state ==='DRAGGING_EDGE_END') {const ev =(this.resolveSelectedEdge ?this.resolveSelectedEdge() :this.chart.edgeEvents.find(e =>e.id ===this.engine.selectedEdgeId));if (!ev) {this.state ='IDLE';return;}
 const newLfStart =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));if (newLfStart >ev.beat + 0.125) {ev.interval =newLfStart - ev.beat;}
return;}
if (this.state ==='DRAGGING_GLOBAL_START') {const g =(this.chart.globalEvents ||[]).find(e =>e.id ===this.engine.selectedGlobalId);if (!g) {this.state ='IDLE';return;}
const newBeat =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));const end =g.beat + g.duration;if (newBeat < end - 0.125) {const newDur =end - newBeat;g.beat =newBeat;g.duration =newDur;}
return;}
if (this.state ==='DRAGGING_GLOBAL_END') {const g =(this.chart.globalEvents ||[]).find(e =>e.id ===this.engine.selectedGlobalId);if (!g) {this.state ='IDLE';return;}
const newEnd =Math.max(0,this._snap(this.engine.yToBeat(pos.y)));if (newEnd >g.beat + 0.125) g.duration =newEnd - g.beat;return;}
if (this.state ==='DRAGGING_GLOBAL_BODY') {const g =(this.chart.globalEvents ||[]).find(e =>e.id ===this.engine.selectedGlobalId);if (!g) {this.state ='IDLE';return;}
const dy =pos.y - this.dragStart.y;const dBeat =-dy / this.engine.pixelsPerBeat;g.beat =Math.max(0,this._snap(this.dragStart.beat + dBeat));return;}
if (this.state ==='DRAGGING_EDGE_BODY') {const ev =(this.resolveSelectedEdge ?this.resolveSelectedEdge() :this.chart.edgeEvents.find(e =>e.id ===this.engine.selectedEdgeId));if (!ev) {this.state ='IDLE';return;}
const dy =pos.y - this.dragStart.y;const dBeat =-dy / this.engine.pixelsPerBeat;const newBeat =Math.max(0,this._snap(this.dragStart.beat + dBeat));const delta =newBeat - ev.beat;ev.beat =newBeat;return;}
}
onMouseUp(e) {let addedNote =null;if (this.state ==='SCRUB_PLAYHEAD') {this.playheadDrag =false;this.endpointEdit =null;this.state ='IDLE';return;}
if (this.state ==='BOX_SELECT' &&this.boxSelect) {const notes =this._notesInBox(this.boxSelect.x0,this.boxSelect.y0,this.boxSelect.x1,this.boxSelect.y1);const next =this.boxSelect.additive ?new Set(this.selectedIds) :new Set();notes.forEach(n =>next.add(n.id));this.selectedIds =next;this.engine.selectedIds =this.selectedIds;if (notes[0]) this.engine.selectedNoteId =notes[0].id;else if (!this.boxSelect.additive) this.engine.selectedNoteId =null;this.boxSelect =null;this.engine.boxSelect =null;this.state ='IDLE';this.refreshUI();return;}
this.engine.placingHold =null;if (this.state ==='PLACING_HOLD') {const pos =this._getPos(e);const lane =this.dragStart.lane;const startBeat =this.dragStart.beat;const endBeat =this._snap(this.engine.yToBeat(pos.y));if (Math.abs(endBeat - startBeat) >=0.125) {const realEnd =endBeat >startBeat ?endBeat :startBeat;const realStart =endBeat >startBeat ?startBeat :endBeat;if (realStart < 0) return;const cmd =new AddNoteCommand(this.chart,{lane,type:'hold',beat:realStart,endBeat:realEnd,speedMultiplier:1.0,timeDivisor:this.snapDivisor,endTimeDivisor:this.snapDivisor
});this.cmd.execute(cmd);addedNote =cmd.note;this.engine.selectedNoteId =cmd.note.id;this.selectedIds =new Set([cmd.note.id]);this.engine.selectedIds =this.selectedIds;this.refreshUI();}
}
if (this.state ==='DRAGGING_NOTE' ||this.state ==='DRAGGING_HEAD' ||this.state ==='DRAGGING_TAIL') {if (this.state ==='DRAGGING_NOTE' &&this.multiDrag &&this.multiDrag.items &&this.multiDrag.items.length >1) {const batch =new BatchCommand();for (const it0 of this.multiDrag.items) {const nn =this.chart.getNoteById(it0.id);if (!nn) continue;const newBeat =nn.beat,newEnd =nn.endBeat,newLane =nn.lane;nn.beat =it0.beat;nn.endBeat =it0.endBeat;nn.lane =it0.lane;nn.timeDivisor =it0.timeDivisor;nn.endTimeDivisor =it0.endTimeDivisor;if (newLane !==it0.lane) batch.add(new ChangeLaneCommand(this.chart,it0.id,newLane,it0.lane));if (newBeat !==it0.beat ||newEnd !==it0.endBeat) {const extra ={timeDivisor:this.snapDivisor };if (nn.type ==='hold' ||newEnd !=null) extra.endTimeDivisor =this.snapDivisor;batch.add(new MoveNoteCommand(this.chart,it0.id,newBeat,newEnd,extra,{beat:it0.beat,endBeat:it0.endBeat,extras:{timeDivisor:it0.timeDivisor,endTimeDivisor:it0.endTimeDivisor }
}));}
}
if (batch.commands.length) this.cmd.execute(batch);this.multiDrag =null;this.refreshUI();} else {const n =this.chart.getNoteById(this.activeNoteId);if (n) {const oldLane =this.dragStart.lane;const oldBeat =this.dragStart.beat;const oldEnd =this.dragStart.endBeat !=null ?this.dragStart.endBeat :null;const oldTimeDiv =this.dragStart.oldTimeDivisor;const oldEndTimeDiv =this.dragStart.oldEndTimeDivisor;const newLane =n.lane;const newBeat =n.beat;const newEnd =n.endBeat;n.lane =oldLane;n.beat =oldBeat;n.endBeat =oldEnd;if (oldTimeDiv !=null) n.timeDivisor =oldTimeDiv;if (oldEndTimeDiv !=null) n.endTimeDivisor =oldEndTimeDiv;if (oldLane !==newLane) {this.cmd.execute(new ChangeLaneCommand(this.chart,n.id,newLane,oldLane));}
if (oldBeat !==newBeat ||oldEnd !==newEnd) {const extra ={timeDivisor:this.snapDivisor };if (n.type ==='hold' ||newEnd !=null) extra.endTimeDivisor =this.snapDivisor;const oldState ={beat:oldBeat,endBeat:oldEnd,extras:{timeDivisor:oldTimeDiv !=null ?oldTimeDiv :n.timeDivisor,endTimeDivisor:oldEndTimeDiv !=null ?oldEndTimeDiv :n.endTimeDivisor
}
};this.cmd.execute(new MoveNoteCommand(this.chart,n.id,newBeat,newEnd,extra,oldState));}
this._checkOverlapForNote(this.chart.getNoteById(this.activeNoteId));}
}
this.multiDrag =null;}
if (this.state ==='DRAGGING_EDGE_START' ||this.state ==='DRAGGING_EDGE_END' ||this.state ==='DRAGGING_EDGE_BODY') {const ev =(this.resolveSelectedEdge ?this.resolveSelectedEdge() :this.chart.edgeEvents.find(e =>e.id ===this.engine.selectedEdgeId));if (ev) {const patch ={beat:ev.beat,interval:ev.interval,lfSpread:ev.lfSpread,loSpread:ev.loSpread
};const before ={beat:this.dragStart.oldBeat,interval:this.dragStart.oldInterval,lfSpread:this.dragStart.oldLfSpread,loSpread:this.dragStart.oldLoSpread
};const changed =Object.keys(patch).some(k =>patch[k] !==before[k]);if (changed) {Object.assign(ev,before);this.cmd.execute(new UpdateEdgeEventCommand(this.chart,ev.id,patch,ev));this.checkEdgeOverlaps(ev);}
if (window.updateEdgePanel) window.updateEdgePanel();}
}
if (this.state ==='DRAGGING_GLOBAL_START' ||this.state ==='DRAGGING_GLOBAL_END' ||this.state ==='DRAGGING_GLOBAL_BODY') {const g =(this.chart.globalEvents ||[]).find(e =>e.id ===this.engine.selectedGlobalId);if (g) {const patch ={beat:g.beat,duration:g.duration };const before ={beat:this.dragStart.oldBeat,duration:this.dragStart.oldDuration };const changed =patch.beat !==before.beat ||patch.duration !==before.duration;if (changed) {Object.assign(g,before);this.cmd.execute(new UpdateGlobalEventCommand(this.chart,g.id,patch));this.checkGlobalOverlaps(g);}
if (window.updateGlobalPanel) window.updateGlobalPanel();}
}
if (addedNote) {this._checkOverlapForNote(addedNote);}
this.state ='IDLE';this.activeNoteId =null;this.multiDrag =null;this.playheadDrag =false;this.endpointEdit =null;if (this.boxSelect) {this.boxSelect =null;this.engine.boxSelect =null;}
this.canvas.style.cursor =this.tool ===Tool.SELECT ?'default' :'crosshair';}
onWheel(e) {e.preventDefault();const pos =this._getPos(e);if (e.ctrlKey ||e.metaKey) {const zoomFactor =e.deltaY >0 ?0.9 :1.1;const newPPB =Math.max(20,Math.min(2000,this.engine.pixelsPerBeat *zoomFactor));const anchorBeat =this.engine.yToBeat(pos.y);this.engine.pixelsPerBeat =newPPB;const h =this.canvas.height / (window.devicePixelRatio ||1);this.engine.scrollBeat =anchorBeat - (h *PLAYHEAD_RATIO - pos.y) / newPPB;this._clampScroll();if (window.updateZoomSlider) window.updateZoomSlider(newPPB);} else {const scrollAmount =e.deltaY >0 ?-0.75 :0.75;this.engine.scrollBeat +=scrollAmount;this._clampScroll();this._didManualScroll =true;}
}
_checkOverlapForNote(note) {const conflicts =[];for (const other of this.chart.notes) {if (other.id ===note.id) continue;if (other.lane !==note.lane) continue;if (this._notesOverlap(note,other)) conflicts.push(other);}
if (conflicts.length ===0) {this.engine.overlappingIds.delete(note.id);if (window.refreshOverlapPanel) window.refreshOverlapPanel();return;}
this.engine.overlappingIds.add(note.id);if (window.reportOverlap) window.reportOverlap(note,conflicts);}
_notesOverlap(a,b) {const aStart =a.beat;const aEnd =a.type ==='hold' ?a.endBeat :a.beat;const bStart =b.beat;const bEnd =b.type ==='hold' ?b.endBeat :b.beat;const minDist =Math.min(Math.abs(aStart - bStart),Math.abs(aStart - bEnd),Math.abs(aEnd - bStart),Math.abs(aEnd - bEnd)
);const intersect =!(aEnd < bStart - OVERLAP_THRESHOLD ||bEnd < aStart - OVERLAP_THRESHOLD);return intersect ||minDist <=OVERLAP_THRESHOLD;}
refreshUI() {if (window.updateSidePanel) window.updateSidePanel();if (window.updateEdgePanel) window.updateEdgePanel();if (window.updateGlobalPanel) window.updateGlobalPanel();if (window.updateStatus) window.updateStatus();}
copySelection() {const ids =this.selectedIds &&this.selectedIds.size ?this.selectedIds :(this.engine.selectedNoteId ?new Set([this.engine.selectedNoteId]) :new Set());this.clipboard =[];ids.forEach(id =>{const n =this.chart.getNoteById(id);if (n) this.clipboard.push({lane:n.lane,type:n.type,beat:n.beat,endBeat:n.endBeat,speedMultiplier:n.speedMultiplier,timeDivisor:n.timeDivisor ||this.snapDivisor,endTimeDivisor:n.endTimeDivisor ||n.timeDivisor ||this.snapDivisor
});});return this.clipboard.length;}
pasteClipboard() {if (!this.clipboard.length) return 0;const base =Math.min(...this.clipboard.map(n =>n.beat));const target =Math.max(0,this.engine.isPlaying ?this.engine.currentBeat :this.engine.scrollBeat);const delta =target - base;const batch =new BatchCommand();this.clipboard.forEach(src =>{batch.add(new AddNoteCommand(this.chart,{lane:src.lane,type:src.type,beat:src.beat + delta,endBeat:src.type ==='hold' ?(src.endBeat + delta) :null,speedMultiplier:src.speedMultiplier ||1,timeDivisor:src.timeDivisor ||this.snapDivisor,endTimeDivisor:src.endTimeDivisor ||src.timeDivisor ||this.snapDivisor
}));});this.cmd.execute(batch);const newIds =batch.commands.map(c =>c.note &&c.note.id).filter(Boolean);this.selectedIds =new Set(newIds);this.engine.selectedIds =this.selectedIds;if (newIds[0]) this.engine.selectedNoteId =newIds[0];this.refreshUI();return newIds.length;}
}
const APP_VERSION ='v0.5.2';function initUI(chart,audio,engine,input,commands) {const btnPlay =document.getElementById('btn-play');const btnStop =document.getElementById('btn-stop');const btnGotoStart =document.getElementById('btn-goto-start');const btnMetronome =document.getElementById('btn-metronome');const btnLoop =document.getElementById('btn-loop');const btnShiftUp =document.getElementById('btn-shift-up');const btnShiftDown =document.getElementById('btn-shift-down');const btnGlobalList =document.getElementById('btn-global-list');const btnMinimap =document.getElementById('btn-minimap');const btnValidate =document.getElementById('btn-validate');const btnInfo =document.getElementById('btn-info');const infoTitlePreview =document.getElementById('info-title-preview');const inputBPM =document.getElementById('input-bpm');const btnAddBPM =document.getElementById('btn-add-bpm');const selectSnap =document.getElementById('select-snap');const inputCustomSnap =document.getElementById('input-custom-snap');const inputZoom =document.getElementById('input-zoom');const selectRate =document.getElementById('select-rate');const btnAudio =document.getElementById('btn-audio');const fileAudio =document.getElementById('file-audio');const btnImport =document.getElementById('btn-import');const fileImport =document.getElementById('file-import');const btnExport =document.getElementById('btn-export');const btnUndo =document.getElementById('btn-undo');const btnRedo =document.getElementById('btn-redo');const btnEdgeAdd =document.getElementById('btn-edge-add');const btnEdgeList =document.getElementById('btn-edge-list');const btnGlobalAdd =document.getElementById('btn-global-add');const btnPreview =document.getElementById('btn-preview');const btnQuantize =document.getElementById('btn-quantize');const btnMirror =document.getElementById('btn-mirror');const btnBpmManage =document.getElementById('btn-bpm-manage');const inputOffset =document.getElementById('input-offset');if (document.getElementById('app-version')) document.getElementById('app-version').textContent =APP_VERSION;if (inputOffset) {inputOffset.value =Math.round((chart.offset ||0) *1000);inputOffset.onchange =() =>{chart.offset =(parseFloat(inputOffset.value) ||0) / 1000;};}
const audioBar =document.getElementById('audio-progress-bg');const audioFill =document.getElementById('audio-progress-fill');const audioThumb =document.getElementById('audio-progress-thumb');const audioTime =document.getElementById('audio-time');let audioDragging =false;if (!document.getElementById('app-version')) {const ver =document.createElement('div');ver.className ='tool-group';ver.innerHTML =`<span id="app-version" style="color:var(--accent);font-size:12px;font-weight:bold;">${APP_VERSION}</span>`;document.getElementById('toolbar').appendChild(ver);} else {document.getElementById('app-version').textContent =APP_VERSION;}
 const toolGroup =document.createElement('div');toolGroup.className ='tool-group';toolGroup.innerHTML =`
    <button id="tool-select" class="active">选择</button>
    <button id="tool-tap">Tap</button>
    <button id="tool-hold">Hold</button>
  `;document.getElementById('toolbar').insertBefore(toolGroup,document.getElementById('toolbar').children[1]);const btnToolSelect =document.getElementById('tool-select');const btnToolTap =document.getElementById('tool-tap');const btnToolHold =document.getElementById('tool-hold');function setToolUI(t) {input.setTool(t);[btnToolSelect,btnToolTap,btnToolHold].forEach(b =>b.classList.remove('active'));if (t ===Tool.SELECT) btnToolSelect.classList.add('active');if (t ===Tool.TAP) btnToolTap.classList.add('active');if (t ===Tool.HOLD) btnToolHold.classList.add('active');}
btnToolSelect.onclick =() =>setToolUI(Tool.SELECT);btnToolTap.onclick =() =>setToolUI(Tool.TAP);btnToolHold.onclick =() =>setToolUI(Tool.HOLD);function sliderToPPB(val) {const h =engine.canvas.height / (window.devicePixelRatio ||1);const ppbMax =h / 1;const totalBeats =(chart.audioData &&chart.audioData.duration)
?chart.timeToBeat(chart.audioData.duration) :500;const ppbMin =Math.max(5,(h *0.9) / totalBeats);return ppbMin *Math.pow(ppbMax / Math.max(ppbMin,1e-6),val / 100);}
function ppbToSlider(ppb) {const h =engine.canvas.height / (window.devicePixelRatio ||1);const totalBeats =(chart.audioData &&chart.audioData.duration)
?chart.timeToBeat(chart.audioData.duration) :500;const ppbMax =h / 1;const ppbMin =Math.max(5,(h *0.9) / totalBeats);return Math.max(0,Math.min(100,Math.round(Math.log(ppb / ppbMin) / Math.log(ppbMax / Math.max(ppbMin,1e-6)) *100)));}
window.updateZoomSlider =(ppb) =>{inputZoom.value =ppbToSlider(ppb);};inputZoom.oninput =() =>{engine.pixelsPerBeat =sliderToPPB(parseInt(inputZoom.value,10));engine.scrollBeat =engine.isPlaying ?engine.currentBeat :engine.scrollBeat;};window.updateZoomSlider(engine.pixelsPerBeat);function togglePlay() {if (audio.isPlaying) {audio.pause();engine.isPlaying =false;engine.scrollBeat =engine.currentBeat;btnPlay.textContent ='▶';} else if (audio.buffer) {const t =chart.beatToTime(engine.scrollBeat);audio.seek(Math.max(0,t));audio.play();engine.isPlaying =true;btnPlay.textContent ='⏸';}
}
btnPlay.onclick =togglePlay;if (btnGotoStart) btnGotoStart.onclick =() =>{engine.scrollBeat =0;engine.currentBeat =0;if (audio.buffer) audio.seek(0);};btnStop.onclick =() =>{audio.stop();engine.isPlaying =false;engine.currentBeat =0;engine.scrollBeat =0;btnPlay.textContent ='▶';};if (btnLoop) {btnLoop.onclick =() =>{const now =engine.isPlaying ?engine.currentBeat :engine.scrollBeat;if (engine.loopStart ==null) {engine.loopStart =Math.max(0,now);btnLoop.classList.add('active');} else if (engine.loopEnd ==null) {engine.loopEnd =Math.max(engine.loopStart + 0.125,now);btnLoop.textContent ='🔁ON';} else {engine.loopStart =null;engine.loopEnd =null;btnLoop.textContent ='🔁';btnLoop.classList.remove('active');}
};}
 window.__checkLoop =() =>{if (!audio.isPlaying) return;if (engine.loopStart ==null ||engine.loopEnd ==null) return;const end =Math.max(engine.loopStart,engine.loopEnd);const t =audio.getCurrentTime();if (t >=chart.beatToTime(end) - 0.01) {audio.seek(chart.beatToTime(engine.loopStart ||0));audio.play();}
};if (btnInfo) {const updateInfoPreview =() =>{if (infoTitlePreview) infoTitlePreview.textContent ='(' + (chart.title ||'Untitled') + ')';};updateInfoPreview();btnInfo.onclick =() =>{const old =document.getElementById('info-dlg');if (old) old.remove();const dlg =document.createElement('div');dlg.id ='info-dlg';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:2100;display:flex;align-items:center;justify-content:center;';dlg.innerHTML =`
        <div style="width:360px;background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:16px;color:#fff;">
          <h3 style="margin:0 0 12px;">谱面信息</h3>
          <div style="margin-bottom:8px;"><label style="display:block;font-size:12px;color:var(--text-dim);">曲名</label><input id="info-title" value="${chart.title||''}" style="width:100%;background:#0b0c10;color:#fff;border:1px solid #374151;padding:4px;"></div>
          <div style="margin-bottom:8px;"><label style="display:block;font-size:12px;color:var(--text-dim);">曲师</label><input id="info-artist" value="${chart.artist||''}" style="width:100%;background:#0b0c10;color:#fff;border:1px solid #374151;padding:4px;"></div>
          <div style="margin-bottom:8px;"><label style="display:block;font-size:12px;color:var(--text-dim);">谱师</label><input id="info-charter" value="${chart.charter||''}" style="width:100%;background:#0b0c10;color:#fff;border:1px solid #374151;padding:4px;"></div>
          <div style="margin-bottom:8px;"><label style="display:block;font-size:12px;color:var(--text-dim);">难度</label><input id="info-diff" value="${chart.difficulty||''}" style="width:100%;background:#0b0c10;color:#fff;border:1px solid #374151;padding:4px;"></div>
          <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:12px;">
            <button id="info-cancel">取消</button>
            <button id="info-save" style="background:#45a29e;border:none;color:#000;font-weight:bold;padding:4px 12px;">保存</button>
          </div>
        </div>`;document.body.appendChild(dlg);dlg.querySelector('#info-cancel').onclick =() =>dlg.remove();dlg.querySelector('#info-save').onclick =() =>{chart.title =dlg.querySelector('#info-title').value ||'Untitled';chart.artist =dlg.querySelector('#info-artist').value ||'';chart.charter =dlg.querySelector('#info-charter').value ||'';chart.difficulty =dlg.querySelector('#info-diff').value ||'';updateInfoPreview();dlg.remove();};dlg.addEventListener('keydown',(e) =>{if (e.key ==='Enter') dlg.querySelector('#info-save').click();});};}
 let metronomeEnabled =false;let lastMetroBeat =null;let metroCtx =null;function ensureMetroCtx() {if (!metroCtx) metroCtx =new (window.AudioContext ||window.webkitAudioContext)();if (metroCtx.state ==='suspended') metroCtx.resume();return metroCtx;}
function tickMetronome(nowBeat) {if (!metronomeEnabled) return;const b =Math.floor(nowBeat + 1e-9);if (lastMetroBeat ===b) return;lastMetroBeat =b;try {const ctx =ensureMetroCtx();const o =ctx.createOscillator();const g =ctx.createGain();const isDown =(b % 4) ===0;o.frequency.value =isDown ?1200 :800;g.gain.value =0.0001;o.connect(g);g.connect(ctx.destination);const t0 =ctx.currentTime;g.gain.exponentialRampToValueAtTime(0.08,t0 + 0.005);g.gain.exponentialRampToValueAtTime(0.0001,t0 + 0.06);o.start(t0);o.stop(t0 + 0.07);} catch (e) {}
}
if (btnMetronome) {btnMetronome.onclick =() =>{metronomeEnabled =!metronomeEnabled;btnMetronome.classList.toggle('active',metronomeEnabled);lastMetroBeat =null;if (metronomeEnabled) ensureMetroCtx();};}
window.__tickMetronome =() =>{if (!engine.isPlaying) {lastMetroBeat =null;return;}
tickMetronome(engine.currentBeat);};function shiftNow(dir) {const d =input.snapDivisor ||4;const step =1 / d;let b =engine.isPlaying ?engine.currentBeat :engine.scrollBeat;const snapped =Math.round(b *d) / d;let next =snapped + dir *step;if (next < 0) next =0;engine.scrollBeat =next;engine.currentBeat =next;if (audio.buffer) {try {audio.seek(Math.max(0,chart.beatToTime(next)));} catch (e) {}
}
updateStatus();}
if (btnShiftUp) btnShiftUp.onclick =() =>shiftNow(1);if (btnShiftDown) btnShiftDown.onclick =() =>shiftNow(-1);if (btnGlobalList) btnGlobalList.onclick =() =>{if (chart.ensureUniqueIds) chart.ensureUniqueIds();const old =document.getElementById('global-list-dlg');if (old) old.remove();const dlg =document.createElement('div');dlg.id ='global-list-dlg';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:2100;display:flex;align-items:center;justify-content:center;';const rows =(chart.globalEvents||[]).slice().sort((a,b)=>a.beat-b.beat).map(g =>{return `<tr>
        <td style="padding:4px 6px;">${(g.type||'').toUpperCase()}</td>
        <td style="padding:4px 6px;font-family:monospace;opacity:.75;">${g.id}</td>
        <td style="padding:4px 6px;">${Number(g.beat).toFixed(3)}</td>
        <td style="padding:4px 6px;">${Number(g.duration).toFixed(3)}</td>
        <td style="padding:4px 6px;">
          <button data-act="go" data-id="${g.id}">定位</button>
          <button data-act="sel" data-id="${g.id}">选中</button>
          <button data-act="del" data-id="${g.id}" style="color:#ef4444;">删</button>
        </td>
      </tr>`;}).join('') ||'<tr><td colspan="5" style="padding:8px;opacity:.7;">暂无全局事件</td></tr>';dlg.innerHTML =`
      <div style="width:520px;max-height:70vh;overflow:auto;background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:14px;color:#fff;">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px;">
          <strong>全局事件列表</strong>
          <button id="global-list-close">关闭</button>
        </div>
        <table style="width:100%;border-collapse:collapse;font-size:12px;">
          <thead><tr><th align="left">类型</th><th align="left">id</th><th align="left">开始</th><th align="left">时长</th><th></th></tr></thead>
          <tbody>${rows}</tbody>
        </table>
      </div>`;document.body.appendChild(dlg);dlg.querySelector('#global-list-close').onclick =() =>dlg.remove();dlg.querySelectorAll('button[data-act]').forEach(btn =>{btn.onclick =() =>{const id =btn.getAttribute('data-id');const g =(chart.globalEvents||[]).find(x =>x.id ===id);if (!g) return;const act =btn.getAttribute('data-act');if (act ==='del') {commands.execute(new DeleteGlobalEventCommand(chart,id));btn.closest('tr').remove();updateStatus();return;}
engine.selectedGlobalId =g.id;engine.selectedNoteId =null;engine.selectedEdgeId =null;engine.selectedEdgeRef =null;engine.scrollBeat =Math.max(0,g.beat);window.updateGlobalPanel();updateStatus();if (act ==='go') dlg.remove();};});};if (btnMinimap) {btnMinimap.style.display ='none';engine.showMinimap =false;}
if (btnQuantize) {btnQuantize.style.display ='none';}
if (btnValidate) btnValidate.onclick =() =>{if (chart.ensureUniqueIds) chart.ensureUniqueIds();const issues =[];const ids =[];chart.notes.forEach(n =>ids.push(['note',n.id]));chart.edgeEvents.forEach(e =>ids.push(['edge',e.id]));(chart.globalEvents||[]).forEach(g =>ids.push(['global',g.id]));const seen =new Map();ids.forEach(([k,id]) =>{const key =String(id);if (seen.has(key)) issues.push('重复ID ' + key + ' (' + seen.get(key) + ' 与 ' + k + ')');else seen.set(key,k);});chart.notes.forEach(n =>{if (n.type ==='hold' &&!(n.endBeat >n.beat)) issues.push('Hold ' + n.id + ' 结束<=开始');if (n.beat < 0) issues.push('音符 ' + n.id + ' beat<0');});chart.edgeEvents.forEach(e =>{if ((e.interval||0) <=0) issues.push('边线 ' + e.id + ' interval 无效');if ((e.loSpread||0) <=0 ||(e.lfSpread||0) <=0) issues.push('边线 ' + e.id + ' 蔓延时间无效');});(chart.globalEvents||[]).forEach(g =>{if (g.type ==='censor' &&(g.fadeIn||0)+(g.fadeOut||0) >(g.duration||0)+1e-9) {issues.push('Censor ' + g.id + ' 淡入+淡出 > 总时长');}
});let ov =0;for (let i=0;i<chart.notes.length;i++){for (let j=0;j<i;j++){const a=chart.notes[i],b=chart.notes[j];if (a.lane!==b.lane) continue;if (input._notesOverlap &&input._notesOverlap(a,b)) ov++;}
}
if (ov) issues.push('检测到约 ' + ov + ' 组同轨近距/重叠');alert(issues.length ?('校验结果：\n- ' + issues.join('\n- ')) :'校验通过：未发现明显问题');};window.updateAudioBar =() =>{const duration =audio.duration ||(chart.audioData &&chart.audioData.duration) ||0;if (!duration) {audioFill.style.width ='0%';audioThumb.style.left ='0%';audioTime.textContent ='0:00 / 0:00';return;}
let cur =audio.isPlaying ?audio.getCurrentTime() :Math.max(0,chart.beatToTime(engine.scrollBeat));cur =Math.max(0,Math.min(cur,duration));const pct =Math.min(100,(cur / duration) *100);audioFill.style.width =pct + '%';audioThumb.style.left =pct + '%';const fmt =(s) =>{s =Math.max(0,s ||0);return `${Math.floor(s / 60)}:${String(Math.floor(s % 60)).padStart(2, '0')}`;};audioTime.textContent =`${fmt(cur)} / ${fmt(duration)}`;};audioBar.addEventListener('mousedown',(e) =>{audioDragging =true;seekFromBar(e);});window.addEventListener('mousemove',(e) =>{if (audioDragging) seekFromBar(e);});window.addEventListener('mouseup',() =>{audioDragging =false;});function seekFromBar(e) {const rect =audioBar.getBoundingClientRect();const pct =Math.max(0,Math.min(1,(e.clientX - rect.left) / rect.width));const duration =audio.duration ||(chart.audioData &&chart.audioData.duration) ||0;if (duration) {const t =pct *duration;audio.seek(t);engine.scrollBeat =Math.max(0,chart.timeToBeat(t));if (audio.isPlaying) engine.currentBeat =engine.scrollBeat;}
}
inputBPM.value =chart.baseBPM;inputBPM.onchange =() =>{chart.baseBPM =parseFloat(inputBPM.value) ||120;chart.bpmList[0].bpm =chart.baseBPM;updateStatus();};btnAddBPM.onclick =() =>{const beat =engine.isPlaying ?engine.currentBeat :engine.scrollBeat;commands.execute(new AddBPMPointCommand(chart,Math.max(0,beat),parseFloat(inputBPM.value) ||chart.bpmAt(beat)));updateStatus();};function updateSnap() {const v =parseInt(inputCustomSnap.value,10);input.setSnap(v &&v >0 ?v :parseInt(selectSnap.value,10));}
selectSnap.onchange =updateSnap;inputCustomSnap.oninput =updateSnap;updateSnap();selectRate.onchange =() =>audio.setRate(parseFloat(selectRate.value));btnAudio.onclick =() =>fileAudio.click();fileAudio.onchange =async (e) =>{const file =e.target.files[0];if (!file) return;try {const ab =await file.arrayBuffer();const dur =await audio.loadArrayBuffer(ab);chart.audioData ={name:file.name,type:file.type,duration:dur,_arrayBuffer:ab.slice(0) };document.getElementById('status-audio').textContent =`音频: ${file.name} (${dur.toFixed(2)}s)`;window.updateZoomSlider(engine.pixelsPerBeat);} catch (err) {alert('音频解码失败: ' + err.message);}
};btnImport.onclick =() =>fileImport.click();fileImport.onchange =async (e) =>{const file =e.target.files[0];if (!file) return;try {const zip =await JSZip.loadAsync(file);let cFile =null,aFile =null;zip.forEach((p,o) =>{if (p.endsWith('chart.json')) cFile =o;else if (!o.dir &&/\.(mp3|ogg|wav|m4a)$/.test(p)) aFile =o;});if (!cFile) {alert('ZIP 内未找到 chart.json');return;}
const data =JSON.parse(await cFile.async('string'));const nc =Chart.fromJSON(data);if (nc._idFixCount) {console.warn('Fixed duplicate ids:',nc._idFixCount);const tip =document.createElement('div');tip.style.cssText ='position:fixed;top:12px;left:50%;transform:translateX(-50%);background:#3b1d1d;border:1px solid #ef4444;color:#fecaca;padding:6px 12px;border-radius:6px;z-index:2500;font-size:12px;';tip.textContent ='谱面存在重复ID，已自动修复 ' + nc._idFixCount + ' 处（请重新导出保存）';document.body.appendChild(tip);setTimeout(() =>tip.remove(),4500);}
if (aFile) {const ab =await aFile.async('arraybuffer');const dur =await audio.loadArrayBuffer(ab);nc.audioData ={name:aFile.name,type:'audio/*',duration:dur,_arrayBuffer:ab.slice(0) };document.getElementById('status-audio').textContent =`音频: ${aFile.name} (${dur.toFixed(2)}s)`;}
chart.title =nc.title;chart.artist =nc.artist;chart.charter =nc.charter;chart.difficulty =nc.difficulty;chart.offset =nc.offset;chart.baseBPM =nc.baseBPM;chart.bpmList =nc.bpmList;chart.baseSpeed =nc.baseSpeed;chart.notes =nc.notes;chart.edgeEvents =nc.edgeEvents ||[];chart.globalEvents =nc.globalEvents ||[];if (chart.ensureUniqueIds) chart.ensureUniqueIds();chart.audioData =nc.audioData ||chart.audioData;commands.clear();engine.selectedNoteId =null;engine.selectedEdgeId =null;engine.selectedEdgeRef =null;engine.selectedGlobalId =null;engine.overlappingIds.clear();engine.scrollBeat =0;engine.currentBeat =0;inputBPM.value =chart.baseBPM;updateStatus();window.updateSidePanel();window.updateEdgePanel();window.updateGlobalPanel();if (window.refreshOverlapPanel) window.refreshOverlapPanel();alert('谱面导入成功!');} catch (err) {alert('导入失败: ' + err.message);}
};btnExport.onclick =async () =>{const zip =new JSZip();zip.file('chart.json',JSON.stringify(chart.toJSON(),null,2));if (chart.audioData &&chart.audioData._arrayBuffer) zip.file(chart.audioData.name,chart.audioData._arrayBuffer);const blob =await zip.generateAsync({type:'blob' });const a =document.createElement('a');a.href =URL.createObjectURL(blob);a.download =(chart.title ||'chart') + '.zip';a.click();URL.revokeObjectURL(a.href);};btnUndo.onclick =() =>{commands.undo();window.updateSidePanel &&window.updateSidePanel();window.updateEdgePanel &&window.updateEdgePanel();window.updateGlobalPanel &&window.updateGlobalPanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();};btnRedo.onclick =() =>{commands.redo();window.updateSidePanel &&window.updateSidePanel();window.updateEdgePanel &&window.updateEdgePanel();window.updateGlobalPanel &&window.updateGlobalPanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();};const panelPlaceholder =document.getElementById('panel-placeholder');const notePanel =document.getElementById('note-panel');const edgePanel =document.getElementById('edge-panel');const globalPanel =document.getElementById('global-panel');const pnBeat =document.getElementById('pn-beat');const pnEndWrap =document.getElementById('pn-endbeat-wrap');const pnEnd =document.getElementById('pn-endbeat');const pnSpeed =document.getElementById('pn-speed');const pnEasing =document.getElementById('pn-easing');const pnCurve =document.getElementById('pn-curve');const pnExpr =document.getElementById('pn-expr');const pnExprApply =document.getElementById('pn-expr-apply');const pnEdgeLit =document.getElementById('pn-edge-lit');const pnDelete =document.getElementById('pn-delete');function gcd(a,b) {a =Math.abs(a|0);b =Math.abs(b|0);while (b) {const t =a % b;a =b;b =t;}
return a ||1;}
 function formatBeatTime(beat,divisor) {const a =Math.floor(beat + 1e-9);let frac =Math.max(0,beat - a);let C =Math.max(1,divisor ||4);let B =Math.round(frac *C);if (B >=C) return `${a + 1}:0/1`;if (B <=0) return `${a}:0/1`;const g =gcd(B,C);B =B / g;C =C / g;return `${a}:${B}/${C}`;}
function hideAllPanels() {panelPlaceholder.style.display ='none';notePanel.style.display ='none';if (edgePanel) edgePanel.style.display ='none';if (globalPanel) globalPanel.style.display ='none';}
 function drawNoteCurve(n) {if (!pnCurve ||!n) return;const c =pnCurve.getContext('2d');const W =pnCurve.width,H =pnCurve.height;c.clearRect(0,0,W,H);const sp =n.speedMultiplier ||1;const isCustom =n.easing ==='custom' &&n.curvePoints &&n.curvePoints.length >=2;let pts;if (isCustom) {pts =n.curvePoints;} else {const N =50;pts =[];for (let i =0;i <=N;i++) {const t =i / N;const e =sampleEasing(n.easing ||'easeLinear',t);pts.push({t,v:sp *e *2 });}
}
let vMin =Infinity,vMax =-Infinity;for (const p of pts) {if (p.v < vMin) vMin =p.v;if (p.v >vMax) vMax =p.v;}
const pad =Math.max(0.05,(vMax - vMin) *0.12);vMin -=pad;vMax +=pad;function vY(v) {return H - ((v - vMin) / (vMax - vMin)) *H;}
 c.strokeStyle ='rgba(255,255,255,0.05)';c.beginPath();for (let i=0;i<=4;i++){const x=i*W/4;c.moveTo(x,0);c.lineTo(x,H);}
c.stroke();if (vMin < 0 &&vMax >0) {const zy =vY(0);c.strokeStyle='rgba(255,0,0,0.3)';c.setLineDash([2,3]);c.beginPath();c.moveTo(0,zy);c.lineTo(W,zy);c.stroke();c.setLineDash([]);}
 c.strokeStyle ='#66fcf1';c.lineWidth =1.5;c.beginPath();for (let i=0;i<=Math.min(100,pts.length*5);i++){const tt=i/Math.min(100,pts.length*5);let a=pts[0],b=pts[pts.length-1];for(let j=0;j<pts.length-1;j++){if(tt>=pts[j].t&&tt<=pts[j+1].t){a=pts[j];b=pts[j+1];break;}}
const u=(b.t-a.t)<1e-9?0:(tt-a.t)/(b.t-a.t);const vv=a.v+(b.v-a.v)*u;const x=tt*W,y=vY(vv);if(i===0)c.moveTo(x,y);else c.lineTo(x,y);}
c.stroke();if (isCustom) pts.forEach((p,i)=>{c.fillStyle=i===0||i===pts.length-1?'#f1c40f':'#4facfe';c.beginPath();c.arc(p.t*W,vY(p.v),3,0,Math.PI*2);c.fill();});c.fillStyle='rgba(255,255,255,0.35)';c.font='9px sans-serif';c.fillText(vMax.toFixed(1),2,10);c.fillText(vMin.toFixed(1),2,H-3);c.fillText('速时',W-25,H-3);}
 if (pnCurve) pnCurve.addEventListener('dblclick',(e) =>{const n =chart.getNoteById(engine.selectedNoteId);if (!n) return;const old =document.getElementById('note-curve-dlg');if (old) {old.remove();return;}
const dlg =document.createElement('div');dlg.id ='note-curve-dlg';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.8);z-index:3600;display:flex;align-items:center;justify-content:center;';dlg.innerHTML ='<div style="width:min(86vw,660px);height:min(60vh,400px);background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:14px;display:flex;flex-direction:column;"><div style="display:flex;justify-content:space-between;margin-bottom:8px;"><strong style="color:#66fcf1;">音符流速曲线</strong><button id="nc-close" style="color:#ef4444;background:none;border:none;cursor:pointer;font-size:18px">×</button></div><canvas id="nc-canvas" style="flex:1;width:100%;background:#0b0c10;border:1px solid #374151;border-radius:4px;cursor:crosshair"></canvas><div style="color:#80868b;font-size:11px;margin-top:6px">单击空白加点 | 双击控制点删除 | 拖拽修改</div></div>';document.body.appendChild(dlg);const ncCanvas =document.getElementById('nc-canvas');ncCanvas.oncontextmenu =ev =>ev.preventDefault();document.getElementById('nc-close').onclick =() =>dlg.remove();drawNoteBig(n,ncCanvas);let ncDrag =-1;ncCanvas.onmousedown =(ev) =>{if (!n) return;if (n.easing !=='custom') {n.easing ='custom';if (pnEasing) pnEasing.value ='custom';}
if (!n.curvePoints ||n.curvePoints.length < 2) {n.curvePoints =[{t:0,v:0},{t:1,v:(n.speedMultiplier||1)*2}];}
const rect =ncCanvas.getBoundingClientRect();const nx =(ev.clientX-rect.left)/rect.width;const pts =n.curvePoints;let near =-1;for (let i=1;i<pts.length-1;i++){if(Math.abs(pts[i].t-nx)<0.04){near=i;break;} }
if (near>=0 &&ev.detail===2){pts.splice(near,1);commitNotePts(n);drawNoteBig(n,ncCanvas);return;}
for (let i=0;i<pts.length;i++){if(Math.abs(pts[i].t-nx)<0.04){ncDrag=i;break;} }
if (ncDrag<0){const ny=1-(ev.clientY-rect.top)/rect.height;pts.push({t:nx,v:ny});pts.sort((a,b)=>a.t-b.t);pts[0].t=0;pts[pts.length-1].t=1;commitNotePts(n);drawNoteBig(n,ncCanvas);}
};window.addEventListener('mousemove',(mv)=>{if (ncDrag<0) return;const rect=ncCanvas.getBoundingClientRect();let nx=(mv.clientX-rect.left)/rect.width,ny=1-(mv.clientY-rect.top)/rect.height;const pts=n.curvePoints;if (ncDrag===0) pts[0].v=ny;else if (ncDrag===pts.length-1) pts[pts.length-1].v=ny;else {pts[ncDrag].t=Math.max(0.02,Math.min(0.98,nx));pts[ncDrag].v=ny;pts.sort((a,b)=>a.t-b.t);}
drawNoteBig(n,ncCanvas);});window.addEventListener('mouseup',()=>{if(ncDrag>=0)commitNotePts(n);ncDrag=-1;});function commitNotePts(nn){commands.execute(new UpdateNoteParamsCommand(chart,nn.id,{easing:'custom',curvePoints:JSON.parse(JSON.stringify(nn.curvePoints))
}));if (pnEasing) pnEasing.value ='custom';window.updateSidePanel();}
function drawNoteBig(nn,ec){const cc=ec.getContext('2d');const dpr=window.devicePixelRatio||1;const W=ec.clientWidth,H=ec.clientHeight;ec.width=W*dpr;ec.height=H*dpr;cc.setTransform(dpr,0,0,dpr,0,0);cc.clearRect(0,0,W,H);const sp=nn.speedMultiplier||1;const isCustom=nn.easing==='custom'&&nn.curvePoints&&nn.curvePoints.length>=2;let pts;if(isCustom)pts=nn.curvePoints;else{pts=[];for(let i=0;i<=80;i++){const t=i/80;const e=sampleEasing(nn.easing||'easeLinear',t);pts.push({t,v:sp*e*2});}}
let vMin=Infinity,vMax=-Infinity;for(const p of pts){if(p.v<vMin)vMin=p.v;if(p.v>vMax)vMax=p.v;}
const pad=Math.max(0.05,(vMax-vMin)*0.12);vMin-=pad;vMax+=pad;function vY(v){return H-((v-vMin)/(vMax-vMin))*H;}
cc.strokeStyle='rgba(255,255,255,0.04)';cc.beginPath();for(let i=0;i<=4;i++){const x=i*W/4;cc.moveTo(x,0);cc.lineTo(x,H);}cc.stroke();if(vMin<0&&vMax>0){cc.strokeStyle='rgba(255,0,0,0.3)';cc.setLineDash([2,3]);cc.beginPath();cc.moveTo(0,vY(0));cc.lineTo(W,vY(0));cc.stroke();cc.setLineDash([]);}
cc.strokeStyle='#66fcf1';cc.lineWidth=2;cc.beginPath();for(let i=0;i<=100;i++){const tt=i/100;let a=pts[0],b=pts[pts.length-1];for(let j=0;j<pts.length-1;j++){if(tt>=pts[j].t&&tt<=pts[j+1].t){a=pts[j];b=pts[j+1];break;}}
const u=(b.t-a.t)<1e-9?0:(tt-a.t)/(b.t-a.t);const x=tt*W,y=vY(a.v+(b.v-a.v)*u);if(i===0)cc.moveTo(x,y);else cc.lineTo(x,y);}cc.stroke();if(isCustom)pts.forEach((p,i)=>{cc.fillStyle=i===0||i===pts.length-1?'#f1c40f':'#4facfe';cc.beginPath();cc.arc(p.t*W,vY(p.v),5,0,Math.PI*2);cc.fill();cc.strokeStyle='rgba(255,255,255,0.5)';cc.lineWidth=1;cc.stroke();});cc.fillStyle='rgba(255,255,255,0.4)';cc.font='11px sans-serif';cc.fillText(vMax.toFixed(1),4,14);cc.fillText(vMin.toFixed(1),4,H-4);}
});if (pnExprApply &&pnExpr) pnExprApply.onclick =() =>{const n =chart.getNoteById(engine.selectedNoteId);if (!n) return;const expr =(pnExpr.value ||'').trim();if (!expr) return;if (!window.ChartRuntime ||!ChartRuntime.evaluateExpression) {alert('运行时未加载');return;}
n.easing ='custom';if (pnEasing) pnEasing.value ='custom';const pts =[];for (let i =0;i <=40;i++) {const t =i / 40;let v =ChartRuntime.evaluateExpression(expr,t);if (isNaN(v)) v =0;pts.push({t,v });}
n.curvePoints =pts;n.expression =expr;commands.execute(new UpdateNoteParamsCommand(chart,n.id,{easing:'custom',curvePoints:JSON.parse(JSON.stringify(pts)),expression:expr
}));window.updateSidePanel();};window.updateSidePanel =() =>{const id =engine.selectedNoteId;if (!id) {notePanel.style.display ='none';return;}
const n =chart.getNoteById(id);if (!n) {engine.selectedNoteId =null;window.updateSidePanel();return;}
hideAllPanels();notePanel.style.display ='';pnBeat.textContent =formatBeatTime(n.beat,n.timeDivisor ||4);if (n.type ==='hold') {pnEndWrap.style.display ='';pnEnd.textContent =formatBeatTime(n.endBeat,n.endTimeDivisor ||n.timeDivisor ||4);}
else pnEndWrap.style.display ='none';pnSpeed.value =n.speedMultiplier;if (pnEasing) pnEasing.value =n.easing ||'easeLinear';if (pnEdgeLit) pnEdgeLit.checked =n.edgeLit ||false;if (pnCurve) {const hasCustom =n.easing ==='custom' &&n.curvePoints &&n.curvePoints.length >=2;const isSpecial =(n.easing &&n.easing !=='easeLinear' &&n.easing !=='linear') ||hasCustom ||(n.speedMultiplier !==1.0);if (isSpecial) {pnCurve.style.display ='';if (pnExpr) {pnExpr.style.display ='';pnExpr.value =n.expression ||'';}
if (pnExprApply) pnExprApply.style.display ='';drawNoteCurve(n);} else {pnCurve.style.display ='none';if (pnExpr) pnExpr.style.display ='none';if (pnExprApply) pnExprApply.style.display ='none';}
}
};pnSpeed.onchange =() =>{const n =chart.getNoteById(engine.selectedNoteId);if (!n) return;const v =parseFloat(pnSpeed.value) ||1.0;if (v ===n.speedMultiplier) return;commands.execute(new UpdateNoteParamsCommand(chart,n.id,{speedMultiplier:v }));};if (pnEasing) pnEasing.onchange =() =>{const n =chart.getNoteById(engine.selectedNoteId);if (!n) return;const v =pnEasing.value ||'easeLinear';if (v ===(n.easing ||'easeLinear')) return;commands.execute(new UpdateNoteParamsCommand(chart,n.id,{easing:v }));window.updateSidePanel();};if (pnEdgeLit) pnEdgeLit.onchange =() =>{const n =chart.getNoteById(engine.selectedNoteId);if (!n) return;commands.execute(new UpdateNoteParamsCommand(chart,n.id,{edgeLit:pnEdgeLit.checked }));};pnDelete.onclick =() =>{const id =engine.selectedNoteId;if (!id) return;commands.execute(new DeleteNoteCommand(chart,id));engine.selectedNoteId =null;engine.overlappingIds.delete(id);window.updateSidePanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();};const epEdgeId =document.getElementById('ep-edge-id');const epLoSpread =document.getElementById('ep-lo-spread');const epLoSpreadUnit =document.getElementById('ep-lo-spread-unit');const epLoGrad =document.getElementById('ep-lo-grad');const epLfSpread =document.getElementById('ep-lf-spread');const epLfSpreadUnit =document.getElementById('ep-lf-spread-unit');const epLfGrad =document.getElementById('ep-lf-grad');const epInterval =document.getElementById('ep-interval');const epIntervalUnit =document.getElementById('ep-interval-unit');const epDuration =document.getElementById('ep-duration');const epLoGlowI =document.getElementById('ep-lo-glow-i');const epLoGlowR =document.getElementById('ep-lo-glow-r');const epLfGlowI =document.getElementById('ep-lf-glow-i');const epLfGlowR =document.getElementById('ep-lf-glow-r');const epSelectMode =document.getElementById('ep-select-mode');const epDelete =document.getElementById('ep-delete');const epClone =document.getElementById('ep-clone');function beatToUnit(beat,unit,atBeat) {if (unit ==='sec') return (beat / chart.bpmAt(atBeat)) *60;return beat;}
function unitToBeat(val,unit,atBeat) {if (unit ==='sec') return (val / 60) *chart.bpmAt(atBeat);return val;}
window.updateEdgePanel =() =>{if (!edgePanel) return;const id =engine.selectedEdgeId;if (!id &&!engine.selectedEdgeRef) {edgePanel.style.display ='none';return;}
let ev =(engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef))
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===id);if (!ev) {engine.selectedEdgeId =null;engine.selectedEdgeRef =null;window.updateEdgePanel();return;}
 engine.selectedEdgeId =ev.id;engine.selectedEdgeRef =ev;hideAllPanels();edgePanel.style.display ='';epEdgeId.textContent =`E${ev.edgeId}`;epLoSpread.value =beatToUnit(ev.loSpread,epLoSpreadUnit.value,ev.beat).toFixed(3);epLoGrad.value =ev.loGradient;epLfSpread.value =beatToUnit(ev.lfSpread,epLfSpreadUnit.value,ev.lfStartBeat).toFixed(3);epLfGrad.value =ev.lfGradient;epInterval.value =beatToUnit(ev.interval,epIntervalUnit.value,ev.beat).toFixed(3);epDuration.textContent =`${ev.duration.toFixed(3)} 拍`;epLoGlowI.value =ev.loGlowIntensity;epLoGlowR.value =ev.loGlowRadius;epLfGlowI.value =ev.lfGlowIntensity;epLfGlowR.value =ev.lfGlowRadius;const inf =document.getElementById('ep-show-influence');if (inf) inf.checked =engine.edgeInfluenceId ===ev.id;};function applyEdgeChange() {let ev =(engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef))
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===engine.selectedEdgeId);if (!ev) return;engine.selectedEdgeId =ev.id;engine.selectedEdgeRef =ev;const patch ={loSpread:Math.max(0.01,unitToBeat(parseFloat(epLoSpread.value) ||0.25,epLoSpreadUnit.value,ev.beat)),loGradient:parseFloat(epLoGrad.value) ||0,lfSpread:Math.max(0.01,unitToBeat(parseFloat(epLfSpread.value) ||0.25,epLfSpreadUnit.value,ev.lfStartBeat)),lfGradient:parseFloat(epLfGrad.value) ||0,interval:Math.max(0.01,unitToBeat(parseFloat(epInterval.value) ||0.5,epIntervalUnit.value,ev.beat)),loGlowIntensity:parseFloat(epLoGlowI.value) ||1,loGlowRadius:parseFloat(epLoGlowR.value) ||0,lfGlowIntensity:parseFloat(epLfGlowI.value) ||1,lfGlowRadius:parseFloat(epLfGlowR.value) ||0,};const changed =Object.keys(patch).some(k =>patch[k] !==ev[k]);if (changed) commands.execute(new UpdateEdgeEventCommand(chart,ev.id,patch,ev));window.updateEdgePanel();}
[epLoSpread,epLfSpread,epInterval,epLoSpreadUnit,epLfSpreadUnit,epIntervalUnit,epLoGrad,epLfGrad,epLoGlowI,epLoGlowR,epLfGlowI,epLfGlowR].forEach(el =>{if (el) el.onchange =applyEdgeChange;});if (epSelectMode) epSelectMode.onclick =() =>{const ev =(engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef))
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===engine.selectedEdgeId);if (!ev) {alert('请先选中边线参数线段');return;}
input.startEndpointEdit('edge',ev);epSelectMode.classList.add('active');const tip =document.createElement('div');tip.id ='endpoint-edit-tip';tip.style.cssText ='position:fixed;bottom:70px;left:50%;transform:translateX(-50%);background:#1a1d26;border:1px solid #ff2a6d;color:#fff;padding:6px 12px;border-radius:6px;z-index:1200;font-size:12px;';tip.innerHTML ='端点编辑：先点击线段某一端点抓取，移动鼠标实时吸附，再点击放置 · <button id="endpoint-edit-cancel">取消</button>';document.getElementById('endpoint-edit-tip')?.remove();document.body.appendChild(tip);document.getElementById('endpoint-edit-cancel').onclick =() =>{input.cancelEndpointEdit();epSelectMode.classList.remove('active');tip.remove();};};const gpSelectMode =document.getElementById('gp-select-mode');if (gpSelectMode) gpSelectMode.onclick =() =>{const g =(chart.globalEvents ||[]).find(x =>x.id ===engine.selectedGlobalId);if (!g) {alert('请先选中全局参数线段');return;}
input.startEndpointEdit('global',g);gpSelectMode.classList.add('active');const tip =document.createElement('div');tip.id ='endpoint-edit-tip';tip.style.cssText ='position:fixed;bottom:70px;left:50%;transform:translateX(-50%);background:#1a1d26;border:1px solid #ff2a6d;color:#fff;padding:6px 12px;border-radius:6px;z-index:1200;font-size:12px;';tip.innerHTML ='端点编辑：先点端点抓取，移动实时吸附，再点击放置 · <button id="endpoint-edit-cancel">取消</button>';document.getElementById('endpoint-edit-tip')?.remove();document.body.appendChild(tip);document.getElementById('endpoint-edit-cancel').onclick =() =>{input.cancelEndpointEdit();gpSelectMode.classList.remove('active');tip.remove();};};if (epDelete) epDelete.onclick =() =>{const ev =(engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef))
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===engine.selectedEdgeId);if (!ev) return;const id =ev.id;commands.execute(new DeleteEdgeEventCommand(chart,id,ev));if (engine.edgeInfluenceId ===id) engine.edgeInfluenceId =null;engine.selectedEdgeId =null;engine.selectedEdgeRef =null;window.updateEdgePanel();updateStatus();};const epInfluence =document.getElementById('ep-show-influence');if (epInfluence) {epInfluence.onchange =() =>{const id =engine.selectedEdgeId;if (!id) {epInfluence.checked =false;return;}
if (epInfluence.checked) engine.edgeInfluenceId =id;else if (engine.edgeInfluenceId ===id) engine.edgeInfluenceId =null;};}
 let cloneSourceEdge =null;if (epClone) epClone.onclick =() =>{const ev =(engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef))
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===engine.selectedEdgeId);if (!ev) return;cloneSourceEdge ={loSpread:ev.loSpread,lfSpread:ev.lfSpread,interval:ev.interval,loGradient:ev.loGradient,lfGradient:ev.lfGradient,loGlowIntensity:ev.loGlowIntensity,loGlowRadius:ev.loGlowRadius,lfGlowIntensity:ev.lfGlowIntensity,lfGlowRadius:ev.lfGlowRadius,beat:ev.beat,duration:ev.duration,edgeId:ev.edgeId,id:ev.id,_ref:ev
};alert('已复制边线参数。请点击目标边线线段，将弹出克隆选项。');};const _oldUpdateEdgePanel =window.updateEdgePanel;window.updateEdgePanel =() =>{_oldUpdateEdgePanel &&_oldUpdateEdgePanel();if (!cloneSourceEdge ||!engine.selectedEdgeId) return;const target =(engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef))
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===engine.selectedEdgeId);if (!target) return;if (target.id ===cloneSourceEdge.id &&target.edgeId ===cloneSourceEdge.edgeId &&target.beat ===cloneSourceEdge.beat) return;if (cloneSourceEdge._ref &&cloneSourceEdge._ref ===target) return;openCloneDialog(cloneSourceEdge,target);};function openCloneDialog(src,dst) {const old =document.getElementById('clone-dialog');if (old) old.remove();const dlg =document.createElement('div');dlg.id ='clone-dialog';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:2000;display:flex;align-items:center;justify-content:center;';dlg.innerHTML =`
      <div style="width:360px;background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:14px;color:#fff;">
        <div style="font-weight:bold;margin-bottom:10px;">克隆参数信息 E${src.edgeId} → E${dst.edgeId}</div>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="loSpread" checked> LO蔓延</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="lfSpread" checked> LF蔓延</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="interval" checked> 启动间隔</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="loGradient" checked> LO渐变</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="lfGradient" checked> LF渐变</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="loGlowIntensity" checked> LO发光强度</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="loGlowRadius" checked> LO发光半径</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="lfGlowIntensity" checked> LF发光强度</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="lfGlowRadius" checked> LF发光半径</label>
        <label style="display:block;margin:4px 0;"><input type="checkbox" data-k="duration"> 线段总时长</label>
        <label style="display:block;margin:4px 0 10px;"><input type="checkbox" id="clone-keep-start"> 保留起始端（复制起止时间位置）</label>
        <div style="display:flex;gap:8px;justify-content:flex-end;">
          <button id="clone-cancel" style="padding:4px 10px;">取消</button>
          <button id="clone-ok" style="padding:4px 10px;background:#45a29e;border:none;color:#000;font-weight:bold;">确认</button>
        </div>
      </div>`;document.body.appendChild(dlg);dlg.querySelector('#clone-cancel').onclick =() =>{cloneSourceEdge =null;dlg.remove();};dlg.querySelector('#clone-ok').onclick =() =>{const patch ={};dlg.querySelectorAll('input[data-k]').forEach(cb =>{if (!cb.checked) return;const k =cb.getAttribute('data-k');if (k ==='duration') return;patch[k] =src[k];});const durCb =dlg.querySelector('input[data-k="duration"]');const keepStart =dlg.querySelector('#clone-keep-start').checked;if (durCb &&durCb.checked) {if (keepStart) {patch.beat =src.beat;patch.interval =src.interval;patch.lfSpread =src.lfSpread;} else {const total =src.interval + src.lfSpread;const ratio =total >0 ?src.interval / total :0.5;patch.interval =total *ratio;patch.lfSpread =total - patch.interval;}
} else if (keepStart) {patch.beat =src.beat;}
if (Object.keys(patch).length) commands.execute(new UpdateEdgeEventCommand(chart,dst.id,patch,dst));cloneSourceEdge =null;dlg.remove();window.updateEdgePanel();};}
 let edgeAddBar =null;if (btnEdgeAdd) btnEdgeAdd.onclick =() =>{if (input.edgeAddMode) {input.edgeAddMode =false;input.edgeAddSelected.clear();btnEdgeAdd.textContent ='+ 边线参数';if (edgeAddBar) {edgeAddBar.remove();edgeAddBar =null;}
return;}
input.edgeAddMode =true;input.edgeAddSelected.clear();btnEdgeAdd.textContent ='选择边线';edgeAddBar =document.createElement('div');edgeAddBar.style.cssText ='position:fixed;bottom:70px;left:50%;transform:translateX(-50%);background:#1a1d26;border:1px solid var(--border);padding:6px 12px;border-radius:6px;display:flex;gap:8px;align-items:center;z-index:1000;';edgeAddBar.innerHTML =`
      <span style="font-size:12px;color:var(--text-dim);">点击轨道选择边线</span>
      <button id="edge-add-ok" style="background:var(--accent-dim);border:none;color:#000;padding:3px 10px;border-radius:3px;cursor:pointer;font-size:12px;font-weight:bold;">✓ 确认</button>
      <button id="edge-add-cancel" style="background:var(--bg-hover);border:1px solid var(--border);color:var(--text);padding:3px 10px;border-radius:3px;cursor:pointer;font-size:12px;">✗ 取消</button>
    `;document.body.appendChild(edgeAddBar);document.getElementById('edge-add-ok').onclick =() =>{if (input.edgeAddSelected.size ===0) {alert('请至少选择一条边线');return;}
const beat =Math.max(0,engine.isPlaying ?engine.currentBeat :engine.scrollBeat);const batch =new BatchCommand();input.edgeAddSelected.forEach(edgeId =>{batch.add(new AddEdgeEventCommand(chart,{edgeId,beat,loSpread:0.25,lfSpread:0.5,interval:0.5,loGradient:0,lfGradient:0,loGlowIntensity:1,loGlowRadius:0,lfGlowIntensity:1,lfGlowRadius:0
}));});commands.execute(batch);batch.commands.forEach(c =>{if (c.edge &&input.checkEdgeOverlaps) input.checkEdgeOverlaps(c.edge);});input.edgeAddMode =false;input.edgeAddSelected.clear();btnEdgeAdd.textContent ='+ 边线参数';edgeAddBar.remove();edgeAddBar =null;updateStatus();};document.getElementById('edge-add-cancel').onclick =() =>{input.edgeAddMode =false;input.edgeAddSelected.clear();btnEdgeAdd.textContent ='+ 边线参数';edgeAddBar.remove();edgeAddBar =null;};};window.updateEdgeAddUI =() =>{};const gpType =document.getElementById('gp-type');const gpKind =document.getElementById('gp-kind');const gpDuration =document.getElementById('gp-duration');const gpStart =document.getElementById('gp-start');const gpEnd =document.getElementById('gp-end');const gpEasing =document.getElementById('gp-easing');const gpSvcam =document.getElementById('gp-svcam');const gpCensor =document.getElementById('gp-censor');const gpDir =document.getElementById('gp-dir');const gpFadeIn =document.getElementById('gp-fadein');const gpFadeOut =document.getElementById('gp-fadeout');const gpDelete =document.getElementById('gp-delete');const gpCurve =document.getElementById('gp-curve');const gpExpr =document.getElementById('gp-expr');const gpExprApply =document.getElementById('gp-expr-apply');const gpOffset =document.getElementById('gp-offset');const gpAmplitude =document.getElementById('gp-amplitude');const gpAmpWrap =document.getElementById('gp-amp-wrap');function sampleEasing(name,t,curvePoints) {if (window.ChartRuntime &&ChartRuntime.sampleEasing) {return ChartRuntime.sampleEasing(name,t,curvePoints);}
t =Math.max(0,Math.min(1,t));if (name ==='custom') return t;return t;}
function drawCurveEditor(g) {if (!gpCurve ||!g ||g.type ==='censor') return;const c =gpCurve.getContext('2d');const W =gpCurve.width,H =gpCurve.height;c.clearRect(0,0,W,H);const sv =g.startValue !=null ?g.startValue :(g.type ==='camera' ?100 :1);const ev =g.endValue !=null ?g.endValue :(g.type ==='camera' ?100 :1);let pts;if (g.easing ==='custom' &&g.curvePoints &&g.curvePoints.length >=2) {pts =g.curvePoints;} else {const N =50;pts =[];const same =Math.abs(sv - ev) < 1e-6;const amp =same ?(g.amplitude ||(g.type ==='camera' ?20 :0.3)) :0;for (let i =0;i <=N;i++) {const t =i / N;const e =sampleEasing(g.easing ||'linear',t);if (same) {const dev =amp *(e - t);pts.push({t,v:sv + dev });} else {pts.push({t,v:sv + (ev - sv) *e });}
}
}
 let vMin =Infinity,vMax =-Infinity;for (const p of pts) {if (p.v < vMin) vMin =p.v;if (p.v >vMax) vMax =p.v;}
const pad =Math.max(0.1,(vMax - vMin) *0.15);vMin -=pad;vMax +=pad;if (vMax - vMin < 0.01) {vMin -=1;vMax +=1;}
function vToY(v) {return H - ((v - vMin) / (vMax - vMin)) *H;}
 function yAt(t) {if (pts.length < 2) return pts[0] ?pts[0].v :0;let a =pts[0],b =pts[pts.length - 1];for (let i =0;i < pts.length - 1;i++) {if (t >=pts[i].t &&t <=pts[i + 1].t) {a =pts[i];b =pts[i + 1];break;}
}
const u =(b.t - a.t) < 1e-9 ?0 :(t - a.t) / (b.t - a.t);return a.v + (b.v - a.v) *u;}
 c.strokeStyle ='rgba(255,255,255,0.08)';c.beginPath();for (let i=0;i<=4;i++){const x=i*W/4;c.moveTo(x,0);c.lineTo(x,H);}
c.stroke();if (vMin < 0 &&vMax >0) {const zy =vToY(0);c.strokeStyle ='rgba(255,0,0,0.3)';c.setLineDash([3,4]);c.beginPath();c.moveTo(0,Math.max(0,Math.min(H,zy)));c.lineTo(W,Math.max(0,Math.min(H,zy)));c.stroke();c.setLineDash([]);}
 c.strokeStyle ='#66fcf1';c.lineWidth=2;c.beginPath();for (let i=0;i<=50;i++){const tt=i/50;const x=tt*W;const y=vToY(yAt(tt));if(i===0) c.moveTo(x,Math.max(0,Math.min(H,y)));else c.lineTo(x,Math.max(0,Math.min(H,y)));}
c.stroke();if (g.type ==='sv') {c.fillStyle ='rgba(102,252,241,0.08)';c.beginPath();let cum =0;const dt =1/50;for (let i=0;i<=50;i++) {const tt =i*dt;cum +=yAt(tt) *dt;const x =tt *W;const dispY =vToY(cum *0.2 + (vMin + vMax) / 2);if (i===0) {c.moveTo(x,H);c.lineTo(x,Math.max(0,Math.min(H,dispY)));}
else c.lineTo(x,Math.max(0,Math.min(H,dispY)));}
c.lineTo(W,H);c.closePath();c.fill();c.fillStyle ='rgba(255,255,255,0.35)';c.font ='9px sans-serif';c.fillText('位移积分预览',4,H-4);}
 if (g.type ==='camera' &&window.ChartRuntime &&ChartRuntime.sampleDerivative) {c.strokeStyle ='rgba(167,139,250,0.45)';c.lineWidth =1;c.setLineDash([2,4]);c.beginPath();const sv =g.startValue ||0;const ev =g.endValue ||1;for (let i=0;i<=50;i++) {const tt=i/50;const dv =ChartRuntime.sampleDerivative(g.easing ||'linear',tt,g.curvePoints,sv,ev);const dy =vToY(dv + (sv||0));const x =tt*W;if(i===0) c.moveTo(x,Math.max(0,Math.min(H,dy)));else c.lineTo(x,Math.max(0,Math.min(H,dy)));}
c.stroke();c.setLineDash([]);c.fillStyle ='rgba(167,139,250,0.5)';c.font ='9px sans-serif';c.fillText('导数预览',W-50,12);}
 if (g.easing ==='custom') {pts.forEach((p,idx)=>{c.fillStyle =idx===0||idx===pts.length-1 ?'#f1c40f' :'#4facfe';c.beginPath();c.arc(p.t*W,Math.max(0,Math.min(H,vToY(p.v))),4,0,Math.PI*2);c.fill();});}
 c.fillStyle ='rgba(255,255,255,0.4)';c.font ='10px sans-serif';c.fillText(vMax.toFixed(1),2,12);c.fillText(vMin.toFixed(1),2,H - 4);if (g.type ==='sv') c.fillText('速时',W - 30,H - 4);if (g.type ==='camera') c.fillText('位时',W - 30,H - 4);}
 if (gpCurve) gpCurve.addEventListener('dblclick',(e) =>{const g =getSelectedGlobal();if (!g ||g.type ==='censor') return;const old =document.getElementById('curve-expand-dlg');if (old) {old.remove();return;}
const dlg =document.createElement('div');dlg.id ='curve-expand-dlg';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.8);z-index:3500;display:flex;align-items:center;justify-content:center;';dlg.innerHTML ='<div style="width:min(88vw,680px);height:min(65vh,420px);background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:14px;display:flex;flex-direction:column;"><div style="display:flex;justify-content:space-between;margin-bottom:8px;"><strong style="color:#66fcf1;">' + (g.type==='sv'?'SV 速时曲线':'Camera 位时曲线') + (g.easing!=='custom'?(' ['+(g.easing||'linear')+']'):' [custom]') + '</strong><button id="curve-exp-close" style="color:#ef4444;background:none;border:none;cursor:pointer;font-size:18px">×</button></div><canvas id="curve-exp-canvas" style="flex:1;width:100%;background:#0b0c10;border:1px solid #374151;border-radius:4px;cursor:crosshair"></canvas><div style="color:#80868b;font-size:11px;margin-top:6px">单击空白添加点 | 双击控制点删除 | 拖拽控制点移动</div></div>';document.body.appendChild(dlg);const expCanvas =document.getElementById('curve-exp-canvas');expCanvas.oncontextmenu =ev =>ev.preventDefault();document.getElementById('curve-exp-close').onclick =() =>dlg.remove();drawExpCurve(g,expCanvas);let expDragIdx =-1;expCanvas.onmousedown =(ev) =>{if (!g ||g.easing !=='custom') return;if (!g.curvePoints ||g.curvePoints.length<2) {const sv=g.startValue!=null?g.startValue:(g.type==='camera'?100:1);const evv=g.endValue!=null?g.endValue:(g.type==='camera'?100:1);g.curvePoints=[{t:0,v:sv},{t:1,v:evv}];}
const rect=expCanvas.getBoundingClientRect();const nx=(ev.clientX-rect.left)/rect.width;const pts=g.curvePoints;let near=-1;for(let i=1;i<pts.length-1;i++){if(Math.abs(pts[i].t-nx)<0.03){near=i;break;}
}
if (near>=0 &&ev.detail===2){pts.splice(near,1);commitExpPts(g);drawExpCurve(g,expCanvas);return;}
 for(let i=0;i<pts.length;i++){if(Math.abs(pts[i].t-nx)<0.03){expDragIdx=i;break;}
}
if (expDragIdx<0){const ny=1-(ev.clientY-rect.top)/rect.height;pts.push({t:nx,v:ny});pts.sort((a,b)=>a.t-b.t);pts[0].t=0;pts[pts.length-1].t=1;commitExpPts(g);drawExpCurve(g,expCanvas);}
};window.addEventListener('mousemove',(mv)=>{if(expDragIdx<0)return;const rect=expCanvas.getBoundingClientRect();let nx=(mv.clientX-rect.left)/rect.width,ny=1-(mv.clientY-rect.top)/rect.height;const pts=g.curvePoints;if(expDragIdx===0){pts[0].v=ny;}
else if(expDragIdx===pts.length-1){pts[pts.length-1].v=ny;}
else{pts[expDragIdx].t=Math.max(0.02,Math.min(0.98,nx));pts[expDragIdx].v=ny;pts.sort((a,b)=>a.t-b.t);}
drawExpCurve(g,expCanvas);});window.addEventListener('mouseup',()=>{if(expDragIdx>=0){commitExpPts(g);}
expDragIdx=-1;});function commitExpPts(gg){const pts=gg.curvePoints;if(!pts)return;commands.execute(new UpdateGlobalEventCommand(chart,gg.id,{curvePoints:JSON.parse(JSON.stringify(pts)),easing:'custom'}));if(gpEasing)gpEasing.value='custom';if(window.updateGlobalPanel)window.updateGlobalPanel();}
function drawExpCurve(gg,ec){const cc=ec.getContext('2d');const dpr=window.devicePixelRatio||1;const W=ec.clientWidth,H=ec.clientHeight;ec.width=W*dpr;ec.height=H*dpr;cc.setTransform(dpr,0,0,dpr,0,0);cc.clearRect(0,0,W,H);const sv=gg.startValue!=null?gg.startValue:(gg.type==='camera'?100:1);const evv=gg.endValue!=null?gg.endValue:(gg.type==='camera'?100:1);const same=Math.abs(sv-evv)<1e-6;const amp=same?(gg.amplitude||(gg.type==='camera'?20:0.3)):0;const isCustom=gg.easing==='custom'&&gg.curvePoints&&gg.curvePoints.length>=2;let pts;if(isCustom)pts=gg.curvePoints;else{pts=[];for(let i=0;i<=80;i++){const t=i/80;const e=sampleEasing(gg.easing||'linear',t);pts.push({t,v:same?(sv+amp*(e-t)):(sv+(evv-sv)*e)});}}
let vMin=Infinity,vMax=-Infinity;for(const p of pts){if(p.v<vMin)vMin=p.v;if(p.v>vMax)vMax=p.v;}
const pad=Math.max(0.1,(vMax-vMin)*0.12);vMin-=pad;vMax+=pad;function vY(v){return H-((v-vMin)/(vMax-vMin))*H;}
cc.strokeStyle='rgba(255,255,255,0.05)';cc.beginPath();for(let i=0;i<=4;i++){const x=i*W/4;cc.moveTo(x,0);cc.lineTo(x,H);}
cc.stroke();if(vMin<0&&vMax>0){cc.strokeStyle='rgba(255,0,0,0.35)';cc.setLineDash([3,4]);cc.beginPath();cc.moveTo(0,vY(0));cc.lineTo(W,vY(0));cc.stroke();cc.setLineDash([]);}
cc.strokeStyle='#66fcf1';cc.lineWidth=2;cc.beginPath();for(let i=0;i<=100;i++){const tt=i/100;let a=pts[0],b=pts[pts.length-1];for(let j=0;j<pts.length-1;j++){if(tt>=pts[j].t&&tt<=pts[j+1].t){a=pts[j];b=pts[j+1];break;}}
const u=(b.t-a.t)<1e-9?0:(tt-a.t)/(b.t-a.t);const vv=a.v+(b.v-a.v)*u;const x=tt*W,y=vY(vv);if(i===0)cc.moveTo(x,y);else cc.lineTo(x,y);}
cc.stroke();if(isCustom)pts.forEach((p,i)=>{cc.fillStyle=i===0||i===pts.length-1?'#f1c40f':'#4facfe';cc.beginPath();cc.arc(p.t*W,vY(p.v),5,0,Math.PI*2);cc.fill();cc.strokeStyle='rgba(255,255,255,0.5)';cc.lineWidth=1;cc.stroke();});cc.fillStyle='rgba(255,255,255,0.4)';cc.font='11px sans-serif';cc.fillText(vMax.toFixed(1),4,14);cc.fillText(vMin.toFixed(1),4,H-4);cc.fillText(gg.type==='sv'?'速时':'位时',W-40,H-4);}
});if (gpCurve) {if (gpExprApply &&gpExpr) gpExprApply.onclick =() =>{const g =getSelectedGlobal();if (!g ||g.type ==='censor') {alert('仅 SV/Camera 可用');return;}
const expr =(gpExpr.value ||'').trim();if (!expr) return;if (!window.ChartRuntime ||!ChartRuntime.evaluateExpression) {alert('运行时未加载');return;}
if (g.type ==='camera' &&!g.offsetMode) {g.offsetMode =true;g.amplitude =30;if (gpOffset) gpOffset.checked =true;if (gpAmpWrap) gpAmpWrap.style.display ='';}
g.easing ='custom';if (gpEasing) gpEasing.value ='custom';const pts =[];for (let i =0;i <=40;i++) {const t =i / 40;let v =ChartRuntime.evaluateExpression(expr,t);if (isNaN(v)) v =0;pts.push({t,v });}
g.curvePoints =pts;g.expression =expr;commands.execute(new UpdateGlobalEventCommand(chart,g.id,{curvePoints:JSON.parse(JSON.stringify(pts)),expression:expr,easing:'custom',offsetMode:g.offsetMode ||false,amplitude:g.amplitude ||20
}));window.updateGlobalPanel();};let dragIdx =-1;let curveDirty =false;let curveBefore =null;gpCurve.onmousedown =(e) =>{const g =getSelectedGlobal();if (!g ||g.type==='censor') return;curveBefore ={easing:g.easing ||'linear',curvePoints:g.curvePoints ?JSON.parse(JSON.stringify(g.curvePoints)) :[{t:0,v:0},{t:1,v:1}]
};if (g.easing !=='custom') {g.easing ='custom';if (gpEasing) gpEasing.value='custom';}
if (!g.curvePoints ||g.curvePoints.length<2) g.curvePoints=[{t:0,v:0},{t:1,v:1}];const rect =gpCurve.getBoundingClientRect();const x=(e.clientX-rect.left)/rect.width;const y=1-(e.clientY-rect.top)/rect.height;dragIdx=-1;g.curvePoints.forEach((p,i)=>{const dx=p.t-x,dy=p.v-y;if (dx*dx+dy*dy<0.01) dragIdx=i;});if (dragIdx<0) {g.curvePoints.push({t:Math.max(0.02,Math.min(0.98,x)),v:y});g.curvePoints.sort((a,b)=>a.t-b.t);g.curvePoints[0].t=0;g.curvePoints[g.curvePoints.length-1].t=1;}
curveDirty =true;drawCurveEditor(g);};window.addEventListener('mousemove',(e) =>{if (dragIdx<0) return;const g =getSelectedGlobal();if (!g) return;const rect =gpCurve.getBoundingClientRect();let x=(e.clientX-rect.left)/rect.width;let y=1-(e.clientY-rect.top)/rect.height;if (dragIdx===0){g.curvePoints[0].t=0;g.curvePoints[0].v=y;}
else if (dragIdx===g.curvePoints.length-1){g.curvePoints[dragIdx].t=1;g.curvePoints[dragIdx].v=y;}
else {g.curvePoints[dragIdx].t=Math.max(0.02,Math.min(0.98,x));g.curvePoints[dragIdx].v=y;g.curvePoints.sort((a,b)=>a.t-b.t);}
drawCurveEditor(g);curveDirty =true;});window.addEventListener('mouseup',() =>{if (curveDirty) {const g =getSelectedGlobal();if (g &&g.type !=='censor' &&g.curvePoints &&curveBefore) {const after ={easing:'custom',curvePoints:JSON.parse(JSON.stringify(g.curvePoints))
};g.easing =curveBefore.easing;g.curvePoints =JSON.parse(JSON.stringify(curveBefore.curvePoints));commands.execute(new UpdateGlobalEventCommand(chart,g.id,after));}
}
dragIdx=-1;curveDirty=false;curveBefore=null;});}
const _oldUpdateGlobalPanel =window.updateGlobalPanel;window.updateGlobalPanel =() =>{_oldUpdateGlobalPanel &&_oldUpdateGlobalPanel();const g =getSelectedGlobal();if (g) drawCurveEditor(g);};function getSelectedGlobal() {if (!engine.selectedGlobalId) return null;return (chart.globalEvents ||[]).find(g =>g.id ===engine.selectedGlobalId) ||null;}
window.updateGlobalPanel =() =>{if (!globalPanel) return;const g =getSelectedGlobal();if (!g) {globalPanel.style.display ='none';return;}
hideAllPanels();globalPanel.style.display ='';if (gpType) gpType.textContent =(g.type ||'').toUpperCase();if (gpKind) gpKind.value =g.type;if (gpDuration) gpDuration.value =g.duration;if (gpOffset) {const isCam =g.type ==='camera';gpOffset.style.display =isCam ?'' :'none';gpOffset.checked =!!(isCam &&g.offsetMode);if (gpAmpWrap) gpAmpWrap.style.display =(isCam &&g.offsetMode) ?'' :'none';if (gpAmplitude) gpAmplitude.value =g.amplitude !=null ?g.amplitude :20;if (gpExpr) {gpExpr.style.display =(g.type ==='sv' ||g.type ==='camera') ?'' :'none';gpExpr.value =g.expression ||'';if (gpExprApply) gpExprApply.style.display =(g.type ==='sv' ||g.type ==='camera') ?'' :'none';}
if (gpStart) gpStart.value =g.type ==='camera' &&g.offsetMode ?100 :g.startValue;if (gpEnd) gpEnd.value =g.type ==='camera' &&g.offsetMode ?100 :g.endValue;if (gpStart &&g.type ==='camera' &&g.offsetMode) gpStart.disabled =true;if (gpEnd &&g.type ==='camera' &&g.offsetMode) gpEnd.disabled =true;if (gpStart &&(!g.type ==='camera' ||!g.offsetMode)) gpStart.disabled =false;if (gpEnd &&(!g.type ==='camera' ||!g.offsetMode)) gpEnd.disabled =false;}
if (g.type ==='censor') {if (gpSvcam) gpSvcam.style.display ='none';if (gpCensor) gpCensor.style.display ='';if (gpDir) gpDir.value =g.direction ||'all';if (gpFadeIn) gpFadeIn.value =g.fadeIn;if (gpFadeOut) gpFadeOut.value =g.fadeOut;} else {if (gpSvcam) gpSvcam.style.display ='';if (gpCensor) gpCensor.style.display ='none';if (gpStart) gpStart.value =g.startValue;if (gpEnd) gpEnd.value =g.endValue;if (gpEasing) gpEasing.value =g.easing ||'linear';}
};function applyGlobal() {const g =getSelectedGlobal();if (!g) return;const patch ={};patch.type =gpKind ?gpKind.value :g.type;patch.duration =Math.max(0.125,parseFloat(gpDuration &&gpDuration.value) ||1);if (patch.type ==='censor') {patch.direction =gpDir ?gpDir.value :'all';patch.fadeIn =Math.max(0,parseFloat(gpFadeIn &&gpFadeIn.value) ||0);patch.fadeOut =Math.max(0,parseFloat(gpFadeOut &&gpFadeOut.value) ||0);if (patch.fadeIn + patch.fadeOut >patch.duration + 1e-9) {alert('淡入+淡出不能大于总时长');const half =patch.duration / 2;patch.fadeIn =Math.min(patch.fadeIn,half);patch.fadeOut =Math.min(patch.fadeOut,patch.duration - patch.fadeIn);}
} else {patch.startValue =parseFloat(gpStart &&gpStart.value);patch.endValue =parseFloat(gpEnd &&gpEnd.value);patch.easing =gpEasing ?gpEasing.value :'linear';if (patch.type ==='sv') {if (isNaN(patch.startValue)) patch.startValue =1;if (isNaN(patch.endValue)) patch.endValue =1;} else {if (isNaN(patch.startValue)) patch.startValue =100;if (isNaN(patch.endValue)) patch.endValue =100;}
 if (patch.easing !=='custom') {patch.curvePoints =null;g.curvePoints =null;} else if (g.curvePoints) {patch.curvePoints =g.curvePoints;}
}
const changed =Object.keys(patch).some(k =>JSON.stringify(patch[k]) !==JSON.stringify(g[k]));if (changed) commands.execute(new UpdateGlobalEventCommand(chart,g.id,patch));window.updateGlobalPanel();}
[gpKind,gpDuration,gpStart,gpEnd,gpEasing,gpDir,gpFadeIn,gpFadeOut].forEach(el =>{if (el) el.onchange =applyGlobal;});if (gpDelete) gpDelete.onclick =() =>{const id =engine.selectedGlobalId;if (!id) return;commands.execute(new DeleteGlobalEventCommand(chart,id));engine.selectedGlobalId =null;window.updateGlobalPanel();updateStatus();};if (btnGlobalAdd) btnGlobalAdd.onclick =() =>{const beat =Math.max(0,engine.isPlaying ?engine.currentBeat :engine.scrollBeat);const cmd =new AddGlobalEventCommand(chart,{type:'sv',beat,duration:1,startValue:1,endValue:1,easing:'linear'
});commands.execute(cmd);engine.selectedGlobalId =cmd.event.id;engine.selectedNoteId =null;engine.selectedEdgeId =null;engine.selectedEdgeRef =null;if (input.checkGlobalOverlaps) input.checkGlobalOverlaps(cmd.event);window.updateGlobalPanel();updateStatus();};window.updateStatus =() =>{const selN =(input.selectedIds &&input.selectedIds.size) ||(engine.selectedNoteId ?1 :0);const totalBeats =(typeof window.__getTotalBeats ==='function') ?window.__getTotalBeats(chart) :0;const dens =totalBeats >0 ?(chart.notes.length / totalBeats) :0;document.getElementById('status-note-count').textContent =`Notes: ${chart.notes.length} | Sel: ${selN} | Edges: ${chart.edgeEvents.length} | Global: ${(chart.globalEvents || []).length} | dens ${dens.toFixed(2)}/beat`;const bpms =chart.bpmList.map(p =>`♩${p.bpm}@${p.beat.toFixed(1)}`).join(', ');document.getElementById('status-bpm-list').textContent =`BPM: ${bpms || chart.baseBPM}`;if (btnUndo) {btnUndo.disabled =!commands.canUndo();btnUndo.style.opacity =commands.canUndo() ?'1' :'0.45';}
if (btnRedo) {btnRedo.disabled =!commands.canRedo();btnRedo.style.opacity =commands.canRedo() ?'1' :'0.45';}
};if (btnQuantize) btnQuantize.onclick =() =>{const ids =(input.selectedIds &&input.selectedIds.size)
?[...input.selectedIds]
:(engine.selectedNoteId ?[engine.selectedNoteId] :[]);if (!ids.length) {alert('请先选中音符（可 Shift 多选）');return;}
const d =input.snapDivisor ||4;const snap =(b) =>Math.round(b *d) / d;const batch =new BatchCommand();ids.forEach(id =>{const n =chart.getNoteById(id);if (!n) return;const nb =snap(n.beat);const ne =n.type ==='hold' ?snap(n.endBeat) :null;const extra ={timeDivisor:d };if (n.type ==='hold') extra.endTimeDivisor =d;if (nb !==n.beat ||(ne !=null &&ne !==n.endBeat)) {batch.add(new MoveNoteCommand(chart,id,nb,ne,extra));}
});if (batch.commands.length) commands.execute(batch);window.updateSidePanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();};if (btnMirror) btnMirror.onclick =() =>{const ids =(input.selectedIds &&input.selectedIds.size)
?[...input.selectedIds]
:(engine.selectedNoteId ?[engine.selectedNoteId] :[]);if (!ids.length) {alert('请先选中音符');return;}
const batch =new BatchCommand();ids.forEach(id =>{const n =chart.getNoteById(id);if (!n) return;const nl =(PLAY_COLS - 1) - n.lane;if (nl !==n.lane) batch.add(new ChangeLaneCommand(chart,id,nl,n.lane));});if (batch.commands.length) commands.execute(batch);window.updateSidePanel();updateStatus();};if (btnBpmManage) btnBpmManage.onclick =() =>{const old =document.getElementById('bpm-manage-dlg');if (old) old.remove();const dlg =document.createElement('div');dlg.id ='bpm-manage-dlg';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:2100;display:flex;align-items:center;justify-content:center;';const rows =chart.bpmList.map((p,i) =>{return `<tr data-i="${i}">
        <td style="padding:4px;"><input data-k="beat" type="number" step="0.001" value="${p.beat}" style="width:90px;background:#0b0c10;color:#fff;border:1px solid #374151;" ${p.beat===0?'readonly':''}></td>
        <td style="padding:4px;"><input data-k="bpm" type="number" step="0.01" value="${p.bpm}" style="width:90px;background:#0b0c10;color:#fff;border:1px solid #374151;"></td>
        <td style="padding:4px;">${p.beat===0 ? '<span style="color:#80868b;">初始</span>' : '<button data-del="1" style="color:#ef4444;">删除</button>'}</td>
      </tr>`;}).join('');dlg.innerHTML =`
      <div style="width:420px;max-height:70vh;overflow:auto;background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:14px;color:#fff;">
        <div style="font-weight:bold;margin-bottom:10px;">BPM 变化点</div>
        <table style="width:100%;border-collapse:collapse;font-size:13px;">
          <thead><tr><th align="left">Beat</th><th align="left">BPM</th><th></th></tr></thead>
          <tbody id="bpm-rows">${rows}</tbody>
        </table>
        <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:12px;">
          <button id="bpm-add-row">+ 当前时间</button>
          <button id="bpm-cancel">取消</button>
          <button id="bpm-apply" style="background:#45a29e;border:none;color:#000;font-weight:bold;padding:4px 10px;">应用</button>
        </div>
      </div>`;document.body.appendChild(dlg);dlg.querySelector('#bpm-cancel').onclick =() =>dlg.remove();dlg.querySelector('#bpm-add-row').onclick =() =>{const beat =Math.max(0,engine.isPlaying ?engine.currentBeat :engine.scrollBeat);const bpm =chart.bpmAt(beat);const tb =dlg.querySelector('#bpm-rows');const tr =document.createElement('tr');tr.innerHTML =`<td style="padding:4px;"><input data-k="beat" type="number" step="0.001" value="${beat.toFixed(3)}" style="width:90px;background:#0b0c10;color:#fff;border:1px solid #374151;"></td>
        <td style="padding:4px;"><input data-k="bpm" type="number" step="0.01" value="${bpm}" style="width:90px;background:#0b0c10;color:#fff;border:1px solid #374151;"></td>
        <td style="padding:4px;"><button data-del="1" style="color:#ef4444;">删除</button></td>`;tb.appendChild(tr);tr.querySelector('[data-del]').onclick =() =>tr.remove();};dlg.querySelectorAll('[data-del]').forEach(btn =>{btn.onclick =() =>btn.closest('tr').remove();});dlg.querySelector('#bpm-apply').onclick =() =>{const list =[];dlg.querySelectorAll('#bpm-rows tr').forEach(tr =>{const beat =parseFloat(tr.querySelector('[data-k=beat]').value);const bpm =parseFloat(tr.querySelector('[data-k=bpm]').value);if (!isNaN(beat) &&!isNaN(bpm) &&bpm >0) list.push({beat,bpm });});if (!list.some(p =>p.beat ===0)) {list.push({beat:0,bpm:chart.baseBPM ||120 });}
list.sort((a,b) =>a.beat - b.beat);const map =new Map();list.forEach(p =>map.set(p.beat,p.bpm));const next =[...map.entries()].map(([beat,bpm]) =>({beat,bpm})).sort((a,b)=>a.beat-b.beat);commands.execute(new SetBPMListCommand(chart,next));inputBPM.value =chart.baseBPM;dlg.remove();updateStatus();};};let previewMode =false;let previewCanvas =null;let previewRaf =0;function ensurePreviewUI() {if (previewCanvas) return;const wrap =document.createElement('div');wrap.id ='preview-wrap';wrap.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.92);z-index:3000;display:none;flex-direction:column;';wrap.innerHTML =`
      <div style="padding:8px 12px;display:flex;gap:8px;align-items:center;background:#111;">
        <strong style="color:#66fcf1;">谱面预览</strong>
        <button id="preview-hidden-toggle" style="margin:0 8px;">👁隐藏可见</button><button id="preview-close" style="margin-left:auto;">退出预览</button>
      </div>
      <canvas id="preview-canvas" style="flex:1;width:100%;"></canvas>`;document.body.appendChild(wrap);previewCanvas =document.getElementById('preview-canvas');document.getElementById('preview-close').onclick =() =>stopPreview();document.getElementById('preview-hidden-toggle').onclick =(e) =>{previewShowHidden =!previewShowHidden;e.target.textContent =previewShowHidden ?'👁全部可见' :'👁隐藏可见';};}
function stopPreview() {previewMode =false;if (previewRaf) cancelAnimationFrame(previewRaf);const wrap =document.getElementById('preview-wrap');if (wrap) wrap.style.display ='none';if (previewSeekBar) {previewSeekBar.remove();previewSeekBar =null;}
if (btnPreview) btnPreview.textContent ='▶ 预览';}
function startPreview() {ensurePreviewUI();previewMode =true;document.getElementById('preview-wrap').style.display ='flex';if (btnPreview) btnPreview.textContent ='■ 退出预览';if (audio.buffer &&!audio.isPlaying) {const t =chart.beatToTime(engine.scrollBeat);audio.seek(Math.max(0,t));audio.play();engine.isPlaying =true;document.getElementById('btn-play').textContent ='⏸';}
const loopP =() =>{if (!previewMode) return;drawPreview();previewRaf =requestAnimationFrame(loopP);};previewRaf =requestAnimationFrame(loopP);}
let previewShowHidden =false;let previewSeekBar =null;let previewSeekThumb =null;function drawPreview() {const c =previewCanvas;const dpr =window.devicePixelRatio ||1;const rect =c.parentElement.getBoundingClientRect();if (audio.duration &&!previewSeekBar) {const bar =document.createElement('div');bar.style.cssText ='position:absolute;bottom:30px;left:20%;right:20%;height:8px;background:rgba(255,255,255,0.15);border-radius:4px;cursor:pointer;z-index:3100;';bar.id ='preview-seek';bar.onclick =(e) =>{const pct =(e.clientX - bar.getBoundingClientRect().left) / bar.offsetWidth;audio.seek(pct *audio.duration);};document.getElementById('preview-wrap').appendChild(bar);previewSeekBar =bar;}
if (previewSeekBar &&audio.duration) {const pct =audio.getCurrentTime() / audio.duration;previewSeekBar.style.background =`linear-gradient(to right, var(--accent) ${pct*100}%, rgba(255,255,255,0.15) ${pct*100}%)`;}
 const W =Math.max(100,rect.width);const H =Math.max(100,rect.height - 40);c.width =W *dpr;c.height =H *dpr;c.style.width =W+'px';c.style.height =H+'px';const ctx =c.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,W,H);ctx.fillStyle ='#05060a';ctx.fillRect(0,0,W,H);const nowBeat =audio.isPlaying ?chart.timeToBeat(audio.getCurrentTime()) :engine.scrollBeat;const rt =(window.ChartRuntime &&ChartRuntime.sampleAt)
?ChartRuntime.sampleAt(chart,nowBeat)
:{sv:1,camera:100,censors:[],edges:[] };const cam =rt.camera;const sv =rt.sv;const scale =100 / Math.max(20,cam);const laneCount =6;const laneW =Math.min(70,W *0.08) *scale;const totalW =laneW *laneCount;const left =(W - totalW) / 2;const judgeY =H *0.82;const pxPerBeat =140 *sv *scale;for (let i=0;i<laneCount;i++){const x =left + i*laneW;ctx.fillStyle =i%2?'rgba(102,252,241,0.06)':'rgba(102,252,241,0.10)';ctx.fillRect(x,0,laneW,H);ctx.strokeStyle='rgba(255,255,255,0.08)';ctx.strokeRect(x,0,laneW,H);}
 for (let e=0;e<7;e++){const x =left + e *laneW;const fr =(rt.edges &&rt.edges[e]) ||{loFront:0,lfFront:0,lit:false,loGlow:1,segments:[] };const topY =20;const botY =judgeY;const segs =(fr.segments &&fr.segments.length)
?fr.segments
:(fr.lit ?[{lf:fr.lfFront||0,lo:fr.loFront||0,loGlow:fr.loGlow||1 }] :[]);for (const seg of segs) {const lo =seg.lo ||0,lf =seg.lf ||0;if (lo <=lf) continue;const loY =topY + (botY - topY) *lo;const lfY =topY + (botY - topY) *lf;const glow =Math.max(0.2,seg.loGlow ||fr.loGlow ||1);const grad =ctx.createLinearGradient(0,lfY,0,loY);grad.addColorStop(0,`rgba(255,215,0,${0.05*glow})`);grad.addColorStop(1,`rgba(255,215,0,${0.85*Math.min(1,glow)})`);ctx.strokeStyle =grad;ctx.lineWidth =3;ctx.shadowColor =`rgba(255,215,0,${0.55*glow})`;ctx.shadowBlur =8 + 6*glow;ctx.beginPath();ctx.moveTo(x,lfY);ctx.lineTo(x,loY);ctx.stroke();ctx.shadowBlur =0;}
}
 for (const n of chart.notes) {const dy =(n.beat - nowBeat) *pxPerBeat;const y =judgeY - dy;if (y < -50 ||y >H + 50) continue;if (n.edgeLit &&!previewShowHidden &&window.ChartRuntime &&ChartRuntime.isLaneLit) {if (!ChartRuntime.isLaneLit(chart,n.lane,nowBeat)) continue;}
const x =left + n.lane *laneW + laneW/2;const noteLen =Math.max(8,laneW - 6);const thick =6;if (n.type ==='tap') {ctx.fillStyle ='rgba(102,252,241,0.9)';ctx.beginPath();const r=2;const nx=x-noteLen/2,ny=y-thick/2,nw=noteLen,nh=thick;ctx.moveTo(nx+r,ny);ctx.arcTo(nx+nw,ny,nx+nw,ny+nh,r);ctx.arcTo(nx+nw,ny+nh,nx,ny+nh,r);ctx.arcTo(nx,ny+nh,nx,ny,r);ctx.arcTo(nx,ny,nx+nw,ny,r);ctx.closePath();ctx.fill();} else {const dy2 =(n.endBeat - nowBeat) *pxPerBeat;const y2 =judgeY - dy2;const top =Math.min(y,y2),hh=Math.abs(y2-y);const bodyLen =noteLen *0.72;ctx.fillStyle='rgba(241,196,15,0.35)';ctx.fillRect(x-bodyLen/2,top,bodyLen,Math.max(2,hh));ctx.fillStyle='rgba(241,196,15,0.95)';ctx.fillRect(x-noteLen/2,y-thick/2,noteLen,thick);}
}
 ctx.strokeStyle='#ff2a6d';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(left-20,judgeY);ctx.lineTo(left+totalW+20,judgeY);ctx.stroke();for (const cnv of (rt.censors ||[])) {const alpha =cnv.alpha ||0;if (alpha <=0.001) continue;ctx.fillStyle =`rgba(0,0,0,${0.85*alpha})`;const dir =cnv.direction||'all';const m =Math.min(W,H)*0.25;if (dir==='top' ||dir==='all') ctx.fillRect(0,0,W,m);if (dir==='bottom' ||dir==='all') ctx.fillRect(0,H-m,W,m);if (dir==='left' ||dir==='all') {ctx.fillRect(0,0,m,H);ctx.fillRect(W-m,0,m,H);}
}
ctx.fillStyle='#66fcf1';ctx.font='12px sans-serif';ctx.fillText(`Beat ${nowBeat.toFixed(2)} | SV ${sv.toFixed(2)} | CAM ${cam.toFixed(1)}`,12,18);}
if (btnPreview) btnPreview.onclick =() =>{if (previewMode) stopPreview();else startPreview();};window.addEventListener('keydown',(e) =>{if (e.target.tagName ==='INPUT' ||e.target.tagName ==='SELECT') return;if (e.code ==='Space') {e.preventDefault();togglePlay();}
if (e.key ==='z' &&(e.ctrlKey ||e.metaKey)) {e.preventDefault();commands.undo();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();}
if (e.key ==='y' &&(e.ctrlKey ||e.metaKey)) {e.preventDefault();commands.redo();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();}
if (e.key ==='Delete' ||e.key ==='Backspace') {if (input.selectedIds &&input.selectedIds.size >1) {const batch =new BatchCommand();input.selectedIds.forEach(id =>batch.add(new DeleteNoteCommand(chart,id)));commands.execute(batch);input.selectedIds =new Set();engine.selectedIds =input.selectedIds;engine.selectedNoteId =null;engine.overlappingIds.clear();window.updateSidePanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();} else if (engine.selectedNoteId) {const id =engine.selectedNoteId;commands.execute(new DeleteNoteCommand(chart,id));engine.selectedNoteId =null;engine.overlappingIds.delete(id);window.updateSidePanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();} else if (engine.selectedEdgeId ||engine.selectedEdgeRef) {const ev =engine.selectedEdgeRef &&chart.edgeEvents.includes(engine.selectedEdgeRef)
?engine.selectedEdgeRef
:chart.edgeEvents.find(e =>e.id ===engine.selectedEdgeId);if (ev) commands.execute(new DeleteEdgeEventCommand(chart,ev.id,ev));if (engine.edgeInfluenceId ===(ev &&ev.id)) engine.edgeInfluenceId =null;engine.selectedEdgeId =null;engine.selectedEdgeRef =null;window.updateEdgePanel();updateStatus();} else if (engine.selectedGlobalId) {commands.execute(new DeleteGlobalEventCommand(chart,engine.selectedGlobalId));engine.selectedGlobalId =null;window.updateGlobalPanel();updateStatus();}
}
if (e.key ==='1') setToolUI(Tool.SELECT);if (e.key ==='2') setToolUI(Tool.TAP);if (e.key ==='3') setToolUI(Tool.HOLD);if (e.key ==='c' &&(e.ctrlKey ||e.metaKey)) {e.preventDefault();const hasNotes =(input.selectedIds &&input.selectedIds.size) ||engine.selectedNoteId;let n =0;if (hasNotes) n =input.copySelection ?input.copySelection() :0;else n =input.copyParamSelection ?input.copyParamSelection() :0;if (n) {const tip =document.createElement('div');tip.style.cssText='position:fixed;top:12px;left:50%;transform:translateX(-50%);background:#1f2833;border:1px solid #45a29e;color:#c5c6c7;padding:4px 10px;border-radius:6px;z-index:2500;font-size:12px;';tip.textContent =hasNotes ?('已复制 ' + n + ' 个音符') :'已复制参数线段';document.body.appendChild(tip);setTimeout(()=>tip.remove(),1200);}
}
if (e.key ==='v' &&(e.ctrlKey ||e.metaKey)) {e.preventDefault();let n =0;if (input.clipboard &&input.clipboard.length) n =input.pasteClipboard ?input.pasteClipboard() :0;else if (input.paramClipboard) n =input.pasteParamClipboard ?input.pasteParamClipboard() :0;updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();if (window.refreshParamOverlapPanel) window.refreshParamOverlapPanel();if (n) {const tip =document.createElement('div');tip.style.cssText='position:fixed;top:12px;left:50%;transform:translateX(-50%);background:#1f2833;border:1px solid #45a29e;color:#c5c6c7;padding:4px 10px;border-radius:6px;z-index:2500;font-size:12px;';tip.textContent ='已粘贴';document.body.appendChild(tip);setTimeout(()=>tip.remove(),1200);}
}
if (e.key ==='Escape') {if (input.cancelEndpointEdit) input.cancelEndpointEdit();document.getElementById('endpoint-edit-tip')?.remove();epSelectMode &&epSelectMode.classList.remove('active');document.getElementById('gp-select-mode')?.classList.remove('active');}
 if (e.key ==='a' &&(e.ctrlKey ||e.metaKey)) {e.preventDefault();input.selectedIds =new Set(chart.notes.map(n =>n.id));engine.selectedIds =input.selectedIds;if (chart.notes[0]) engine.selectedNoteId =chart.notes[0].id;window.updateSidePanel();updateStatus();}
 if (e.key ==='f' &&(e.ctrlKey ||e.metaKey)) {e.preventDefault();let target =null;if (input.selectedIds &&input.selectedIds.size) {const arr =[...input.selectedIds].map(id =>chart.getNoteById(id)).filter(Boolean);if (arr.length) target =arr.sort((a,b)=>a.beat-b.beat)[0];} else if (engine.selectedNoteId) {target =chart.getNoteById(engine.selectedNoteId);} else if (engine.selectedEdgeId) {target =chart.edgeEvents.find(ev =>ev.id ===engine.selectedEdgeId);} else if (engine.selectedGlobalId) {target =(chart.globalEvents||[]).find(g =>g.id ===engine.selectedGlobalId);}
if (target) {engine.scrollBeat =Math.max(0,target.beat);if (!audio.isPlaying) engine.currentBeat =engine.scrollBeat;}
}
 if (['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(e.key)) {const ids =(input.selectedIds &&input.selectedIds.size)
?[...input.selectedIds]
:(engine.selectedNoteId ?[engine.selectedNoteId] :[]);if (!ids.length) return;e.preventDefault();const step =e.shiftKey ?1 :(1 / (input.snapDivisor ||4));const batch =new BatchCommand();ids.forEach(id =>{const n =chart.getNoteById(id);if (!n) return;if (e.key ==='ArrowLeft' ||e.key ==='ArrowRight') {const dir =e.key ==='ArrowRight' ?1 :-1;const nl =Math.max(0,Math.min(5,n.lane + dir));if (nl !==n.lane) batch.add(new ChangeLaneCommand(chart,id,nl,n.lane));} else {const dir =e.key ==='ArrowUp' ?1 :-1;const nb =Math.max(0,n.beat + dir *step);let ne =n.endBeat;if (n.type ==='hold' &&n.endBeat !=null) ne =Math.max(nb + 0.125,n.endBeat + dir *step);const extra ={timeDivisor:input.snapDivisor ||n.timeDivisor ||4 };if (n.type ==='hold') extra.endTimeDivisor =input.snapDivisor ||n.endTimeDivisor ||4;const oldState ={beat:n.beat,endBeat:n.endBeat,extras:{timeDivisor:n.timeDivisor,endTimeDivisor:n.endTimeDivisor }
};if (nb !==n.beat ||ne !==n.endBeat) {batch.add(new MoveNoteCommand(chart,id,nb,ne,extra,oldState));}
}
});if (batch.commands.length) commands.execute(batch);window.updateSidePanel();updateStatus();if (window.refreshOverlapPanel) window.refreshOverlapPanel();}
});window.addEventListener('resize',() =>engine.resize());if (screen.orientation &&screen.orientation.lock) screen.orientation.lock('landscape').catch(() =>{});const paramOverlapPanel =document.createElement('div');paramOverlapPanel.id ='param-overlap-panel';paramOverlapPanel.style.cssText ='position:fixed;bottom:44px;right:350px;width:320px;background:#1a1d26;border:1px solid #f59e0b;border-radius:8px;color:#fff;font-size:13px;z-index:1000;display:none;flex-direction:column;overflow:hidden;box-shadow:0 8px 24px rgba(0,0,0,0.5);';paramOverlapPanel.innerHTML =`
    <div id="param-overlap-header" style="padding:8px 12px;background:rgba(245,158,11,0.15);border-bottom:1px solid rgba(245,158,11,0.4);font-weight:bold;display:flex;justify-content:space-between;align-items:center;cursor:pointer;user-select:none;">
      <span>⚠️ 重叠参数 <span id="param-overlap-count" style="color:#fcd34d;font-weight:normal;">(0)</span></span>
      <span id="param-overlap-toggle" style="color:#fcd34d;font-size:12px;">收起 ▾</span>
    </div>
    <div id="param-overlap-list" style="overflow-y:auto;padding:8px 12px;max-height:200px;"></div>
  `;document.body.appendChild(paramOverlapPanel);let paramOverlapCollapsed =false;const paramOverlapListEl =document.getElementById('param-overlap-list');const paramOverlapCountEl =document.getElementById('param-overlap-count');const paramOverlapToggleEl =document.getElementById('param-overlap-toggle');document.getElementById('param-overlap-header').onclick =() =>{paramOverlapCollapsed =!paramOverlapCollapsed;paramOverlapListEl.style.display =paramOverlapCollapsed ?'none' :'block';paramOverlapToggleEl.textContent =paramOverlapCollapsed ?'展开 ▸' :'收起 ▾';};const paramOverlapRegistry =new Map();function renderParamOverlapPanel() {for (const [id,rec] of [...paramOverlapRegistry.entries()]) {if (rec.kind ==='edge') {if (!chart.edgeEvents.some(e =>e.id ===id)) paramOverlapRegistry.delete(id);} else if (!(chart.globalEvents ||[]).some(g =>g.id ===id)) {paramOverlapRegistry.delete(id);}
}
const items =[...paramOverlapRegistry.values()];paramOverlapCountEl.textContent ='(' + items.length + ')';paramOverlapListEl.innerHTML ='';if (!items.length) {paramOverlapPanel.style.display ='none';return;}
paramOverlapPanel.style.display ='flex';paramOverlapPanel.style.visibility ='visible';paramOverlapListEl.style.display =paramOverlapCollapsed ?'none' :'block';items.forEach(rec =>{const row =document.createElement('div');row.style.cssText ='display:flex;justify-content:space-between;align-items:center;padding:5px 0;border-bottom:1px solid rgba(255,255,255,0.06);gap:8px;';let label ='';if (rec.kind ==='edge') {const ev =rec.ref;label ='Edge E' + ev.edgeId + ' · ' + Number(ev.beat).toFixed(2) + ' ~ ' + (ev.beat + (ev.interval ||0)).toFixed(2);} else {const g =rec.ref;label =(g.type ||'').toUpperCase() + ' · ' + Number(g.beat).toFixed(2) + ' +' + Number(g.duration).toFixed(2);}
row.innerHTML ='<span style="flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">' + label + '</span>' +
'<button data-kind="' + rec.kind + '" data-id="' + rec.ref.id + '" class="param-ov-del" style="background:rgba(239,68,68,0.2);border:1px solid #ef4444;color:#ef4444;border-radius:3px;padding:2px 8px;cursor:pointer;font-size:12px;">删除</button>';paramOverlapListEl.appendChild(row);});paramOverlapListEl.querySelectorAll('.param-ov-del').forEach(btn =>{btn.onclick =(ev) =>{ev.stopPropagation();const kind =btn.getAttribute('data-kind');const id =btn.getAttribute('data-id');if (kind ==='edge') {const edge =chart.edgeEvents.find(x =>x.id ===id);if (edge) commands.execute(new DeleteEdgeEventCommand(chart,id,edge));engine.overlappingEdgeIds &&engine.overlappingEdgeIds.delete(id);} else {commands.execute(new DeleteGlobalEventCommand(chart,id));engine.overlappingGlobalIds &&engine.overlappingGlobalIds.delete(id);}
paramOverlapRegistry.delete(id);renderParamOverlapPanel();updateStatus();};});}
window.reportParamOverlap =(kind,focus,overlaps) =>{if (!focus) {renderParamOverlapPanel();return;}
if (overlaps &&overlaps.length) {paramOverlapRegistry.set(String(focus.id),{kind,ref:focus });paramOverlapPanel.style.display ='flex';} else {paramOverlapRegistry.delete(String(focus.id));}
renderParamOverlapPanel();};window.refreshParamOverlapPanel =() =>renderParamOverlapPanel();const overlapPanel =document.createElement('div');overlapPanel.id ='overlap-panel';overlapPanel.style.cssText ='position:fixed;bottom:44px;right:16px;width:320px;background:#1a1d26;border:1px solid #ef4444;border-radius:8px;color:#fff;font-size:13px;z-index:1000;display:none;flex-direction:column;overflow:hidden;box-shadow:0 8px 24px rgba(0,0,0,0.5);';overlapPanel.innerHTML =`
    <div id="overlap-header" style="padding:8px 12px;background:rgba(239,68,68,0.15);border-bottom:1px solid rgba(239,68,68,0.4);font-weight:bold;display:flex;justify-content:space-between;align-items:center;cursor:pointer;user-select:none;">
      <span>⚠️ 重叠按键 <span id="overlap-count" style="color:#fca5a5;font-weight:normal;">(0)</span></span>
      <span id="overlap-toggle" style="color:#fca5a5;font-size:12px;">收起 ▾</span>
    </div>
    <div id="overlap-list" style="overflow-y:auto;padding:8px 12px;max-height:200px;"></div>
  `;document.body.appendChild(overlapPanel);let overlapCollapsed =false;const overlapListEl =document.getElementById('overlap-list');const overlapCountEl =document.getElementById('overlap-count');const overlapToggleEl =document.getElementById('overlap-toggle');document.getElementById('overlap-header').onclick =() =>{overlapCollapsed =!overlapCollapsed;overlapListEl.style.display =overlapCollapsed ?'none' :'block';overlapToggleEl.textContent =overlapCollapsed ?'展开 ▸' :'收起 ▾';};const overlapRegistry =new Map();function formatBeatSimple(beat,div) {return formatBeatTime(beat,div ||4);}
function renderOverlapPanel() {for (const id of [...overlapRegistry.keys()]) {if (!chart.getNoteById(id) ||!engine.overlappingIds.has(id)) {overlapRegistry.delete(id);engine.overlappingIds.delete(id);}
}
for (const id of engine.overlappingIds) {const n =chart.getNoteById(id);if (!n) {engine.overlappingIds.delete(id);continue;}
overlapRegistry.set(id,n);}
const div =input.snapDivisor ||4;const items =[...overlapRegistry.values()].sort((a,b) =>a.beat - b.beat ||a.lane - b.lane);overlapCountEl.textContent =`(${items.length})`;overlapListEl.innerHTML ='';if (items.length ===0) {overlapPanel.style.display ='none';return;}
overlapPanel.style.display ='flex';overlapListEl.style.display =overlapCollapsed ?'none' :'block';overlapToggleEl.textContent =overlapCollapsed ?'展开 ▸' :'收起 ▾';items.forEach(n =>{const row =document.createElement('div');row.style.cssText ='display:flex;justify-content:space-between;align-items:center;padding:5px 0;border-bottom:1px solid rgba(255,255,255,0.06);gap:8px;';row.innerHTML =`
        <span style="flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">
          ${n.type === 'tap' ? 'Tap' : 'Hold'} · L${n.lane} · ${formatBeatSimple(n.beat, div)}
        </span>
        <button data-id="${n.id}" class="overlap-del" style="background:rgba(239,68,68,0.2);border:1px solid #ef4444;color:#ef4444;border-radius:3px;padding:2px 8px;cursor:pointer;font-size:12px;flex-shrink:0;">删除</button>
      `;overlapListEl.appendChild(row);});overlapListEl.querySelectorAll('.overlap-del').forEach(btn =>{btn.onclick =(e) =>{e.stopPropagation();const id =btn.getAttribute('data-id');commands.execute(new DeleteNoteCommand(chart,id));engine.overlappingIds.delete(id);overlapRegistry.delete(id);if (engine.selectedNoteId ===id) engine.selectedNoteId =null;updateStatus();if (window.updateSidePanel) window.updateSidePanel();rescanOverlaps();};});}
function rescanOverlaps() {engine.overlappingIds.clear();overlapRegistry.clear();const list =[...chart.notes];for (let i =0;i < list.length;i++) {const n =list[i];for (let j =0;j < i;j++) {const o =list[j];if (o.lane !==n.lane) continue;if (input._notesOverlap &&input._notesOverlap(n,o)) {engine.overlappingIds.add(n.id);overlapRegistry.set(n.id,n);break;}
}
}
renderOverlapPanel();}
window.reportOverlap =(note) =>{if (!note) return;engine.overlappingIds.add(note.id);overlapRegistry.set(note.id,note);renderOverlapPanel();};window.refreshOverlapPanel =() =>renderOverlapPanel();window.showOverlapPopup =(notes) =>{if (notes &&notes[0]) window.reportOverlap(notes[0]);};if (btnEdgeList) btnEdgeList.onclick =() =>{if (chart.ensureUniqueIds) chart.ensureUniqueIds();const old =document.getElementById('edge-list-dlg');if (old) old.remove();const dlg =document.createElement('div');dlg.id ='edge-list-dlg';dlg.style.cssText ='position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:2100;display:flex;align-items:center;justify-content:center;';const rows =(chart.edgeEvents||[]).slice().sort((a,b)=>a.beat-b.beat||a.edgeId-b.edgeId).map(ev =>{const lfS =ev.beat + (ev.interval||0);const lfE =lfS + (ev.lfSpread||0);return `<tr>
        <td style="padding:4px 6px;">E${ev.edgeId}</td>
        <td style="padding:4px 6px;font-family:monospace;opacity:.8;">${ev.id}</td>
        <td style="padding:4px 6px;">${Number(ev.beat).toFixed(3)}</td>
        <td style="padding:4px 6px;">${lfS.toFixed(3)}</td>
        <td style="padding:4px 6px;">${lfE.toFixed(3)}</td>
        <td style="padding:4px 6px;">
          <button data-act="go" data-id="${ev.id}">定位</button>
        </td>
      </tr>`;}).join('') ||'<tr><td colspan="6" style="padding:8px;opacity:.7;">暂无边线事件</td></tr>';dlg.innerHTML =`
      <div style="width:560px;max-height:70vh;overflow:auto;background:#1a1d26;border:1px solid #374151;border-radius:10px;padding:14px;color:#fff;">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px;">
          <strong>边线事件列表</strong>
          <button id="edge-list-close">关闭</button>
        </div>
        <table style="width:100%;border-collapse:collapse;font-size:12px;">
          <thead><tr>
            <th align="left">边</th><th align="left">id</th><th align="left">LO起</th><th align="left">LF起</th><th align="left">LF终</th><th></th>
          </tr></thead>
          <tbody>${rows}</tbody>
        </table>
      </div>`;document.body.appendChild(dlg);dlg.querySelector('#edge-list-close').onclick =() =>dlg.remove();dlg.querySelectorAll('button[data-act]').forEach(btn =>{btn.onclick =() =>{const id =btn.getAttribute('data-id');const ev =chart.edgeEvents.find(e =>e.id ===id);if (!ev) return;const act =btn.getAttribute('data-act');if (act ==='go' ||act ==='sel' ||act ==='inf') {engine.selectedEdgeId =ev.id;engine.selectedEdgeRef =ev;engine.selectedNoteId =null;engine.selectedGlobalId =null;engine.scrollBeat =Math.max(0,ev.beat);if (act ==='inf') engine.edgeInfluenceId =ev.id;window.updateEdgePanel();updateStatus();}
if (act ==='go') dlg.remove();};});};const AUTOSAVE_KEY ='chart-editor-autosave-v1';function serializeChartLite() {try {const json =chart.toJSON();return JSON.stringify({chart:json,hasAudioName:chart.audioData &&chart.audioData.name ||null,ts:Date.now()
});} catch (e) {return null;}
}
function autosave() {const s =serializeChartLite();if (!s) return;try {localStorage.setItem(AUTOSAVE_KEY,s);} catch (e) {}
}
setInterval(autosave,8000);window.addEventListener('beforeunload',autosave);try {const raw =localStorage.getItem(AUTOSAVE_KEY);if (raw) {const data =JSON.parse(raw);if (data &&data.chart &&(!chart.notes.length &&!(chart.edgeEvents||[]).length)) {const nc =Chart.fromJSON(data.chart);chart.title =nc.title;chart.artist =nc.artist;chart.charter =nc.charter;chart.difficulty =nc.difficulty;chart.offset =nc.offset;chart.baseBPM =nc.baseBPM;chart.bpmList =nc.bpmList;chart.baseSpeed =nc.baseSpeed;chart.notes =nc.notes;chart.edgeEvents =nc.edgeEvents ||[];chart.globalEvents =nc.globalEvents ||[];if (chart.ensureUniqueIds) chart.ensureUniqueIds();if (inputBPM) inputBPM.value =chart.baseBPM;if (inputOffset) inputOffset.value =Math.round((chart.offset ||0) *1000);updateStatus();const tip =document.createElement('div');tip.style.cssText ='position:fixed;top:12px;left:50%;transform:translateX(-50%);background:#1f2833;border:1px solid #45a29e;color:#c5c6c7;padding:6px 12px;border-radius:6px;z-index:2500;font-size:12px;';tip.textContent ='已恢复本地自动保存谱面（音频需重新导入）';document.body.appendChild(tip);setTimeout(() =>tip.remove(),3200);}
}
} catch (e) {}
 const _exec =commands.execute.bind(commands);commands.execute =(c) =>{_exec(c);autosave();if (window.refreshOverlapPanel) window.refreshOverlapPanel();};renderOverlapPanel();updateStatus();}
let chart,audioEngine,renderer,inputHandler,commandStack;let __lastRenderError ='';let __renderErrorCount =0;(function ensureGetTotalBeats() {function computeTotalBeats(c) {try {let maxBeat =0;for (const n of (c.notes ||[])) {const e =n.type ==='hold' &&n.endBeat !=null ?n.endBeat :n.beat;if (e >maxBeat) maxBeat =e;}
for (const ev of (c.edgeEvents ||[])) {const e =(typeof ev.endBeat ==='number') ?ev.endBeat :((ev.beat ||0) + (ev.interval ||0) + (ev.lfSpread ||0));if (e >maxBeat) maxBeat =e;}
for (const g of (c.globalEvents ||[])) {const e =(g.beat ||0) + (g.duration ||0);if (e >maxBeat) maxBeat =e;}
if (c.audioData &&c.audioData.duration &&typeof c.timeToBeat ==='function') {const ab =c.timeToBeat(c.audioData.duration);if (ab >maxBeat) maxBeat =ab;}
return Math.max(16,maxBeat + 4);} catch (e) {return 64;}
}
window.__getTotalBeats =function (c) {if (c &&typeof c.getTotalBeats ==='function') {try {return c.getTotalBeats();} catch (e) {}
}
return computeTotalBeats(c ||{});};if (typeof Chart !=='undefined') {Chart.prototype.getTotalBeats =function () {return computeTotalBeats(this);};}
})();function init() {if (typeof Chart !=='undefined' &&typeof Chart.prototype.getTotalBeats !=='function') {Chart.prototype.getTotalBeats =function () {return window.__getTotalBeats(this);};}
chart =new Chart();if (typeof chart.getTotalBeats !=='function') {chart.getTotalBeats =function () {return window.__getTotalBeats(this);};}
audioEngine =new AudioEngine();commandStack =new CommandStack();const canvas =document.getElementById('editor-canvas');renderer =new Renderer(canvas);renderer.resize();inputHandler =new InputHandler(canvas,renderer,chart,audioEngine,commandStack);initUI(chart,audioEngine,renderer,inputHandler,commandStack);renderer.scrollBeat =0;renderer.currentBeat =0;if (typeof updateStatus ==='function') updateStatus();else if (window.updateStatus) window.updateStatus();requestAnimationFrame(loop);}
function loop() {try {if (audioEngine.isPlaying) {const t =audioEngine.getCurrentTime();renderer.currentBeat =chart.timeToBeat(t);if (audioEngine.duration &&t >=audioEngine.duration - 0.001) {audioEngine.stop();renderer.isPlaying =false;document.getElementById('btn-play').textContent ='▶';}
}
renderer.edgeAddMode =inputHandler.edgeAddMode;renderer.edgeAddSelected =inputHandler.edgeAddSelected;const curBeat =audioEngine.isPlaying ?renderer.currentBeat :renderer.scrollBeat;const curTime =Math.max(0,chart.beatToTime(curBeat));const mm =Math.floor(curTime / 60);const ss =Math.floor(curTime % 60);const ms =Math.floor((curTime % 1) *1000);document.getElementById('time-display').textContent =`${String(mm).padStart(2, '0')}:${String(ss).padStart(2, '0')}.${String(ms).padStart(3, '0')}`;document.getElementById('beat-display').textContent =`| Beat: ${curBeat.toFixed(2)}`;if (window.updateAudioBar) window.updateAudioBar();if (window.__tickMetronome) window.__tickMetronome();if (audioEngine.isPlaying &&renderer.loopStart !=null &&renderer.loopEnd !=null) {const now =audioEngine.getCurrentTime();const end =Math.max(renderer.loopStart,renderer.loopEnd);if (now >=chart.beatToTime(end) - 0.005) {audioEngine.pause();const t0 =chart.beatToTime(Math.min(renderer.loopStart,renderer.loopEnd));audioEngine.seek(Math.max(0,t0));audioEngine.play();}
}
renderer.render(chart);} catch (err) {const msg =String(err &&err.message ?err.message :err);if (msg !==__lastRenderError) {__lastRenderError =msg;__renderErrorCount =1;console.error('loop',err);} else {__renderErrorCount++;if (__renderErrorCount <=3 ||__renderErrorCount % 500 ===0)
console.error('loop x' + __renderErrorCount,err);}
}
requestAnimationFrame(loop);}
window.addEventListener('DOMContentLoaded',init);</script>
</body></html>
